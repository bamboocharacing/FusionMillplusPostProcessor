/*
 * Autodesk CAM Post Processor Reference
 * Parsed from https://cam.autodesk.com/posts/reference/index.html
 * Source: Generated by Autodesk, Inc. 17 April 2026
 *
 * Definition file generated on: Thursday, June 18, 2026 2:17 PM
 */
namespace Fusion {
    type Integer = Number;
    type PropertyMap = Map<any, any>;
    type PropertySet = Map<any, any>;

    /**
     * Machine axis specification. 
     */
    declare class Axis {
        /**
         * Constructs an invalid axis. 
         */
        Axis(): Axis;

        /**
         * Constructs a rotational axis.
         * @param _table Specifies the axis is located in the table as opposed to the head. 
         * @param _axis The axis vector. 
         * @param _offset The axis offset. 
         * @param _coordinate The coordinate to which the axis coordinate is bound (0:X, 1:Y, 2:Z). 
         */
        Axis(
            _table: Boolean,
        ): Axis;

        /**
         * Constructs a rotational axis.
         * @param _table Specifies the axis is located in the table as opposed to the head. 
         * @param _axis The axis vector. 
         * @param _offset The axis offset. 
         * @param _coordinate The coordinate to which the axis coordinate is bound (0:X, 1:Y, 2:Z). 
         * @param _range The valid range. 
         */
        Axis(
            _table: Boolean,
        ): Axis;

        /**
         * Returns the identifier for the axis. 
         */
        getName(): String;

        /**
         * Sets the identifier for the axis. 
         */
        setName(
            name: String
        ): void;

        /**
         * Returns the actuator type.
         * 0: linear actuator. 1: rotational actuator. 
         */
        getActuator(): Integer;

        /**
         * Sets the actuator type.
         * 0: linear actuator. 1: rotational actuator. 
         */
        setActuator(
            actuator: Integer
        ): void;

        /**
         * Returns true if the actuator is linear. 
         */
        isLinear(): Boolean;

        /**
         * Returns true if the actuator is rotational. 
         */
        isRotational(): Boolean;

        /**
         * Returns true if the axes is a fixed aggregate. That is, the axis is fixed at the given position. 
         */
        isAggregate(): Boolean;

        /**
         * Returns the resolution of the axis. 
         */
        getResolution(): Number;

        /**
         * Sets the resolution of the axis. Radians for rotational axis. 
         */
        setResolution(
            resolution: Number
        ): void;

        /**
         * Returns the axis value clamped to the resolution. 
         */
        clampToResolution(
            _value: Number
        ): Number;

        /**
         * Returns the signed resolution error for the specified axis value. 
         */
        getResolutionError(
            _value: Number
        ): Number;

        /**
         * Returns the maximum feed of the axis. 
         */
        getMaximumFeed(): Number;

        /**
         * Sets the maximum feed of the axis. 
         */
        setMaximumFeed(
            _maximumFeed: Number
        ): void;

        /**
         * Returns the rapid feed of the axis. 
         */
        getRapidFeed(): Number;

        /**
         * Sets the rapid feed of the axis. 
         */
        setRapidFeed(
            _rapidFeed: Number
        ): void;

        /**
         * Returns the angle/offset preference.
         * 0: don't care. 1: prefer positive angles/offsets. -1: prefer negative angles/offsets. 
         */
        getPreference(): Integer;

        /**
         * Sets the angle/offset preference.
         * 0: don't care -1: prefer negative angles/offsets. 1: prefer positive angles/offsets.
         * @param preference The preference. 
         */
        setPreference(
            preference: Integer
        ): void;

        /**
         * Returns the axis reset behavior.
         * 0: disabled. 1: reset to 0 at new operation. 2: reset to 0 at rewind.. 3: reset to 0 at new operation and at rewind. 
         */
        getReset(): Integer;

        /**
         * Sets the axis reset behavior.
         * 0: disabled. 1: reset to 0 at new operation. 2: reset to 0 at rewind.. 3: reset to 0 at new operation and at rewind.
         * @param reset The reset behavior. 
         */
        setReset(
            reset: Integer
        ): void;

        /**
         * Returns true if the axis is valid.
         */
        isEnabled(): Boolean;

        /**
         * Returns true if the axis is a head axis. 
         */
        isHead(): Boolean;

        /**
         * Returns true if the axis is a table axis. 
         */
        isTable(): Boolean;

        /**
         * Returns the effective axis direction. Flipped for head. 
         */
        getEffectiveAxis(): Vector;

        /**
         * Returns the axis direction. 
         */
        getAxis(): Vector;

        /**
         * Returns the axis offset. 
         */
        getOffset(): Vector;

        /**
         * Returns the axis home position. 
         */
        getHomePosition(): Number;

        /**
         * Returns the tool change position as specified in the machine definition
         * @since 50264 
         */
        getToolChangePosition(): Number;

        /**
         * Sets the tool change position
         * @since 50264 
         */
        setToolChangePosition(
            position: Number
        ): void;

        /**
         * Returns true/false whether or not a tool change position exists
         * @since 50264 
         */
        hasToolChangePosition(): Boolean;

        /**
         * Returns the axis displacement. 
         */
        getDisplacement(): Number;

        /**
         * Returns true if the axis is cyclic. Only supported for rotational axes. 
         */
        isCyclic(): Boolean;

        /**
         * Returns true if TCP is enabled for the rotary axis. 
         */
        isTCPEnabled(): Boolean;

        /**
         * Returns the range of the axis coordinate. 
         */
        getRange(): Range;

        /**
         * Returns the coordinate to which the axis coordinate is bound (0:X, 1:Y, 2:Z). Returns -1 if the axis is invalid. 
         */
        getCoordinate(): Integer;

        /**
         * Returns true if the specified angle/offset is within the required range.
         * @param value The coordinate (angular for rotational axis and spatial for linear axis). 
         */
        isSupported(
            value: Number
        ): Boolean;

        /**
         * Returns the closest valid angle/offset.
         * @param value The coordinate (angular for rotational axis and spatial for linear axis). 
         */
        clamp(
            value: Number
        ): Number;

        /**
         * Returns the reduced angle closest to 0 but restricted to the supported range. Actuator must be rotational.
         * @param value The axis coordinate. 
         */
        reduce(
            value: Number
        ): Number;

        /**
         * Returns the preferred angle in the allowed range. An exception is raised if no valid angle exists. Actuator must be rotational. 
         */
        remapToRange(
            angle: Number
        ): Number;

        /**
         * Returns the preferred angle in the allowed range closest to the current angle. An exception is raised if no valid angle exists. Actuator must be rotational. 
         */
        remapToRange2(
            angle: Number,
        ): Number;

        /**
         * Returns the rotation for the given axis position. Returns the identity matrix for a linear axis. 
         */
        getAxisRotation(
            position: Number
        ): Matrix;

    }

    /**
     * Class format {@link Base64} encoding/decoding.
     * @since 41349 
     */
    declare class Base64 {
        /**
         * Encodes the given text in {@link Base64} format.
         * @param text The unencoded test. 
         */
        static btoa(
            text: String
        ): String;

        /**
         * Decodes the given string in {@link Base64} format.
         * @param text The encoded text. 
         */
        static atob(
            text: String
        ): String;

    }

    /**
     * Class for binary file load and save.
     * Security: Requires security level 0.
     * @since 41349 
     */
    declare class BinaryFile {
        /**
         * Load binary file as string.
         * @param path The path of the file.
         */
        static loadBinary(
            path: String
        ): String;

        /**
         * Save string as binary file. Each byte must be represented as its own character.
         * @param path The path of the file. 
         * @param data Specifies if the data to write. 
         */
        static saveBinary(
            path: String,
        ): void;

    }

    /**
     * 3D bounding box specified by the lower and upper corners. 
     */
    declare class BoundingBox {
        /**
         * Initializes the bounding box and (0, 0, 0) -&gt; (0, 0, 0). 
         */
        BoundingBox(): BoundingBox;

        /**
         * Initializes the bounding box for the specified corners.
         * @param a Corner of box. 
         * @param b Corner of box. 
         */
        BoundingBox(
            a: Vector,
        ): BoundingBox;

        /**
         * Expands the bounding box to include the specified position. 
         */
        expandTo(
            point: Vector
        ): void;

        /**
         * Expands the bounding box to include the specified bounding box. 
         */
        expandToBox(
            box: BoundingBox
        ): void;

        /**
         * Gets intersection between the bounding box and a ray.
         * @param origin Origin point of the ray. 
         * @param direction Direction vector of the ray. Should be non-zero. 
         * @param expansion Additional thickness added to the bounding box. Should be non-negative. 
         * @since r45500 
         */
        getRayIntersection(
            origin: Vector,
        ): VectorPair;

        /**
         * Compares two bounding boxes.
         * - BOXES_ARE_DIFFERENT - The bounding boxes are different.
         * - BOXES_ARE_SAME - The bounding boxes match at the same position and size.
         * - BOXES_ARE_TRANSLATED - The bounding boxes have the same width and length but at a different position.
         * @param box1 First box to compare. 
         * @param box2 Second box to compare. 
         * @param tolerance An optional comparison tolerance/accuracy. If 'tolerance' is greater than or equal to 1 then 'tolerance' specifies the number of digits to the right of the decimal to truncate the bounding box coordinates to prior to comparison. 
         * @since r51870 
         */
        static compare(
            box1: BoundingBox,
        ): Integer;

        lower: Vector;

        upper: Vector;

    }

    /**
     * A 2D canvas for drawing. 
     */
    declare class Canvas {
        /**
         * Creates a new canvas frame buffer. 
         */
        Canvas(
            _width: Integer,
        ): Canvas;

        /**
         * Returns the width of the canvas. 
         */
        getWidth(): Integer;

        /**
         * Returns the height of the canvas. 
         */
        getHeight(): Integer;

        /**
         * Clears the canvas. 
         */
        clear(
            rgba: Integer
        ): void;

        /**
         * Returns the RGBA color of the specified pixel. 
         */
        getColor(
            x: Integer,
        ): Color;

        /**
         * Returns the RGBA color of the specified pixel. 
         */
        setColor(
            x: Integer,
        ): void;

        /**
         * Returns the RGBA color of the specified pixel. 
         */
        getPixel(
            x: Integer,
        ): Integer;

        /**
         * Sets the RGBA color of the specified pixel. 
         */
        setPixel(
            x: Integer,
        ): void;

        /**
         * Exports the canvas to the specified path.
         * @param path The path of the destination file. 
         * @param mimetype The MIME type of the image (must be "image/png"). 
         */
        saveImage(
            path: String,
        ): void;

        /**
         * Loads the specified picture into a new {@link Canvas}.
         * @param path The path of the source file. 
         * @param mimetype The MIME type of the image (must be "image/png"). 
         */
        static loadImage(
            path: String,
        ): Canvas;

    }

    /**
     * A circular motion. 
     */
    declare class CircularMotion {
        /**
         * Returns the position at the specified u coordinate for the current motion (u is clamped to [0; 1]). 
         */
        getPositionU(
            u: Number
        ): Vector;

        /**
         * Helical offset. 
         */
        getOffset(): Number;

    }

    /**
     * {@link Color} specified by red, green, blue, and alpha values. 
     */
    declare class Color {
        /**
         * Initializes a new color as opaque white. 
         */
        Color(): Color;

        /**
         * Initializes a new color as opaque (i.e. alpha equal to 1). All arguments are clamped to the range [0; 1].
         * @param red Red component. 
         * @param green Green component. 
         * @param blue Blue component. 
         */
        Color(
            red: Number,
        ): Color;

        /**
         * Initializes a new color. All arguments are clamped to the range [0; 1].
         * @param red Red component. 
         * @param green Green component. 
         * @param blue Blue component. 
         * @param alpha Alpha component. 
         */
        Color(
            red: Number,
        ): Color;

        /**
         * Returns the red component. 
         */
        getRed(): Number;

        /**
         * Returns the green component. 
         */
        getGreen(): Number;

        /**
         * Returns the blue component. 
         */
        getBlue(): Number;

        /**
         * Returns the alpha component. 
         */
        getAlpha(): Number;

        /**
         * Converts the color to a string (e.g. [0.25, 0.25, 0.75, 1]). 
         */
        toString(): String;

    }

    /**
     * A 2D curve.
     * @since 41366 
     */
    declare class Curve {
        /**
         * Returns the number of entities in the curve. 
         */
        getNumberOfEntities(): Integer;

        /**
         * Returns the entity at the given index. 
         */
        getEntity(
            index: Integer
        ): CurveEntity;

        /**
         * Returns true if the curve is closed. 
         */
        isClosed(): Boolean;

        /**
         * Returns true if the curve has arcs. 
         */
        hasArcs(): Boolean;

        /**
         * Returns the length of the curve. 
         */
        getLength(): Number;

        /**
         * Returns the extent of the curve. 
         */
        getExtent(): BoundingBox;

        /**
         * Returns the linearization of the {@link Curve}.
         * @param tolerance The linearization tolerance. 
         */
        getLinearize(
            tolerance: Number
        ): Curve;

    }

    /**
     * {@link Curve} entity.
     * @since 41366 
     */
    declare class CurveEntity {
        /**
         * Constructs a {@link Curve} entity. 
         */
        CurveEntity(): CurveEntity;

        /**
         * Returns the length of the entity. 
         */
        getLength(): Number;

        /**
         * Returns the radius of the arc entity as defined by the end and center. 
         */
        getRadius(): Number;

        /**
         * Returns true if the arc entity if big. Sweep &gt; 180deg. 
         */
        isBigArc(): Boolean;

        /**
         * Returns the sweep for the arc entity. 
         */
        getSweep(): Number;

        /**
         * Reverses the entity. 
         */
        reverse(): void;

        /**
         * Translates the entity. Z is ignored. 
         */
        translate(
            offset: Vector
        ): void;

        /**
         * Makes a line entity. 
         */
        static getLine(
            start: Vector,
        ): CurveEntity;

        /**
         * Makes an arc entity. 
         */
        static getArc(
            start: Vector,
        ): CurveEntity;

        /* True for arc. Otherwise a line. */
        arc: Boolean;

        /* The direction of the arc. Clockwise or counterclockwise. */
        clockwise: Boolean;

        /* The start position. The Z coordinate is always 0. */
        start: Vector;

        /* The center for an arc. The Z coordinate is always 0. */
        center: Vector;

        /* The end position. The Z coordinate is always 0. */
        end: Vector;

    }

    /**
     * Deferred variables handling class.
     * This class is used for handling values, which are not known at the time they are output. For example, reference to a sub-program's line number. Use the following code to output a deferred variable. 
     * Use the following code at the place where the variable is known and gets defined (for example, the line number becomes known). 
     * Deferred variable ids will be replaced with the value of the deferred variables at the end of processing.
     * - 'id' is the unique string identifier of the deferred variable when it was defined using the 'DeferredVariable.set' function.
     * - 'format' is optional and is the FormatNumber used to format a numeric variable.
     * - 'id' is a unique identifier assigned to the deferred variable.
     * - 'value' is the value to assign to the deferred variable.
     * @since r45238 
     */
    declare class DeferredVariables {
        /**
         * Function for retrieving the deferred variable for output.
         * @param id An unique string identifier of deferred variable to output. 
         * @param format FormatNumber used to format a numeric variable. 'format' is optional and won't be used to format non-numeric values. If it is not specified, then numeric values are not formatted prior to output.
         */
        static get(
            id: String,
        ): String;

        /**
         * Function for setting deferred variable's value.
         * @param id An unique string identifier of variable to output. 
         * @param value Actual value of variable. 
         */
        static set(
            id: String,
        ): void;

        static separator: String;

    }

    /**
     * This class provides access to the file system.
     * Security: Requires security level 0. 
     */
    declare class FileSystem {
        /**
         * Returns the combined paths.
         * @param rootPath The tool path. 
         * @param relativePath The relative path. 
         */
        static getCombinedPath(
            rootPath: String,
        ): String;

        /**
         * Returns the path of the parent folder.
         * @param path The path. 
         */
        static getFolderPath(
            path: String
        ): String;

        /**
         * Returns the filename from specified path.
         * @param path The path.
         * @since r16737 
         */
        static getFilename(
            path: String
        ): String;

        /**
         * Replaces the extension of the specified path. 
         */
        static replaceExtension(
            path: String,
        ): String;

        /**
         * Creates the specified folder.
         * @param path The path of the folder. 
         */
        static makeFolder(
            path: String
        ): void;

        /**
         * Returns true if the specified folder exists.
         * @param path The path of the folder. 
         */
        static isFolder(
            path: String
        ): Boolean;

        /**
         * Returns true if the specified file exists.
         * @param path The path of the file. 
         */
        static isFile(
            path: String
        ): Boolean;

        /**
         * Copies the specified file from the source path to the destination path.
         * @param src The path of the source file. 
         * @param dest The path of the destination file. 
         */
        static copyFile(
            src: String,
        ): void;

        /**
         * Moves the specified file from the source path to the destination path.
         * @param src The path of the source file. 
         * @param dest The path of the destination file. 
         */
        static moveFile(
            src: String,
        ): void;

        /**
         * Removes the specified file. 
         */
        static remove(
            path: String
        ): void;

        /**
         * Removes the specified folder. 
         */
        static removeFolder(
            path: String
        ): void;

        /**
         * Removes the specified folder recursively. 
         */
        static removeFolderRecursive(
            path: String
        ): void;

        /**
         * Returns the size of the specified file.
         * @param path The path of the file. 
         */
        static getFileSize(
            path: String
        ): Integer;

        /**
         * Returns the path of the primary temporary folder. 
         */
        static getTemporaryFolder(): String;

        /**
         * Returns the path of a new temporary file.
         * @param prefix The prefix of the file. 
         */
        static getTemporaryFile(
            prefix: String
        ): String;

    }

    /**
     * {@link Format} specification and formatter. 
     */
    declare class Format {
        /**
         * Constructs the format specification for the given values. See createFormat().
         * The supported specifiers are:
         * @param specifiers The format specifiers.
         */
        Format(
            specifiers: Map<any, any> // This generic type have been defaulted to any types.
        ): Format;

        /**
         * Returns the formatted string for the specified value. 
         */
        format(
            value: Number
        ): String;

        /**
         * Returns the resulting value for the specified value.
         */
        getResultingValue(
            value: Number
        ): Number;

        /**
         * Returns the error for the specified value.
         */
        getError(
            value: Number
        ): Number;

        /**
         * Returns true if the specified value would be non-zero when formatted. See {@link areDifferent()} for comparing 2 values.
         */
        isSignificant(
            value: Number
        ): Boolean;

        /**
         * Returns true if the specified values are different when formatted.
         * @since r19569 
         */
        areDifferent(
            a: Number,
        ): Boolean;

        /**
         * Returns the minimum epsilon value.
         */
        getMinimumValue(): Number;

    }

    /**
     * This class converts numbers into strings in accordance with several modifiers.
     * When value remapping is enabled the cyclic mapping is done first, then the scaling, and finally the offset. 
     */
    declare class FormatNumber {
        /**
         * Constructs a number formatting object. 
         */
        FormatNumber(): FormatNumber;

        /**
         * Returns the decimal separator. 
         */
        getDecimalSymbol(): Integer;

        /**
         * Sets the decimal separator. 
         */
        setDecimalSymbol(
            decimalSymbol: Integer
        ): void;

        /**
         * Returns the zero padding mode. Deprecated since r45877. 
         */
        getZeroPad(): Boolean;

        /**
         * Sets zero padding mode. Deprecated since r45877. 
         */
        setZeroPad(
            zeroPad: Boolean
        ): void;

        /**
         * Returns the force sign flag. 
         */
        getForceSign(): Boolean;

        /**
         * Sets the force sign flag. 
         */
        setForceSign(
            forceSign: Boolean
        ): void;

        /**
         * Returns the force decimal flag. Deprecated since r45877. 
         */
        getForceDecimal(): Boolean;

        /**
         * Sets the force decimal flag. Deprecated since r45877. 
         */
        setForceDecimal(
            forceDecimal: Boolean
        ): void;

        /**
         * Returns the width. Deprecated since r45877. 
         */
        getWidth(): Integer;

        /**
         * Sets the width. Deprecated since r45877. 
         */
        setWidth(
            width: Integer
        ): void;

        /**
         * Returns the number of decimals. 
         */
        getNumberOfDecimals(): Integer;

        /**
         * Sets the number of decimals. 
         */
        setNumberOfDecimals(
            numberOfDecimals: Integer
        ): void;

        /**
         * Returns true if the suffix zero decimals should be skipped. Deprecated since r45877. 
         */
        getTrimZeroDecimals(): Boolean;

        /**
         * Sets the skip zero decimals flag. Deprecated since r45877. 
         */
        setTrimZeroDecimals(
            trimZeroDecimals: Boolean
        ): void;

        /**
         * Returns true if the leading zero should be skipped. Deprecated since r45877. 
         */
        getTrimLeadZero(): Boolean;

        /**
         * Sets the skip leading zero flag. Deprecated since r45877. 
         */
        setTrimLeadZero(
            trimLeadZero: Boolean
        ): void;

        /**
         * Returns the value after cyclic mapping, scaling, and offset. 
         */
        remap(
            value: Number
        ): Number;

        /**
         * Returns the value after removing the any scaling and offset factors. 
         */
        unmap(
            value: Number
        ): Number;

        /**
         * Returns the cyclic limit. Deprecated since r45877. 
         */
        getCyclicLimit(): Number;

        /**
         * Returns the cyclic sign. Deprecated since r45877. 
         */
        getCyclicSign(): Integer;

        /**
         * Sets the cyclic mapping.
         * For sign -1 the values will be mapped to the range [-cyclicLimit; 0] For sign 0 the values will be mapped to the range [-cyclicLimit; cyclicLimit] For sign 1 the values will be mapped to the range [0; cyclicLimit]
         * The cyclic mapping is done before scaling and offset. Deprecated since r45877. 
         * @param limit Specifies the cyclic limit. Must be positive or 0. Cyclic mapping will be disabled when set to 0. 
         * @param sign Specifies the sign. Must in -1, 0, or 1.
         */
        setCyclicMapping(
            limit: Number,
        ): void;

        /**
         * Returns the scale. 
         */
        getScale(): Number;

        /**
         * Sets the scale. 
         */
        setScale(
            scale: Number
        ): void;

        /**
         * Returns the offset. 
         */
        getOffset(): Number;

        /**
         * Sets the offset. 
         */
        setOffset(
            offset: Number
        ): void;

        /**
         * Returns the prefix. 
         */
        getPrefix(): String;

        /**
         * Sets the prefix. 
         */
        setPrefix(
            prefix: String
        ): void;

        /**
         * Returns the suffix. 
         */
        getSuffix(): String;

        /**
         * Sets the suffix. 
         */
        setSuffix(
            suffix: String
        ): void;

        /**
         * Returns the base number. 
         * @since r45877. 
         */
        getBase(): Number;

        /**
         * Sets the base number. 
         * @since r45877. 
         */
        setBase(
            base: Number
        ): void;

        /**
         * Returns the minimum allowed value. 
         * @since r45877. 
         */
        getMinimum(): Number;

        /**
         * Sets the minimum allowed value. 
         * @since r45877. 
         */
        setMinimum(
            value: Number
        ): void;

        /**
         * Returns the maximum allowed value. 
         * @since r45877. 
         */
        getMaximum(): Number;

        /**
         * Sets the maximum allowed value. 
         * @since r45877. 
         */
        setMaximum(
            value: Number
        ): void;

        /**
         * Returns the minimum number of digits to the left of the decimal point. 
         * @since r45877. 
         */
        getMinDigitsLeft(): Integer;

        /**
         * Sets the minimum number of digits to the left of the decimal point. 
         * @since r45877. 
         */
        setMinDigitsLeft(
            value: Integer
        ): void;

        /**
         * Returns the minimum number of digits to the right of the decimal point. 
         * @since r45877. 
         */
        getMinDigitsRight(): Integer;

        /**
         * Sets the minimum number of digits to the right of the decimal point. 
         * @since r45877. 
         */
        setMinDigitsRight(
            value: Integer
        ): void;

        /**
         * Returns the type of formatted value. 
         * @since r45877. 
         */
        getType(): Integer;

        /**
         * Sets the type of formatted value. Can be FORMAT_INTEGER, FORMAT_REAL, FORMAT_LZS, FORMAT_TZS. 
         * @since r45877. 
         */
        setType(
            type: Integer
        ): void;

        /**
         * Returns the string for the specified value. 
         */
        format(
            value: Number
        ): String;

        /**
         * Returns true if the specified value would be non-zero when formatted. When comparing 2 values use {@link areDifferent()} instead. 
         */
        isSignificant(
            value: Number
        ): Boolean;

        /**
         * Returns true if the specified values are different when formatted. 
         */
        areDifferent(
            a: Number,
        ): Boolean;

        /**
         * Returns the minimum epsilon value.
         */
        getMinimumValue(): Number;

        /**
         * Returns the resulting value for the specified value. 
         */
        getResultingValue(
            value: Number
        ): Number;

        /**
         * Returns the error for the specified value. 
         */
        getError(
            value: Number
        ): Number;

        /* The number of decimals. */
        decimals: Integer;

        /* The trailing zero decimals should be skipped. Deprecated since r45877. */
        trimZeroDecimals: Boolean;

        /* The leading zero should be excluded. Eg. ".123" instead of "0.123". Deprecated since r45877. */
        trimLeadZero: Boolean;

        /* The returned string should be padded with zeros to fit the width. Deprecated since r45877. */
        zeroPad: Boolean;

        /* The minimum width of the formattedd number. Deprecated since r45877. */
        width: Integer;

        /* The minumum digits to the left of decimal point to output. Since r45877. */
        minDigitsLeft: Integer;

        /* The minumum digits to the right of decimal point to output. Since r45877. */
        minDigitsRight: Integer;

        /* The format type: FORMAT_REAL, FORMAT_INTEGER, FORMAT_LZS, FORMAT_TZS. Since r45877. */
        type: Integer;

        /* The decimal symbol. */
        decimalSymbol: Integer;

        /* Forces the '+' sign on positive numbers. */
        forceSign: Boolean;

        /* Forces the decimal separator in the formatted number. Deprecated since r45877. */
        forceDecimal: Boolean;

        /* The cyclic limit. Deprecated since r45877. */
        cyclicLimit: Number;

        /* The cyclic sign. Only valid if cyclicLimit &gt; 0. Deprecated since r45877. */
        cyclicSign: Integer;

        /* The scaling factor for the output number. */
        scale: Number;

        /* The offset for the output number. */
        offset: Number;

        /* The minimum increment to output. Since r45877. */
        base: Number;

        /* The unsigned minimum value. Since r45877. */
        minimum: Number;

        /* The unsigned maximum value. Since r45877. */
        maximum: Number;

        /* The prefix. */
        prefix: String;

        /* The suffix. */
        suffix: String;

    }

    /**
     * {@link FormData} for use with {@link XMLHttpRequest}.
     * @since 41366 
     */
    declare class FormData {
        /**
         * Constructs a new {@link FormData} object. 
         */
        FormData(): FormData;

        /**
         * Append new value.
         * @param name The key. 
         * @param value The value. 
         * @param filename The filename. 
         */
        append(
            name: String,
        ): void;

        /**
         * Returns true if the given key exists.
         * @param name The key of the data. 
         */
        has(
            name: String
        ): Boolean;

        /**
         * Returns the value for the given key. Returns empty if key doesn't exist.
         * @param name The key of the data. 
         */
        get(
            name: String
        ): String;

    }

    /**
     * {@link Holder}. 
     */
    declare class Holder {
        /**
         * Returns true if the holder has any sections. 
         */
        hasSections(): Boolean;

        /**
         * Returns the number of sections. 
         */
        getNumberOfSections(): Integer;

        /**
         * Returns the maximum diameter of the holder. 
         */
        getMaximumDiameter(): Number;

        /**
         * Returns the length of the holder. 
         */
        getTotalLength(): Number;

        /**
         * Returns the gauge length of the holder. 
         */
        getGaugeLength(): Number;

        /**
         * Returns the diameter of the specified section. The section is a value in the range [0; {@link getNumberOfSections()}]. 
         */
        getDiameter(
            index: Integer
        ): Number;

        /**
         * Returns the length of the specified section. The section is a value in the range [0; {@link getNumberOfSections()}]. 
         */
        getLength(
            index: Integer
        ): Number;

        /* The maximum diameter. */
        maximumDiameter: Number;

        /* The length of the holder. */
        length: Number;

    }

    /**
     * An incremental formatting variable. State is maintained to avoid output for zero increments.
     */
    declare class IncrementalVariable {
        /**
         * Constructs a variable.
         * The supported specifiers are:
         */
        IncrementalVariable(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): IncrementalVariable;

        /**
         * Returns the formatted string of the specified value. The empty string is returned if the value is identical to the last formatted value. 
         */
        format(
            value: Number
        ): String;

        /**
         * Returns the prefix. 
         */
        getPrefix(): Value;

        /**
         * Sets the prefix. 
         */
        setPrefix(
            prefix: Value
        ): void;

        /**
         * Disable variable. When invoked {@link format()} always returns the empty string. 
         */
        disable(): void;

        /**
         * Invoke to force output of value on the next invocation of {@link format()}. {@link reset()} has no effect if force has been set to true for the given format. 
         */
        reset(): void;

        /**
         * Returns the last formatted value. 
         */
        getCurrent(): Value;

    }

    /**
     * Machine configuration. This class describes the physical machine to allow mapping from global orientations and positions to machine axis coordinates. 
     */
    declare class MachineConfiguration {
        /**
         * Returns the XML representation of the machine configuration. 
         */
        getXML(): String;

        /**
         * Constructs a new 3D machine configuration. 
         */
        MachineConfiguration(): MachineConfiguration;

        /**
         * Constructs a new 4-axis machine configuration.
         * @param u The U axis. 
         */
        MachineConfiguration(
            u: Axis
        ): MachineConfiguration;

        /**
         * Constructs a new 5-axis machine configuration.
         * @param u The U axis. 
         * @param v The V axis. 
         */
        MachineConfiguration(
            u: Axis,
        ): MachineConfiguration;

        /**
         * Constructs a new 6-axis machine configuration.
         * @param u The U axis. 
         * @param v The V axis. 
         * @param w The W axis. 
         */
        MachineConfiguration(
            u: Axis,
        ): MachineConfiguration;

        /**
         * Returns true if milling is supported. 
         */
        getMilling(): Boolean;

        /**
         * Sets if milling is supported. 
         */
        setMilling(
            milling: Boolean
        ): void;

        /**
         * Returns true if turning is supported. 
         */
        getTurning(): Boolean;

        /**
         * Sets if turning is supported. 
         */
        setTurning(
            turning: Boolean
        ): void;

        /**
         * Returns true if wire is supported. 
         */
        getWire(): Boolean;

        /**
         * Sets if wire is supported. 
         */
        setWire(
            wire: Boolean
        ): void;

        /**
         * Returns true if jet is supported (waterjet, laser cutter, and plasma cutter). 
         */
        getJet(): Boolean;

        /**
         * Sets if jet is supported (waterjet, laser cutter, and plasma cutter). 
         */
        setJet(
            jet: Boolean
        ): void;

        /**
         * Returns true if additive fff is supported. 
         */
        getAdditiveFFF(): Boolean;

        /**
         * Sets if additive fff is supported. 
         */
        setAdditiveFFF(
            additive: Boolean
        ): void;

        /**
         * Returns true if inspection is supported. 
         */
        getInspection(): Boolean;

        /**
         * Sets true if inspection is supported. 
         */
        setInspection(
            inspection: Boolean
        ): void;

        /**
         * Returns true if an automatic tool changer is available. 
         */
        getToolChanger(): Boolean;

        /**
         * Sets if an automatic tool changer is available. 
         */
        setToolChanger(
            toolChanger: Boolean
        ): void;

        /**
         * Returns true if a tool preload is supported. 
         */
        getToolPreload(): Boolean;

        /**
         * Sets the tool preload. 
         */
        setToolPreload(
            toolPreload: Boolean
        ): void;

        /**
         * Returns the maximum number of tools. 
         */
        getNumberOfTools(): Integer;

        /**
         * Sets the maximum number of tools. 
         */
        setNumberOfTools(
            numberOfTools: Integer
        ): void;

        /**
         * Returns the maximum tool length. 
         */
        getMaximumToolLength(): Number;

        /**
         * Sets the maximum tool length. 
         */
        setMaximumToolLength(
            maximumToolLength: Number
        ): void;

        /**
         * Returns the maximum tool diameter. 
         */
        getMaximumToolDiameter(): Number;

        /**
         * Sets the maximum tool diameter. 
         */
        setMaximumToolDiameter(
            maximumToolDiameter: Number
        ): void;

        /**
         * Returns the maximum tool weight. 
         */
        getMaximumToolWeight(): Number;

        /**
         * Sets the maximum tool weight. 
         */
        setMaximumToolWeight(
            maximumToolWeight: Number
        ): void;

        /**
         * Returns the tool interface.
         * @since r50262 
         */
        getToolInterface(): String;

        /**
         * Sets the tool interface.
         * @since r50262 
         */
        setToolInterface(
            toolInterface: String
        ): void;

        /**
         * Returns the maximum feedrate. 
         */
        getMaximumFeedrate(): Number;

        /**
         * Sets the maximum feedrate. 
         */
        setMaximumFeedrate(
            maximumFeedrate: Number
        ): void;

        /**
         * Returns the maximum cutting feedrate. 
         */
        getMaximumCuttingFeedrate(): Number;

        /**
         * Sets the maximum cutting feedrate. 
         */
        setMaximumCuttingFeedrate(
            maximumCuttingFeedrate: Number
        ): void;

        /**
         * Returns the maximum block processing speed. 
         */
        getMaximumBlockProcessingSpeed(): Integer;

        /**
         * Sets the maximum block processing speed. 
         */
        setMaximumBlockProcessingSpeed(
            maximumBlockProcessingSpeed: Integer
        ): void;

        /**
         * Returns the number of work offsets. 
         */
        getNumberOfWorkOffsets(): Integer;

        /**
         * Sets the number of work offsets. 
         */
        setNumberOfWorkOffsets(
            numberOfWorkOffsets: Integer
        ): void;

        /**
         * Returns the feedrate ratio. 
         */
        getFeedrateRatio(): Number;

        /**
         * Sets the feedrate ratio. 
         */
        setFeedrateRatio(
            feedrateRatio: Number
        ): void;

        /**
         * Returns the tool change time. 
         */
        getToolChangeTime(): Number;

        /**
         * Sets the tool change time. 
         */
        setToolChangeTime(
            toolChangeTime: Number
        ): void;

        /**
         * Adds the specified axis. 
         */
        addAxis(
            axis: Axis
        ): void;

        /**
         * Returns whether the machine should rewind when needed. 
         */
        performRewinds(): Boolean;

        /**
         * Enables machine rewinds. 
         */
        enableMachineRewinds(): void;

        /**
         * Disables machine rewinds. 
         */
        disableMachineRewinds(): void;

        /**
         * Defines the machine rewinds preference. 
         * @param rewindPreference - Defines the preferred method for repositioning the tool and rotary axes at a retract and reconfigure operation. REWIND_AT_LINEAR performs retract and reconfigure only when necessary. REWIND_AT_RAPID will attempt to perform the retract and reconfigure at rapid (positioning) moves. 
         * @param retractPreference - REWIND_RETRACT_AT_APEX retracts the tool at a retract and reconfigure. REWIND_STAY_AT_APEX will leave the tool in contact with the part when the rotary axes are repositioned. The tool must be perpendicular to the rotary axis rotational vector (so that only one rotary axis will move) and TCP must be enabled for the operation.
         * @since r52040 
         */
        setMachineRewindPreference(
            rewindPreference: Integer,
        ): void;

        /**
         * Returns whether machine config allows retract and reconfigure. 
         * @deprecated Use performRewinds() instead. 
         */
        getReconfigure(): Boolean;

        /**
         * Sets the reconfigure. 
         * @deprecated Use enableMachineRewinds() and disableMachineRewinds() instead. 
         */
        setReconfigure(
            enable: Boolean
        ): void;

        /**
         * Sets the values by which the stock should be expanded when computing retract moves for rewinds. 
         */
        setRewindStockExpansion(
            expansion: Vector
        ): void;

        /**
         * Returns the stock expansion values used for retracts and rewinds. 
         */
        getRewindStockExpansion(): Vector;

        /**
         * Sets the safe distance value for retract moves in rewinds.
         * @since 45790 
         */
        setSafeRetractDistance(
            safeRetractDistance: Number
        ): void;

        /**
         * Returns the safe distance value for retract moves in rewinds.
         * @since 45790 
         */
        getSafeRetractDistance(): Number;

        /**
         * Sets the safe feedrate value for retract moves in rewinds.
         * @since 45790 
         */
        setSafeRetractFeedrate(
            safeRetractFeedrate: Number
        ): void;

        /**
         * Returns the safe feedrate value used for retract moves in rewinds.
         * @since 45790 
         */
        getSafeRetractFeedrate(): Number;

        /**
         * Sets the safe feedrate value for plunge moves in rewinds.
         * @since 45790 
         */
        setSafePlungeFeedrate(
            safePlungeFeedrate: Number
        ): void;

        /**
         * Returns the safe feedrate value used for plunge moves in rewinds.
         * @since 45790 
         */
        getSafePlungeFeedrate(): Number;

        /**
         * Sets the rewind strategy. Use MINIMIZE_REWINDS to minimize the number of rewinds by preferring the solution that requires the least number of rewinds before reaching the limits of the rotary axes. Use AXIS_PREFERENCE to prefer solutions that match the axis preference even if it requires more rewinds By default, it is AXIS_PREFERENCE.
         * @since r50339 
         */
        setRewindStrategy(
            rewindStrategy: Integer
        ): void;

        /**
         * Returns the rewind strategy (AXIS_PREFERENCE or MINIMIZE_REWINDS).
         * @since r50339 
         */
        getRewindStrategy(): Integer;

        /**
         * Returns the dimensions of the machine as (width, depth, height). 
         */
        getDimensions(): Vector;

        /**
         * Sets the dimensions of the machine as (width, depth, height). 
         */
        setDimensions(
            dimensions: Vector
        ): void;

        /**
         * Returns the width of the machine. 
         */
        getWidth(): Number;

        /**
         * Sets the width of the machine. 
         */
        setWidth(
            width: Number
        ): void;

        /**
         * Returns the width of the machine. 
         */
        getDepth(): Number;

        /**
         * Sets the depth of the machine. 
         */
        setDepth(
            depth: Number
        ): void;

        /**
         * Returns the width of the machine. 
         */
        getHeight(): Number;

        /**
         * Sets the height of the machine. 
         */
        setHeight(
            height: Number
        ): void;

        /**
         * Returns the weight of the machine. 
         */
        getWeight(): Number;

        /**
         * Sets the weight of the machine. 
         */
        setWeight(
            weight: Number
        ): void;

        /**
         * Returns the maximum allowed part dimensions. 
         */
        getPartDimensions(): Vector;

        /**
         * Sets the maximum allowed part dimensions. 
         */
        setPartDimensions(
            partDimensions: Vector
        ): void;

        /**
         * Returns the width of the machine. 
         */
        getPartMaximumX(): Number;

        /**
         * Sets the width of the machine. 
         */
        setPartMaximumX(
            width: Number
        ): void;

        /**
         * Returns the width of the machine. 
         */
        getPartMaximumY(): Number;

        /**
         * Sets the depth of the machine. 
         */
        setPartMaximumY(
            depth: Number
        ): void;

        /**
         * Returns the width of the machine. 
         */
        getPartMaximumZ(): Number;

        /**
         * Sets the height of the machine. 
         */
        setPartMaximumZ(
            height: Number
        ): void;

        /**
         * Returns the weight capacity. 
         */
        getWeightCapacity(): Number;

        /**
         * Sets the weight capacity. 
         */
        setWeightCapacity(
            weightCapacity: Number
        ): void;

        /**
         * Returns the spindle axis without machine rotations. 
         */
        getSpindleAxis(): Vector;

        /**
         * Sets the spindle axis without machine rotations. The default is (0, 0, 1). 
         */
        setSpindleAxis(
            spindleAxis: Vector
        ): void;

        /**
         * Returns the spindle description. 
         */
        getSpindleDescription(): String;

        /**
         * Sets the spindle description. 
         */
        setSpindleDescription(
            spindleDescription: String
        ): void;

        /**
         * Returns the maximum spindle power defined in kW or hp.
         * @since r50273 
         */
        getMaximumSpindlePower(): Number;

        /**
         * Sets the maximum spindle power defined in kW or hp.
         * @since r50273 
         */
        setMaximumSpindlePower(
            maximumSpindlePower: Number
        ): void;

        /**
         * Returns the spindle peak torque defined in Nm or ft-lb.
         * @since r50273 
         */
        getSpindlePeakTorque(): Number;

        /**
         * Sets the spindle peak torque defined in Nm or ft-lb.
         * @since r50273 
         */
        setSpindlePeakTorque(
            spindlePeakTorque: Number
        ): void;

        /**
         * Returns the spindle speed at which peak torque is reached defined in RPM.
         * @since r50273 
         */
        getSpindlePeakTorqueSpeed(): Number;

        /**
         * Sets the spindle speed at which peak torque is reached defined in RPM.
         * @since r50273 
         */
        setSpindlePeakTorqueSpeed(
            spindlePeakTorqueSpeed: Number
        ): void;

        /**
         * Returns the minimum spindle speed. 
         */
        getMinimumSpindleSpeed(): Number;

        /**
         * Sets the minimum spindle speed. 
         */
        setMinimumSpindleSpeed(
            minimumSpindleSpeed: Number
        ): void;

        /**
         * Returns the maximum spindle speed. 
         */
        getMaximumSpindleSpeed(): Number;

        /**
         * Sets the maximum spindle speed. 
         */
        setMaximumSpindleSpeed(
            maximumSpindleSpeed: Number
        ): void;

        /**
         * Returns the collect chuck. Tool-holder interface. 
         */
        getCollectChuck(): String;

        /**
         * Sets the collect chuck. Tool-holder interface. 
         */
        setCollectChuck(
            collectChuck: String
        ): void;

        /**
         * Returns the tool length used for head coordinate adjustments. 
         */
        getToolLength(): Number;

        /**
         * Defines the tool length used for head coordinate adjustments. Issues an event. 
         * @param toolLength - The tool length. 
         */
        setToolLength(
            toolLength: Number
        ): void;

        /**
         * Defines the tool length used for head coordinate adjustments. Without issuing an event. 
         * @param toolLength - The tool length. 
         */
        setToolLengthWithoutEvent(
            toolLength: Number
        ): void;

        /**
         * Returns the virtual tool end positioning setting. 
         */
        getVirtualTooltip(): Boolean;

        /**
         * Defines if the virtual tool end position should be output. 
         * @param virtualPositioning - True if the virtual tool end point position is calculated for head offsets, false if the pivot point is calculated. 
         */
        setVirtualTooltip(
            virtualPositioning: Boolean
        ): void;

        /**
         * Returns the axis by name.
         * @param name The name of the axis (i.e. "X", "Y", "Z", "U", "V", or "W"). 
         */
        getAxisByName(
            name: String
        ): Axis;

        /**
         * Returns the X axis. 
         */
        getAxisX(): Axis;

        /**
         * Returns the Y axis. 
         */
        getAxisY(): Axis;

        /**
         * Returns the Z axis. 
         */
        getAxisZ(): Axis;

        /**
         * Returns true if the specified machine axis position is supported. That is, within any given ranges for the X, Y, and Z axes.
         * @param position The machine axes position. 
         */
        isSupportedPosition(
            position: Vector
        ): Boolean;

        /**
         * Returns a validity status code for the machine configuration. Use {@link getStatusDescription()} to convert the status code to an error message.
         * The following must be true for a configuration to be valid:
         */
        getValidityStatus(): Integer;

        /**
         * Returns true if the configuration is valid. See {@link getValidityStatus()} for additional information. 
         */
        isSupported(): Boolean;

        /**
         * Sets the multi-axis settings for feed rates
         * @param feedMode Can be FEED_INVERSE_TIME, FEED_DEGREES_MINUTE or FEED_PER_MINUTE. 
         * @param maximumFeedrate The maximum feed rate value. 
         * @param feedType Option used for selecting inverse time and DPM options. If FEED_INVERSE_TIME is selected this sets the time units and can be set to INVERSE_SECONDS or INVERSE_MINUTES. If FEED_DEGREES_MINUTE is selected this sets whether to use standard DPM or a DPM/FPM combination and can be set to DPM_STANDARD or DPM_COMBINATION. 
         * @param outputTolerance The tolerance for deciding whether to output feed rate value or not, only used for FEED_DEGREES_MINUTE. 
         * @param bpwRatio The pulse weight ratio, only used for FEED_DEGREES_MINUTE.
         * @since 45610 
         */
        setMultiAxisFeedrate(
            feedMode: Integer,
        ): void;

        /**
         * Returns the multi-axis feed mode. Returns FEED_INVERSE_TIME, FEED_DEGREES_MINUTE or FEED_PER_MINUTE. See {@link setMultiAxisFeedrate()} for description.
         * @since 45760 
         */
        getMultiAxisFeedrateMode(): Integer;

        /**
         * Returns the multi-axis maximum feed rate. See {@link setMultiAxisFeedrate()} for description.
         * @since 45760 
         */
        getMultiAxisFeedrateMaximum(): Number;

        /**
         * Returns the multi-axis feedrate DPM sub-type. Returns either DPM_STANDARD or DPM_COMBINATION. Raises an exception of the feedrate mode is not FEED_DEGREES_MINUTE. See {@link setMultiAxisFeedrate()} for description.
         * @since 45760 
         */
        getMultiAxisFeedrateDPMType(): Integer;

        /**
         * Returns the multi-axis feedrate time units. Returns either NVERSE_SECONDS or INVERSE_MINUTES. Raises an exception of the feedrate mode is not FEED_INVERSE_TIME. See {@link setMultiAxisFeedrate()} for description.
         * @since 45760 
         */
        getMultiAxisFeedrateInverseTimeUnits(): Integer;

        /**
         * Returns the multi-axis feed rate output tolerance. Raises an exception of the feed mode is not FEED_DEGREES_MINUTE. See {@link setMultiAxisFeedrate()} for description.
         * @since 45760 
         */
        getMultiAxisFeedrateOutputTolerance(): Number;

        /**
         * Returns the multi-axis feed rate bpw ratio. Raises an exception of the feed mode is not FEED_DEGREES_MINUTE. See {@link setMultiAxisFeedrate()} for description.
         * @since 45760 
         */
        getMultiAxisFeedrateBpwRatio(): Number;

        /**
         * Sets the multi-axis settings specific for handling singularities in the toolpath.
         * @param adjust Set to 'true' to enable singularity optimization within the post processor. Singularity optimization includes the ability to adjust the tool axis to minimize singularity issues (large rotary axis movement when the tool axis approaches perpendicularity to a rotary axis) and the linearization of the moves around the singularity to keep the tool end point within tolerance. 
         * @param method When set to SINGULARITY_LINEARIZE_OFF it disables the linearization of the moves to keep the tool end point within tolerance of the programmed tool path around the singularity. SINGULARITY_LINEARIZE_ROTARY will linearize the moves around the singularity. Additional points are added to keep the tool within the specified tolerance and is optimized for revolved movement as if the tool were moving around a cylinder or other revolved feature. SINGULARITY_LINEARIZE_LINEAR will also add additional points to keep the tool within tolerance, but will keep the tool end point moving in a straight line. 
         * @param cone Specifies the angular distance that the tool axis vector must be within in reference to the singularity point before the singularity logic is activated. This is usually a small value (less than 5 degrees), since the further away the tool axis is from the singularity, the less noticeable the fluctuations in the rotary axes will be and the less benefit this feature will provide. This parameter is in radians. 
         * @param angle The minimum angular delta movement that the rotary axes have to move prior to considering adjusting the tool axis vector for singularity optimization. This limit is used to keep from adjusting the tool axis vector when the rotary axes do not fluctuate greatly. This is typically set to a vaule of 10 degrees or more. This parameter is in radians. 
         * @param tolerance The tolerance value to used to keep the tool within tolerance when the tool axis is adjusted to minimize rotary axis movement around the singularity. 
         * @param linearizationTolerance The tolerance value to use when addtional points are added to keep the tool end point within tolerance of the programmed move when the tool axis is near the singularity.
         * @since 41535 
         */
        setSingularity(
            adjust: Boolean,
        ): void;

        /**
         * Returns true if multi-axis singularity adjustment is enabled. See {@link setSingularity()} for description.
         * @since 41535 
         */
        getSingularityAdjust(): Boolean;

        /**
         * Returns the multi-axis singularity method. See {@link setSingularity()} for description.
         * @since 41535 
         */
        getSingularityMethod(): Integer;

        /**
         * Returns the multi-axis singularity cone. See {@link setSingularity()} for description. This parameter is in radians.
         * @since 41535 
         */
        getSingularityCone(): Number;

        /**
         * Returns the multi-axis singularity angle. See {@link setSingularity()} for description. This parameter is in radians.
         * @since 41535 
         */
        getSingularityAngle(): Number;

        /**
         * Returns the multi-axis singularity tolerance. See {@link setSingularity()} for description.
         * @since 41535 
         */
        getSingularityTolerance(): Number;

        /**
         * Returns the multi-axis singularity linearization tolerance. See setMultiAxisSingularity() for description.
         * @since 41535 
         */
        getSingularityLinearizationTolerance(): Number;

        /**
         * Returns true if the specified rotation A, B, and C rotates around the available machine axes only. That is, the rotation around a non-machine axis must be exactly 0.
         * @param abc The A, B, and C rotation. 
         */
        isMachineAxisRotation(
            abc: Vector
        ): Boolean;

        /**
         * Returns true if this is a 3D machine configuration (i.e. axis U, V, and W are undefined). 
         */
        is3DConfiguration(): Boolean;

        /**
         * Returns true if the configuration is a multi-axis configuration (i.e. if any of the U, V, or W axes are defined). 
         */
        isMultiAxisConfiguration(): Boolean;

        /**
         * Returns the total number of axes. 
         */
        getNumberOfAxes(): Integer;

        /**
         * Returns true if the configuration is a head configuration (i.e. if any axis excluding X, Y, and Z is a head axis). 
         */
        isHeadConfiguration(): Boolean;

        /**
         * Returns true if the configuration is a table configuration (i.e. if all axes excluding X, Y, and Z are in the table). 
         */
        isTableConfiguration(): Boolean;

        /**
         * Returns the U axis. 
         */
        getAxisU(): Axis;

        /**
         * Returns the V axis. 
         */
        getAxisV(): Axis;

        /**
         * Returns the W axis. 
         */
        getAxisW(): Axis;

        /**
         * Returns the order of the given axis. Returns -1 if not found. 
         */
        getIndexOfAxisById(): Integer;

        /**
         * Returns true if the specified coordinate is mapped to a machine rotary axis.
         * By convention coordinate 0, 1, and 2 maps to A, B, and C, respectively. 
         */
        isMachineCoordinate(
            coordinate: Integer
        ): Boolean;

        /**
         * Returns the rotary axis bound to the specified coordinate. 
         */
        getAxisByCoordinate(
            coordinate: Integer
        ): Axis;

        /**
         * Returns the clamped ABC angles. Disabled coordinates are set to 0. 
         */
        clamp(
            _abc: Vector
        ): Vector;

        /**
         * Returns the clamped ABC angles. Disabled coordinates are set to 0. 
         */
        clampToResolution(
            _abc: Vector
        ): Vector;

        /**
         * Return rotation axis adjusted for carrier rotations. (hidden) 
         */
        getRotationAxis(
            axis: Axis
        ): Vector;

        /**
         * Keep secondary angle when tool axis is perpto rotary axis vector. (hidden) 
         */
        preserveRotaryAtZero(
            currentDirection: Vector,
        ): Vector;

        /**
         * Returns the current angles closest to the new machine angles. (hidden) 
         */
        getClosestABC(
            current: Vector,
        ): Vector;

        /**
         * Returns true if the specified XYZ position is supported. Disabled coordinates are ignored. 
         */
        isXYZSupported(
            _xyz: Vector
        ): Boolean;

        /**
         * Returns true if the specified ABC angles are supported. Disabled coordinates are ignored. 
         */
        isABCSupported(
            _abc: Vector
        ): Boolean;

        /**
         * Returns true if the specified direction is supported by the machine configuration.
         * @param direction The tool axis direction. 
         */
        isDirectionSupported(
            direction: Vector
        ): Boolean;

        /**
         * Adjusts the tool position for the rotary table and head rotations based on the defined kinematics of the machine. You can obtain the adjusted coordinates of the tool position based on the optimization of the specified section instead by using the following command.
         * section.getOptimzedPosition(...)
         * @param currentXYZ Current tool end point or XYZ position based on conversion. 
         * @param currentABC Current rotary axes positions. 
         * @param tcpType Can be one of the following.
TCP_XYZ - Converts the tool end point position to the XYZ machine position. currentXYZ should be the tool end point position.
TCP_TOOL - Converts the XYZ machine position to tool end point position. currentXYZ should be the XYZ position.
TCP_XYZ_OPTIMIZED - Converts the tool end point position to the XYZ machine position. currentXYZ should be based on the input position.
TCP_TOOL_OPTIMIZED - Converts the XYZ machine position to tool end point position. currentXYZ should be the input position. 


         * @param optimizeType Can be one of the following.
OPTIMIZE_BOTH - Converts the position based on both the table and head rotations.
OPTIMIZE_TABLES - Converts the position based on table rotations.
OPTIMIZE_HEADS - Converts the position based on the head rotations. 


         * @param forceAdjustment Set to true if calculation should be done even if axes are not optimized for rotary angles.
         * @since 45270 
         */
        getOptimizedPosition(
            currentXYZ: Vector,
        ): Vector;

        /**
         * Adjusts a vector for the rotary table rotations.
         * @param direction Vector to be adjusted. 
         * @param currentABC Current rotary axes positions. 
         * @param reverse Disabled converts the vector from the part system to the machine system. Enabled to convert the vector from machine system to part system. 
         * @param forceAdjustment Set to true if calculation should be done even if axes are not optimized for rotary angles.
         * @since 45270 
         */
        getOptimizedDirection(
            direction: Vector,
        ): Vector;

        /**
         * Adjust the tool position or tool vector for the rotary table rotations. Uses the same arguments as 'getOptimizedPosition' with the addition of 'isVector', when enabled states that 'currentXYZ' is a vector and not an XYZ coordinate. 
         */
        getOptimizedTables(
            currentXYZ: Vector,
        ): Vector;

        /**
         * Adjust the tool position for the rotary head rotations. 
         */
        getOptimizedHeads(
            currentXYZ: Vector,
        ): Vector;

        /**
         * Returns the ABC angles for the specified orientation such that the forward direction is maintained. An extra Z rotation in the local frame is normally required to achieve the desired orientation.
         * @param orientation The desired orientation. 
         */
        getABC(
            orientation: Matrix
        ): Vector;

        /**
         * Returns the ABC angles for both solutions for the specified direction. Both solutions will be identical when only one solution exists. The first solution is the one with the least tilt.
         * @param direction The desired tool direction. 
         */
        getABCByDirectionBoth(
            direction: Vector
        ): VectorPair;

        /**
         * Returns the ABC angles for the specified direction. The solution with the least tilt is returned.
         * @param direction The desired tool direction. 
         */
        getABCByDirection(
            direction: Vector
        ): Vector;

        /**
         * Returns the secondary ABC angles for the specified direction. Returns the primary solution if the secondary solution does not exist.
         * @param direction The desired tool direction. 
         */
        getABCByDirection2(
            direction: Vector
        ): Vector;

        /**
         * Returns the preferred ABC angles for the specified orientation such that the forward and optionally right directions are maintained. The preferences are defined in the 'controllingAxis', 'control', and 'options' parameters. Behaves the same as section.getABCByDirection, but does not support the ENABLE_LIMITS flag.
         * @param orientation The desired orientation. 
         * @param current The current machine angles. 
         * @param controllingAxis The axis used to determine the preferred solution. It can be A, B, C for a single axis or ABC to consider all axes. 
         * @param type The preference type.
PREFER_PREFERENCE - the preference specified in the createAxis definition.
PREFER_CLOSEST- the closest solution.
PREFER_POSITIVE - a positive angle. Not valid with ABC.
PREFER_NEGATIVE - a negative angle. Not valid with ABC.
PREFER_CW - axis moves in a clockwise direction. Not valid with ABC.
PREFER_CCW - axis moves in a counter-clockwise direction. Not valid with ABC. 


         * @param options - Options used to control the ABC selection.
ENABLE_NONE - disables all options.
ENABLE_RESET - respects the 'reset' parameter in the createAxis definition.
ENABLE_WCS - solves for a rotary axis perpendicular to the spindle vector as defined by the tool orientation of the operation.
ENABLE_ALL - enables all options (ENABLE_RESET and ENABLE_WCS).


         * @since 45821 
         */
        getABCByPreference(
            orientation: Matrix,
        ): Vector;

        /**
         * Returns the other ABC angles for the specified ABC angles. You should normally use {@link getABCByDirection2()} to get the secondary solution directly.
         * For up to 4 axis machines the return value is always the input ABC angles.
         * The result is undefined if given invalid ABC angles.
         * @param abc The abc angles. 
         */
        getOtherABCByDirection(
            abc: Vector
        ): Vector;

        /**
         * Returns the preferred ABC angles for the specified ABC angles.
         * @param abc The abc angles. 
         */
        getPreferredABC(
            abc: Vector
        ): Vector;

        /**
         * Maps the specified angles to the preferred ranges. An exception is raised if a requested rotation is not supported for an axis.
         * @param abc The machine angles. 
         */
        remapABC(
            abc: Vector
        ): Vector;

        /**
         * Maps the specified angles to the current machine angles. An exception is raised if a requested rotation is not supported for an axis. It is assumed that the desired angle difference is less than 180deg always.
         * @param abc The machine angles. 
         * @param current The current machine angles. 
         */
        remapToABC(
            abc: Vector,
        ): Vector;

        /**
         * Returns the bits indicating the machine angle coordinates in use.
         * Bit 0: coordinate 0 is used. Bit 1: coordinate 1 is used. Bit 2: coordinate 2 is used. 
         */
        getCoordinates(): Integer;

        /**
         * Returns the position for the specified absolute position and direction. Use {@link getOptimizedPosition()} instead to get the rotary axis adjusted position.
         * @param p The absolute position. 
         * @param abc The machine angles.
         * @deprecated 
         */
        getPosition(
            p: Vector,
        ): Vector;

        /**
         * Returns the direction for the specified ABC angles.
         * @param abc The machine angles. 
         */
        getDirection(
            abc: Vector
        ): Vector;

        /**
         * Returns the head ABC angles for the specified ABC angles. Ie. non-head axes are set to zero.
         * @param abc The machine angles. 
         */
        getHeadABC(
            abc: Vector
        ): Vector;

        /**
         * Returns the table ABC angles for the specified ABC angles. Ie. non-table axes are set to zero.
         * @param abc The machine angles. 
         */
        getTableABC(
            abc: Vector
        ): Vector;

        /**
         * Returns the head orientation for the specified ABC angles.
         * @param abc The machine angles. 
         */
        getHeadOrientation(
            abc: Vector
        ): Matrix;

        /**
         * Returns the table orientation for the specified ABC angles.
         * @param abc The machine angles. 
         */
        getTableOrientation(
            abc: Vector
        ): Matrix;

        /**
         * Returns the matrix for the specified ABC angles. Returns an identity matrix along the spindle vector.
         * @param abc The machine angles. 
         */
        getOrientation(
            abc: Vector
        ): Matrix;

        /**
         * Returns the matrix for the specified ABC angles.
         * @param abc The machine angles. 
         */
        getOrientationABC(
            abc: Vector
        ): Matrix;

        /**
         * Returns the spindle axis for the given ABC machine angles. 
         */
        getSpindleAxisABC(
            abc: Vector
        ): Vector;

        /**
         * Returns the additional rotation required to reach the specified work orientation. For valid machine rotations this will always be a rotation around the Z.
         * @param abc The machine angles. 
         * @param desired The desired work orientation. 
         */
        getRemainingOrientation(
            abc: Vector,
        ): Matrix;

        /**
         * Returns the additional rotation required to reach the specified work orientation. For valid machine rotations this will always be a rotation around the Z. Use this function when the spindle vector can change due to the rotary axis position.
         * @param abc The machine angles. 
         * @param desired The desired work orientation. 
         */
        getRemainingOrientationABC(
            abc: Vector,
        ): Matrix;

        /**
         * Returns the machine retraction plane coordinate. 
         */
        getRetractPlane(): Number;

        /**
         * Sets the machine retraction plane coordinate. 
         */
        setRetractPlane(
            retractPlane: Number
        ): void;

        /**
         * Returns true if the X-coordinate of the home position is valid. 
         */
        hasHomePositionX(): Boolean;

        /**
         * Returns the X-coordinate home position. 
         */
        getHomePositionX(): Number;

        /**
         * Sets the X-coordinate home position. 
         */
        setHomePositionX(
            x: Number
        ): void;

        /**
         * Returns true if the Y-coordinate of the home position is valid. 
         */
        hasHomePositionY(): Boolean;

        /**
         * Returns the Y-coordinate home position. 
         */
        getHomePositionY(): Number;

        /**
         * Sets the Y-coordinate home position. 
         */
        setHomePositionY(
            y: Number
        ): void;

        /**
         * Returns true if the Z-coordinate of the home position is valid. 
         */
        hasHomePositionZ(): Boolean;

        /**
         * Returns the Z-coordinate home position. 
         */
        getHomePositionZ(): Number;

        /**
         * Sets the Z-coordinate home position. 
         */
        setHomePositionZ(
            z: Number
        ): void;

        /**
         * Returns true if the X-coordinate of the tool change position is valid.
         * @since r50333 
         */
        hasToolChangePositionX(): Boolean;

        /**
         * Returns the X-coordinate tool change position.
         * @since r50333 
         */
        getToolChangePositionX(): Number;

        /**
         * Sets the X-coordinate tool change position.
         * @since r50333 
         */
        setToolChangePositionX(
            x: Number
        ): void;

        /**
         * Returns true if the Y-coordinate of the tool change position is valid.
         * @since r50333 
         */
        hasToolChangePositionY(): Boolean;

        /**
         * Returns the Y-coordinate tool change position.
         * @since r50333 
         */
        getToolChangePositionY(): Number;

        /**
         * Sets the Y-coordinate tool change position.
         * @since r50333 
         */
        setToolChangePositionY(
            y: Number
        ): void;

        /**
         * Returns true if the Z-coordinate of the tool change position is valid.
         * @since r50333 
         */
        hasToolChangePositionZ(): Boolean;

        /**
         * Returns the Z-coordinate tool change position.
         * @since r50333 
         */
        getToolChangePositionZ(): Number;

        /**
         * Sets the Z-coordinate tool change position.
         * @since r50333 
         */
        setToolChangePositionZ(
            z: Number
        ): void;

        /**
         * Returns true if the machine has a valid center position. 
         */
        hasCenterPosition(): Boolean;

        /**
         * Returns the X-coordinate center position. 
         */
        getCenterPositionX(): Number;

        /**
         * Sets the X-coordinate center position. (hidden) 
         */
        setCenterPositionX(
            x: Number
        ): void;

        /**
         * Returns the Y-coordinate center position. 
         */
        getCenterPositionY(): Number;

        /**
         * Sets the Y-coordinate center position. (hidden) 
         */
        setCenterPositionY(
            y: Number
        ): void;

        /**
         * Returns the Z-coordinate center position. 
         */
        getCenterPositionZ(): Number;

        /**
         * Sets the Z-coordinate center position. (hidden) 
         */
        setCenterPositionZ(
            z: Number
        ): void;

        /**
         * Returns true if the machine has a valid park position. 
         */
        hasParkPosition(): Boolean;

        /**
         * Returns the X-coordinate park position. 
         */
        getParkPositionX(): Number;

        /**
         * Sets the X-coordinate park position. (hidden) 
         */
        setParkPositionX(
            x: Number
        ): void;

        /**
         * Returns the Y-coordinate park position. 
         */
        getParkPositionY(): Number;

        /**
         * Sets the Y-coordinate park position. (hidden) 
         */
        setParkPositionY(
            y: Number
        ): void;

        /**
         * Returns the Z-coordinate park position. 
         */
        getParkPositionZ(): Number;

        /**
         * Sets the Z-coordinate park position. (hidden) 
         */
        setParkPositionZ(
            z: Number
        ): void;

        /**
         * Returns the number of extruders. 
         */
        getNumberExtruders(): Integer;

        /**
         * Sets the number of extruders. (hidden) 
         */
        setNumberExtruders(
            num: Integer
        ): void;

        /**
         * Returns the extruder offset X-coordinate.
         * @param extruderNo The extruder number. The first extruder is number 1. 
         */
        getExtruderOffsetX(
            extruderNo: Integer
        ): Number;

        /**
         * Sets the extruder offset X-coordinate. (hidden) 
         */
        setExtruderOffsetX(
            extruderNo: Integer,
        ): void;

        /**
         * Returns the extruder offset Y-coordinate.
         * @param extruderNo The extruder number. The first extruder is number 1. 
         */
        getExtruderOffsetY(
            extruderNo: Integer
        ): Number;

        /**
         * Sets the extruder offset Y-coordinate. (hidden) 
         */
        setExtruderOffsetY(
            extruderNo: Integer,
        ): void;

        /**
         * Returns the extruder offset Z-coordinate.
         * @param extruderNo The extruder number. The first extruder is number 1. 
         */
        getExtruderOffsetZ(
            extruderNo: Integer
        ): Number;

        /**
         * Sets the extruder offset Z-coordinate. (hidden) 
         */
        setExtruderOffsetZ(
            extruderNo: Integer,
        ): void;

        /**
         * Returns the model. 
         */
        getModel(): String;

        /**
         * Sets the model. 
         */
        setModel(
            model: String
        ): void;

        /**
         * Returns the description. 
         */
        getDescription(): String;

        /**
         * Sets the description. 
         */
        setDescription(
            description: String
        ): void;

        /**
         * Returns the vendor. 
         */
        getVendor(): String;

        /**
         * Sets the vendor. 
         */
        setVendor(
            vendor: String
        ): void;

        /**
         * Returns the vendor url. 
         */
        getVendorUrl(): String;

        /**
         * Sets the vendor url. 
         */
        setVendorUrl(
            vendorUrl: String
        ): void;

        /**
         * Returns the control. 
         */
        getControl(): String;

        /**
         * Sets the control. 
         */
        setControl(
            control: String
        ): void;

        /**
         * Returns true if the coolant is supported. 
         */
        isCoolantSupported(
            coolant: Integer
        ): Boolean;

        /**
         * Sets if the coolant is supported. Maximum limit is 256. COOLANT_OFF is not allowed. 
         */
        setCoolantSupported(
            coolant: Integer,
        ): void;

        /**
         * Returns the retract on indexing mode. 
         */
        getRetractOnIndexing(): Boolean;

        /**
         * Sets the retract on indexing mode. 
         */
        setRetractOnIndexing(
            retractOnIndexing: Boolean
        ): void;

        /**
         * Returns shortest angular rotation mode. 
         */
        getShortestAngularRotation(): Boolean;

        /**
         * Sets shortest angular rotation mode. 
         */
        setShortestAngularRotation(
            shortestAngularRotation: Boolean
        ): void;

        /**
         * Returns true if TCP is enabled for any rotary axis.
         * @since r50217 
         */
        isTCPEnabledForAnyAxis(): Boolean;

        /**
         * Returns the machine head attach point in machine system coordinates.
         * @since 45964 
         */
        getHeadAttachPoint(): Vector;

        /**
         * Returns the machine table attach point in machine system coordinates.
         * @since 45964 
         */
        getTableAttachPoint(): Vector;

        /**
         * Returns true if this {@link MachineConfiguration} was specified from the CAM system, or a machine file Returns false if the machine configuration was created by the post script.
         * @since 45765 
         */
        isReceived(): Boolean;

        /**
         * Returns true if tilted workplanes is enabled.
         * @since r50332 
         */
        isTWPEnabled(): Boolean;

        /**
         * Sets whether tilted workplanes should be used ie. G68.2, G254, PLANE SPATIAL, CYCLE800.
         * @since r50332 
         */
        setTWPEnabled(
            useTiltedWorkplane: Boolean
        ): void;

        /**
         * Returns true if tool gauge length should be added to the pivot distance for nonTCP rotary heads.
         * @since r50367 
         */
        getCompensateToolLength(): Boolean;

        /**
         * Sets whether tool gauge length should be added to the pivot distance for nonTCP rotary heads.
         * @since r50367 
         */
        setCompensateToolLength(
            compensateToolLength: Boolean
        ): void;

        /**
         * Returns the spatial text in the current unit.
         */
        static getAsSpatial(
            text: String
        ): Number;

        /**
         * Returns the spatial text in the current unit.
         */
        static getAsSpatialFeedrate(
            text: String
        ): Number;

        /**
         * Returns the angular feed in rad/min.
         */
        static getAsAngularFeedrate(
            text: String
        ): Number;

        /**
         * Returns the weight in kg.
         */
        static getAsWeight(
            text: String
        ): Number;

        /**
         * Returns the power in W (Watt).
         */
        static getAsPower(
            text: String
        ): Number;

        /**
         * Returns the angle in the radians.
         */
        static getAsAngular(
            text: String
        ): Number;

        /**
         * Returns the time in the seconds.
         */
        static getAsTime(
            text: String
        ): Number;

        /**
         * Loads the machine configuration from the specified XML. 
         */
        static createFromXML(
            xml: String
        ): MachineConfiguration;

        /**
         * Loads the machine configuration from the specified path. 
         */
        static createFromPath(
            path: String
        ): MachineConfiguration;

        /**
         * Returns the description for the specified status code. 
         */
        static getStatusDescription(
            status: Integer
        ): String;

    }

    /**
     * Machine parameters. 
     */
    declare class MachineParameters {
        spindleOrientation: Number;

        chipBreakingDistance: Number;

        drillingSafeDistance: Number;

        spindleSpeedDwell: Number;

    }

    /**
     * {@link Mail}.
     * Security: Requires security level 0.
     */
    declare class Mail {
        /**
         * Constructs a new mail interface. 
         */
        Mail(): Mail;

        /**
         * Adds a recipient.
         * @param email The mail address. 
         */
        addRecipient(
            email: String
        ): void;

        /**
         * Adds a recipient.
         * @param email The mail address. 
         * @param name The name of the recipient. Can be empty string. 
         */
        addRecipient2(
            email: String,
        ): void;

        /**
         * Adds a CC recipient.
         * @param email The mail address. 
         */
        addCCRecipient(
            email: String
        ): void;

        /**
         * Adds a CC recipient.
         * @param email The mail address. 
         * @param name The name of the recipient. Can be empty string. 
         */
        addCCRecipient2(
            email: String,
        ): void;

        /**
         * Adds a BCC recipient.
         * @param email The mail address. 
         */
        addBCCRecipient(
            email: String
        ): void;

        /**
         * Adds a BCC recipient.
         * @param email The mail address. 
         * @param name The name of the recipient. Can be empty string. 
         */
        addBCCRecipient2(
            email: String,
        ): void;

        /**
         * Returns the body. 
         */
        getBody(): String;

        /**
         * Sets the body. 
         */
        setBody(
            body: String
        ): void;

        /**
         * Returns the subject. 
         */
        getSubject(): String;

        /**
         * Sets the subject. 
         */
        setSubject(
            subject: String
        ): void;

        /**
         * Returns the sender. 
         */
        getSenderName(): String;

        /**
         * Sets the sender. 
         */
        setSenderName(
            senderName: String
        ): void;

        /**
         * Returns the sender mail address. 
         */
        getSenderEmail(): String;

        /**
         * Sets the sender mail address. 
         */
        setSenderEmail(
            senderEmail: String
        ): void;

        /**
         * Returns the reply to address. 
         */
        getReplyTo(): String;

        /**
         * Sets the reply to address. 
         */
        setReplyTo(
            replyTo: String
        ): void;

    }

    /**
     * A 3 by 3 matrix. 
     */
    declare class Matrix {
        /**
         * Construct a matrix as the identity matrix. 
         */
        Matrix(): Matrix;

        /**
         * Construct a matrix as a scaling matrix. 
         */
        Matrix(
            scale: Number
        ): Matrix;

        /**
         * Constructs the matrix with the specified right, up, and forward directions. 
         */
        Matrix(
            right: Vector,
        ): Matrix;

        /**
         * Constructs the matrix with the specified elements. 
         */
        Matrix(
            m: Number
        ): Matrix;

        /**
         * Constructs the matrix as the specified axis rotation.
         * @param axis The axis of rotation. 
         * @param angle The angle of rotation (radians).
         */
        Matrix(
            axis: Vector,
        ): Matrix;

        /**
         * Rotates this matrix around the X-axis by the specified angle.
         * @param angle The rotation angle in radians. 
         */
        rotateX(
            angle: Number
        ): void;

        /**
         * Rotates this matrix around the Y-axis by the specified angle.
         * @param angle The rotation angle in radians. 
         */
        rotateY(
            angle: Number
        ): void;

        /**
         * Rotates this matrix around the Z-axis by the specified angle.
         * @param angle The rotation angle in radians. 
         */
        rotateZ(
            angle: Number
        ): void;

        /**
         * Returns the value of the specified element.
         * @param row The row [0; 2]. 
         * @param column The column [0; 2]. 
         */
        getElement(
            row: Integer,
        ): Number;

        /**
         * Sets the value of the specified element.
         * @param row The row [0; 2]. 
         * @param column The column [0; 2]. 
         * @param value The desired value of the element. 
         */
        setElement(
            row: Integer,
        ): void;

        /**
         * Returns the specified row.
         * @param row The row [0; 2]. 
         */
        getRow(
            row: Integer
        ): Vector;

        /**
         * Sets the specified row.
         * @param row The row [0; 2]. 
         * @param value The desired row vector. 
         */
        setRow(
            row: Integer,
        ): void;

        /**
         * Returns the specified column.
         * @param column The column [0; 2]. 
         */
        getColumn(
            column: Integer
        ): Vector;

        /**
         * Sets the specified column.
         * @param column The column [0; 2]. 
         * @param value The desired column vector. 
         */
        setColumn(
            column: Integer,
        ): void;

        /**
         * Returns the forward vector. 
         */
        getForward(): Vector;

        /**
         * Sets the forward vector.
         * @param value The forward vector. 
         */
        setForward(
            value: Vector
        ): void;

        /**
         * Returns the up vector. 
         */
        getUp(): Vector;

        /**
         * Sets the up vector.
         * @param value The up vector. 
         */
        setUp(
            value: Vector
        ): void;

        /**
         * Returns the right vector. 
         */
        getRight(): Vector;

        /**
         * Sets the right vector.
         * @param value The right vector. 
         */
        setRight(
            value: Vector
        ): void;

        /**
         * Returns the X and Y rotations around the fixed frame to match the forward direction. Primary and secondary specifies the preferred rotations and must be different.
         * @param primary The primary coordinate (0, or 1). 
         * @param secondary The secondary coordinate (0 or 1). 
         */
        getTiltAndTilt(
            primary: Integer,
        ): Vector;

        /**
         * Returns the X, Y, Z rotations around the fixed frame to match the forward direction. Primary and secondary specifies the preferred rotations and must be different.
         * @param primary The primary coordinate (0, 1, or 2). 
         * @param secondary The secondary coordinate (0, 1, or 2). 
         */
        getTurnAndTilt(
            primary: Integer,
        ): Vector;

        /**
         * Returns the Euler angle for the specified convention in radians. Use {@link getEuler2()} instead to get the standard Euler conventions.
         * @deprecated 
         */
        getEuler(
            convention: Integer
        ): Vector;

        /**
         * Returns the Euler angle for the specified convention in radians.
         * @since r41257 
         */
        getEuler2(
            convention: Integer
        ): Vector;

        /**
         * Returns the Euler angles (ZXZ convention) in radians. Use {@link Matrix.getEuler2()} instead.
         * @deprecated 
         */
        getEulerZXZ(): Vector;

        /**
         * Returns the Euler angles (ZYZ convention) in radians. Use {@link Matrix.getEuler2()} instead.
         * @deprecated 
         */
        getEulerZYZ(): Vector;

        /**
         * Returns the Euler angles (XYZ convention) in radians. Use {@link Matrix.getEuler2()} instead.
         * @deprecated 
         */
        getEulerXYZ(): Vector;

        /**
         * Returns the Euler angles (ZYX convention) in radians. Use {@link Matrix.getEuler2()} instead.
         * @deprecated 
         */
        getEulerZYX(): Vector;

        /**
         * Clamps the matrix elements to -1, 0, and -1 with the specified epsilon value. 
         */
        clamp(
            epsilon: Number
        ): void;

        /**
         * Returns true if the matrix is the zero matrix (i.e. all elements are set to 0). 
         */
        isZero(): Boolean;

        /**
         * Returns true if the matrix is the identity matrix. 
         */
        isIdentity(): Boolean;

        /**
         * Return N1 of this matrix. 
         */
        getN1(): Number;

        /**
         * Return N2 of this matrix. 
         */
        getN2(): Number;

        /**
         * Normalizes the orientation. 
         */
        normalize(): void;

        /**
         * Adds the specified matrix to this matrix. 
         */
        add(
            right: Matrix
        ): void;

        /**
         * Subtracts the specified matrix from this matrix. 
         */
        subtract(
            right: Matrix
        ): void;

        /**
         * Negates this matrix. 
         */
        negate(): void;

        /**
         * Returns the negated matrix. 
         */
        getNegated(): Matrix;

        /**
         * Transposed this matrix. 
         */
        transpose(): void;

        /**
         * Returns the transposed matrix. 
         */
        getTransposed(): Matrix;

        /**
         * Returns the multiple of this matrix (left) and the specified value (right). 
         */
        multiply(
            right: Number
        ): Matrix;

        /**
         * Returns the multiple of this matrix (left) and the specified matrix (right). 
         */
        multiply(
            right: Matrix
        ): Matrix;

        /**
         * Returns the multiple of this matrix (left) and the specified vector (right). 
         */
        multiply(
            right: Vector
        ): Vector;

        /**
         * Converts the matrix to a string (e.g. [[1, 2, 3], [4, 5, 6], [7, 8, 9]]). 
         */
        toString(): String;

        /**
         * Returns the rotation matrix for the specified axis rotation.
         * @param axis The axis of rotation. 
         * @param angle The angle (in radians).
         */
        static getAxisRotation(
            axis: Vector,
        ): Matrix;

        /**
         * Returns the matrix for the specified X-axis rotation. The matrix will rotate the vector and not the frame.
         * @param angle The rotation angle in radians. 
         */
        static getXRotation(
            angle: Number
        ): Matrix;

        /**
         * Returns the matrix for the specified Y-axis rotation. The matrix will rotate the vector and not the frame.
         * @param angle The rotation angle in radians. 
         */
        static getYRotation(
            angle: Number
        ): Matrix;

        /**
         * Returns the matrix for the specified Z-axis rotation. The matrix will rotate the vector and not the frame.
         * @param angle The rotation angle in radians. 
         */
        static getZRotation(
            angle: Number
        ): Matrix;

        /**
         * Returns the matrix for the specified X, Y, and Z rotations. The matrix will rotate the vector and not the frame. The order of the rotations are X, Y, and then Z.
         * @param abc The rotation XYZ angles in radians. 
         */
        static getXYZRotation(
            abc: Vector
        ): Matrix;

        /**
         * Returns the rotation for the specified Euler angles and convention. 
         */
        static getEulerRotation(
            convention: Integer,
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (XZX static convention). Angles are in radians. 
         */
        static getEulerXZXRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (YXY static convention). Angles are in radians. 
         */
        static getEulerYXYRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (ZYZ static convention). Angles are in radians. 
         */
        static getEulerZYZRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (XZY static convention). Angles are in radians. 
         */
        static getEulerXZYRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (YXZ static convention). Angles are in radians. 
         */
        static getEulerYXZRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (ZYX static convention). Angles are in radians. 
         */
        static getEulerZYXRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (XYX static convention). Angles are in radians. 
         */
        static getEulerXYXRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (YZY static convention). Angles are in radians. 
         */
        static getEulerYZYRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (ZXZ static convention). Angles are in radians. 
         */
        static getEulerZXZRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (XYZ static convention). Angles are in radians. 
         */
        static getEulerXYZRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (YZX static convention). Angles are in radians. 
         */
        static getEulerYZXRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the rotation matrix for the specified Euler rotation (ZXY static convention). Angles are in radians. 
         */
        static getEulerZXYRotation(
            angles: Vector
        ): Matrix;

        /**
         * Returns the difference between the specified matrices. 
         */
        static sum(
            left: Matrix,
        ): Matrix;

        /**
         * Returns the difference between the specified matrices. 
         */
        static diff(
            left: Matrix,
        ): Matrix;

        /**
         * Returns a matrix that satisfies the supplied forward vector. Since only a single vector is supplied the resultant matrix can be rotated at any angle around the forward vector. 
         */
        static getOrientationFromDirection(
            forward: Vector
        ): Matrix;

        /* The 1-norm. */
        n1: Number;

        /* The 2-norm. */
        n2: Number;

        /* The negated matrix. */
        negated: Matrix;

        /* The transposed matrix (equal to the inverse for rotations). */
        transposed: Matrix;

        /* The up vector. */
        up: Vector;

        /* The right vector. */
        right: Vector;

        /* The forward vector. */
        forward: Vector;

        /* Euler angles in radians for XZX static/world convention. */
        eulerXZX: Vector;

        /* Euler angles in radians for YXY static/world convention. */
        eulerYXY: Vector;

        /* Euler angles in radians for ZYZ static/world convention. */
        eulerZYZ: Vector;

        /* Euler angles in radians for XZX body convention. */
        eulerXZX_R: Vector;

        /* Euler angles in radians for YXY body convention. */
        eulerYXY_R: Vector;

        /* Euler angles in radians for ZYZ body convention. */
        eulerZYZ_R: Vector;

        /* Euler angles in radians for XZY static/world convention. */
        eulerXZY: Vector;

        /* Euler angles in radians for YXZ static/world convention. */
        eulerYXZ: Vector;

        /* Euler angles in radians for ZYX static/world convention. */
        eulerZYX: Vector;

        /* Euler angles in radians for YZX body convention. */
        eulerYZX_R: Vector;

        /* Euler angles in radians for ZXY body convention. */
        eulerZXY_R: Vector;

        /* Euler angles in radians for XYZ body convention. */
        eulerXYZ_R: Vector;

        /* Euler angles in radians for XYX static/world convention. */
        eulerXYX: Vector;

        /* Euler angles in radians for YZY static/world convention. */
        eulerYZY: Vector;

        /* Euler angles in radians for ZXZ static/world convention. */
        eulerZXZ: Vector;

        /* Euler angles in radians for XYX body convention. */
        eulerXYX_R: Vector;

        /* Euler angles in radians for YZY body convention. */
        eulerYZY_R: Vector;

        /* Euler angles in radians for ZXZ body convention. */
        eulerZXZ_R: Vector;

        /* Euler angles in radians for XYZ static/world convention. */
        eulerXYZ: Vector;

        /* Euler angles in radians for YZX static/world convention. */
        eulerYZX: Vector;

        /* Euler angles in radians for ZXY static/world convention. */
        eulerZXY: Vector;

        /* Euler angles in radians for ZYX body convention. */
        eulerZYX_R: Vector;

        /* Euler angles in radians for XZY body convention. */
        eulerXZY_R: Vector;

        /* Euler angles in radians for YXZ body convention. */
        eulerYXZ_R: Vector;

    }

    /**
     * A mesh.
     * @since 42020 
     */
    declare class Mesh {
        /**
         * Constructs an empty mesh. 
         */
        Mesh(): Mesh;

        /**
         * Reserves memory for the given number of facets. 
         */
        reserve(
            numberOfFacets: Integer
        ): void;

        /**
         * Returns the number of vertices. 
         */
        getNumberOfVertices(): Integer;

        /**
         * Returns the given vertex. 
         */
        getVertex(
            index: Integer
        ): Vector;

        /**
         * Returns the number of facets in the mesh. 
         */
        getNumberOfFacets(): Integer;

        /**
         * Returns the given facet vertex index for the returned node.
         * @param index The index of the facet. 
         * @param node The node of the facet (0, 1, 2). 
         */
        getFacet(
            index: Integer,
        ): Integer;

        /**
         * Clears the mesh. 
         */
        clear(): void;

        /**
         * Adds facet. It is good convention to keep all facets CCW-wound (use the right hand rule).
         * @param a Vertex. 
         * @param b Vertex. 
         * @param c Vertex. 
         */
        addFacet(
            a: Vector,
        ): void;

        /**
         * Scales mesh.
         * @param factor The factor used to scale all vertices. 
         */
        scale(
            factor: Number
        ): void;

        /**
         * Translates mesh.
         * @param translation The translation for all vertices. 
         */
        translate(
            translation: Vector
        ): void;

        /**
         * Transforms mesh. new vertex = ORIENTATION * vertex + TRANSLATION
         * @param translation The translation for all vertices. 
         * @param orientation The orientation for all vertices. 
         */
        transform(
            translation: Vector,
        ): void;

        /**
         * Returns the bounding box of the mesh. 
         */
        getBoundingBox(): BoundingBox;

        /**
         * Load mesh from file.
         * @param path The file path. 
         * @param format The file format. The format can be 'application/sla' or 'ascii-stl'. 
         */
        static loadMesh(
            path: String,
        ): Mesh;

        /**
         * Save mesh to file.
         * @param path The file path. 
         * @param format The file format. The format can be 'application/sla' or 'ascii-stl'. 
         * @param mesh The mesh. 
         */
        static saveMesh(
            path: String,
        ): void;

    }

    /**
     * A modal unformatted variable. State is maintained to avoid output for unchanged values.
     */
    declare class Modal {
        /**
         * Constructs a modal variable.
         * The supported specifiers are:
         * @param specifiers The format specifiers. 
         * @param format The desired format (optional).
         */
        Modal(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): Modal;

        /**
         * Returns the corresponding string.
         * If the value is identical to the last value then the empty string is returned unless the force specifier has been set to true.
         * @param value The value. This must be of type Number if a format has been specified. 
         */
        format(
            value: Value
        ): String;

        /**
         * Returns the prefix. 
         */
        getPrefix(): Value;

        /**
         * Sets the prefix. 
         */
        setPrefix(
            prefix: Value
        ): void;

        /**
         * Returns the suffix. 
         */
        getSuffix(): Value;

        /**
         * Sets the suffix. 
         */
        setSuffix(
            suffix: Value
        ): void;

        /**
         * Invoke to force output of value on the next invocation of {@link format()}. {@link reset()} has no effect if force has been set to true. 
         */
        reset(): void;

        /**
         * Returns the last formatted value. 
         */
        getCurrent(): Value;

    }

    /**
     * A modal group.
     * Use createModalGroup() to construct the object.
     */
    declare class ModalGroup {
        /**
         * Construct a new modal group. 
         */
        ModalGroup(): ModalGroup;

        /**
         * Sets the strict mode. 
         */
        setStrict(
            strict: Boolean
        ): void;

        /**
         * Sets the auto-reset mode. All groups are reset if an undefined code is output. Only appplies in non-strict mode. 
         */
        setAutoReset(
            autoreset: Boolean
        ): void;

        /**
         * Enables output of for use of undefined codes. Only on the first usage. 
         */
        setLogUndefined(
            logundefined: Boolean
        ): void;

        /**
         * Returns the number of groups. 
         */
        getNumberOfGroups(): Integer;

        /**
         * Returns the total number of defined codes. 
         */
        getNumberOfCodes(): Integer;

        /**
         * Returns the number of codes in the specified group. 
         */
        getNumberOfCodesInGroup(
            group: Integer
        ): Integer;

        /**
         * Returns true if the specified code is defined. 
         */
        isCodeDefined(
            code: Integer
        ): Boolean;

        /**
         * Returns true if the specified code is active within its group. 
         */
        isActiveCode(
            code: Integer
        ): Boolean;

        /**
         * Marks the specified code as active within its group. 
         */
        makeActiveCode(
            code: Integer
        ): void;

        /**
         * Returns the active code in the specified group. 
         */
        getActiveCode(
            group: Integer
        ): Integer;

        /**
         * Returns true if the specified group has an active code. 
         */
        hasActiveCode(
            group: Integer
        ): Boolean;

        /**
         * Resets all groups. 
         */
        reset(): void;

        /**
         * Resets the specified group.
         * @param group The group.
         */
        resetGroup(
            group: Integer
        ): Integer;

        /**
         * Creates a new group. 
         */
        createGroup(): Integer;

        /**
         * Removes the specified code.
         * @param code The code. 
         */
        removeCode(
            code: Integer
        ): void;

        /**
         * Adds the specified code to the given group.
         * @param group The group. 
         * @param code The code. 
         */
        addCode(
            group: Integer,
        ): void;

        /**
         * Returns true if the specified group is a valid group. 
         */
        isGroup(
            group: Integer
        ): Boolean;

        /**
         * Returns the group id for the specified code.
         */
        getGroup(
            code: Integer
        ): Integer;

        /**
         * Returns true if the specified codes are in the same group.
         */
        inSameGroup(
            a: Integer,
        ): Boolean;

        /**
         * Returns true if enabled. 
         */
        isEnabled(): Boolean;

        /**
         * Enables the output. 
         */
        enable(): void;

        /**
         * Disables the output. 
         */
        disable(): void;

        /**
         * Sets the force mode. 
         */
        setForce(
            force: Boolean
        ): void;

        /**
         * Sets the format. 
         */
        setFormatNumber(
            formatNumber: FormatNumber
        ): void;

        /**
         * Sets the prefix. 
         */
        setPrefix(
            prefix: String
        ): void;

        /**
         * Sets the suffix. 
         */
        setSuffix(
            suffix: String
        ): void;

        /**
         * Returns the formatted code.
         * Code must be defined in strict mode. If code is not defined in non-strict mode than the codes is output always. 
         */
        format(
            code: Integer
        ): String;

    }

    /**
     * A group of lengths associated with a multi-axis move. 
     */
    declare class MoveLength {
        /**
         * Gets the length of the movement of the linear axes. 
         */
        getLinearMoveLength(): Number;

        /**
         * Gets the length of the movement of the rotary axes. 
         */
        getRadialMoveLength(): Number;

        /**
         * Gets the length of the rotary component of the tool tip movement. 
         */
        getRadialToolTipMoveLength(): Number;

        /* The length of the movement of the linear axes. */
        linear: Number;

        /* The length of the movement of the rotary axes. */
        radial: Number;

        /* The length of the rotary component of the tool tip movement. */
        radialToolTip: Number;

    }

    /**
     * A formatting variable. Instances of this class are used to format incoming values. Supports absolute, incremental, and directional variables. State is maintained to avoid output for unchanged values.
     * @since r45884
     */
    declare class OutputVariable {
        /**
         * Constructs an output variable.
         * The supported specifiers are:
         * @param specifiers The specifiers. 
         * @param format The desired format.
         * @since r45877 
         */
        OutputVariable(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): OutputVariable;

        /**
         * Disable the variable from being output. When invoked {@link format()} always returns an empty string. 
         */
        disable(): void;

        /**
         * Enables the variable for output. 
         */
        enable(): void;

        /**
         * Returns the formatted string of the specified value. An empty string can be returned based on the control setting. 
         */
        format(
            value: Number
        ): String;

        /**
         * Returns the output control. 
         */
        getControl(): Value;

        /**
         * Returns the last formatted value. 
         */
        getCurrent(): Value;

        /**
         * Returns the cyclic limit. 
         */
        getCyclicLimit(): Value;

        /**
         * Returns the cyclic sign. 
         */
        getCyclicSign(): Value;

        /**
         * Returns the format attached to the variable. 
         */
        getFormat(): FormatNumber;

        /**
         * Returns the prefix. 
         */
        getPrefix(): Value;

        /**
         * Returns the resulting value as if it was formatted for the variable. 
         */
        getResultingValue(): Value;

        /**
         * Returns the suffix. 
         */
        getSuffix(): Value;

        /**
         * Returns the output tolerance for the variable. 
         */
        getTolerance(): Value;

        /**
         * Returns the type. 
         */
        getType(): Value;

        /**
         * Returns true if the variable is enabled. 
         */
        isEnabled(): Boolean;

        /**
         * Sets the variable output control. Can be CONTROL_CHANGED, CONTROL_FORCE, or CONTROL_NONZERO. 
         */
        setControl(
            control: Value
        ): void;

        /**
         * Sets the current value for the variable. 
         */
        setCurrent(
            current: Value
        ): void;

        /**
         * Sets the cyclic limit. 
         */
        setCyclicLimit(
            cyclicLimit: Value
        ): void;

        /**
         * Sets the cyclic sign. Can be -1, 0, 1. 
         */
        setCyclicSign(
            cyclicLimit: Value
        ): void;

        /**
         * Assigns a format to the variable. 
         */
        setFormat(): void;

        /**
         * Sets the prefix. 
         */
        setPrefix(
            prefix: Value
        ): void;

        /**
         * Sets the suffix. 
         */
        setSuffix(
            suffix: Value
        ): void;

        /**
         * Sets the output tolerance for the variable. 
         */
        setTolerance(
            tolerance: Value
        ): void;

        /**
         * Sets the variable type. Can be TYPE_ABSOLUTE, TYPE_INCREMENTAL, or TYPE_DIRECTIONAL. 
         */
        setType(
            type: Value
        ): void;

        /**
         * Invoke to force output of value on the next invocation of {@link format()}. {@link reset()} has no effect if control has been set to force for the given format. 
         */
        reset(): void;

        /**
         * Returns the decimal separator of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getDecimalSymbol(): String;

        /**
         * Sets the decimal separator of the {@link FormatNumber} associated with the {@link OutputVariable}.. 
         */
        setDecimalSymbol(
            decimalSymbol: String
        ): void;

        /**
         * Returns the force sign flag of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getForceSign(): Boolean;

        /**
         * Sets the force sign flag of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setForceSign(
            forceSign: Boolean
        ): void;

        /**
         * Returns the number of decimals of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getNumberOfDecimals(): Value;

        /**
         * Sets the number of decimals of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setNumberOfDecimals(
            numberOfDecimals: Value
        ): void;

        /**
         * Returns the scale of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getScale(): Value;

        /**
         * Sets the scale of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setScale(
            scale: Value
        ): void;

        /**
         * Returns the offset of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getOffset(): Value;

        /**
         * Sets the offset of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setOffset(
            offset: Value
        ): void;

        /**
         * Returns the base number of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getBase(): Value;

        /**
         * Sets the base number of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setBase(
            base: Value
        ): void;

        /**
         * Returns the minimum allowed value of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getMinimum(): Value;

        /**
         * Sets the minimum allowed value of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setMinimum(
            minimum: Value
        ): void;

        /**
         * Returns the maximum allowed value of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getMaximum(): Value;

        /**
         * Sets the maximum allowed value of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setMaximum(
            maximum: Value
        ): void;

        /**
         * Returns the minimum number of digits to the left of the decimal point of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getMinDigitsLeft(): Value;

        /**
         * Sets the minimum number of digits to the left of the decimal point of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setMinDigitsLeft(
            minDigitsLeft: Value
        ): void;

        /**
         * Returns the minimum number of digits to the right of the decimal point of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getMinDigitsRight(): Value;

        /**
         * Sets the minimum number of digits to the right of the decimal point of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setMinDigitsRight(
            minDigitsRight: Value
        ): void;

        /**
         * Returns the type of formatted value of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        getFormatType(): Value;

        /**
         * Sets the type of formatted value of the {@link FormatNumber} associated with the {@link OutputVariable}. 
         */
        setFormatType(
            type: Value
        ): void;

    }

    /**
     * Post processor object. This is the main class. The post processor automatically instantiates an instance of this class at startup and makes it the global object for the posting context. 
     */
// Extract as global declare
        /**
         * Constructs a post processor object. 
         */
        declare function PostProcessor(): any;

        /**
         * Returns the active machine configuration. 
         */
        declare function getMachineConfiguration(): MachineConfiguration;

        /**
         * Sets the active machine configuration.
         * @param machine The new machine configuration. 
         */
        declare function setMachineConfiguration(
            machine: MachineConfiguration
        ): void;

        /**
         * Returns an object containing length information about the current multiaxis move.
         * @param x The end linear x coordinate of the move 
         * @param y The end linear y coordinate of the move 
         * @param z The end linear z coordinate of the move 
         * @param a The end radial a coordinate of the move 
         * @param b The end radial b coordinate of the move 
         * @param c The end radial c coordinate of the move 
         */
        declare function getMultiAxisMoveLength(
            x: Number,
        ): MoveLength;

        /**
         * Optimizes the machine angles for 5-axis motion using the active machine configuration. The tool tip position is maintained (TCPM).
         * The directions for {@link onRapid5D()} and {@link onLinear5D()} are hereafter mapped to machine angles from the initial direction vector. The work plane and origin are mapped into the WCS plane and origin. 
         */
        declare function optimizeMachineAngles(): void;

        /**
         * Optimizes the machine angles for 5-axis motion using the active machine configuration.
         * The directions for {@link onRapid5D()} and {@link onLinear5D()} are hereafter mapped to machine angles from the initial direction vector. The work plane and origin are mapped into the WCS plane and origin. 
         * @param optimizeType Can be one of the following values.
OPTIMIZE_NONE: Maintain tool tip position (TCPM).
OPTIMIZE_BOTH: Map tool tip position.
OPTMIZE_TABLES: Map tool tip position for machine axes for tables only. 


         * @since r41179
OPTMIZE_HEADS: Map tool tip position for machine axes for heads only. 


         */
        declare function optimizeMachineAngles2(
            optimizeType: Integer
        ): void;

        /**
         * Optimizes the machine angles for 5-axis motion given the specified machine configuration.
         * The directions for {@link onRapid5D()} and {@link onLinear5D()} are hereafter mapped to machine angles from the initial direction vector. The work plane and origin are mapped into the WCS plane and origin.
         * @param machine The machine configuration. 
         * @param optimizeType Can be one of the following values.
OPTIMIZE_NONE: Maintain tool tip position (TCPM).
OPTIMIZE_BOTH: Map tool tip position.
OPTMIZE_TABLES: Map tool tip position for machine axes for tables only. 


         * @since r41179
OPTMIZE_HEADS: Map tool tip position for machine axes for heads only. 


         */
        declare function optimizeMachineAnglesByMachine(
            machine: MachineConfiguration,
        ): void;

        /**
         * Returns true if the given cycle is considered a special cycle for which to call onSectionSpecialCycle()/onSectionEndSpecialCycle() instead of onSection()/onSectionEnd().
         * @param uri The unique identifier of the cycle.
         * @since r41185 
         */
        declare function isSectionSpecialCycle(
            uri: String
        ): Boolean;

        /**
         * Tells the post processor to call onSectionSpecialCycle()/onSectionEndSpecialCycle() instead of onSection()/onSectionEnd() for sections starting with the given cycle.
         * @param uri The unique identifier of the cycle. 
         * @param specialCycle Indicates if the cycle should be considered special. Set to false to remove a built-in special cycles.
         * @since r41185 
         */
        declare function setSectionSpecialCycle(
            uri: String,
        ): void;

        /**
         * Returns the formal name of the product. 
         */
        declare function getProduct(): String;

        /**
         * Returns the unique id of the software component (i.e. '{@link http://www.autodesk.com/namespace/campostprocessor'}). 
         */
        declare function getProductUri(): String;

        /**
         * Returns the url for the product. 
         */
        declare function getProductUrl(): String;

        /**
         * Returns the vendor of the product. 
         */
        declare function getVendor(): String;

        /**
         * Returns the url for the vendor. 
         */
        declare function getVendorUrl(): String;

        /**
         * Returns the version of the software component. 
         */
        declare function getVersion(): String;

        /**
         * Opens the specified url. 
         */
        declare function openUrl(
            url: String
        ): void;

        /**
         * Prints the specified document. Note that you cannot open the primary output file until the onTerminate() is invoked. The document type must have the "print" command registered in the Windows Registry.
         * @param path The path of the document.
         */
        declare function printDocument(
            path: String
        ): Boolean;

        /**
         * Prints the specified document. Note that you cannot open the primary output file until the onTerminate() is invoked. The document type must have the "printto" command registered in the Windows Registry. If the "printto" command is not registered for a given document type you can sometimes print using the execute() function by referrencing the proper application and parsing appropriate parameters.
         * @param path The path of the document. 
         * @param printerName The printer name.
         */
        declare function printDocumentTo(
            path: String,
        ): Boolean;

        /**
         * Returns a new tool renderer. 
         */
        declare function createToolRenderer(): ToolRenderer;

        /**
         * Called to invoke onRapid in the post engine.
         * @param x The x value of the point the tool will move to. 
         * @param y The y value of the point the tool will move to. 
         * @param z The z value of the point the tool will move to. 
         * @since r45605 
         */
        declare function invokeOnRapid(
            x: Number,
        ): Boolean;

        /**
         * Called to invoke onRapid5D in the post engine.
         * @param x The x value of the point the tool will move to. 
         * @param y The y value of the point the tool will move to. 
         * @param z The z value of the point the tool will move to. 
         * @param dx the x value of the direction the tool will be orientated. 
         * @param dy the y value of the direction the tool will be orientated. 
         * @param dz the z value of the direction the tool will be orientated. 
         * @since r45605 
         */
        declare function invokeOnRapid5D(
            x: Number,
        ): Boolean;

        /**
         * Called to invoke onLinear in the post engine.
         * @param x The x value of the point the tool will move to. 
         * @param y The y value of the point the tool will move to. 
         * @param z The z value of the point the tool will move to. 
         * @param feedrate The feedrate of the tool. 
         * @since r45605 
         */
        declare function invokeOnLinear(
            x: Number,
        ): Boolean;

        /**
         * Called to invoke onLinear5D in the post engine.
         * @param x The x value of the point the tool will move to. 
         * @param y The y value of the point the tool will move to. 
         * @param z The z value of the point the tool will move to. 
         * @param dx The x value of the direction the tool will be orientated. 
         * @param dy The y value of the direction the tool will be orientated. 
         * @param dz The z value of the direction the tool will be orientated. 
         * @param feedrate The feedrate of the tool. 
         * @since r45605 
         */
        declare function invokeOnLinear5D(
            x: Number,
        ): Boolean;

        /**
         * Called to invoke onCircular in the post engine.
         * @param clockwise True if tool should move in a clockwise direction. 
         * @param cx The x value of the point the tool will arc around. 
         * @param cy The y value of the point the tool will arc around. 
         * @param cz The z value of the point the tool will arc around. 
         * @param x The x value of the point the tool will arc to. 
         * @param y The y value of the point the tool will arc to. 
         * @param z The z value of the point the tool will arc to. 
         * @param nx The x value of the direction perpendicular to the arc. 
         * @param ny The y value of the direction perpendicular to the arc. 
         * @param nz The z value of the direction perpendicular to the arc. 
         * @param feedrate The feedrate of the tool. 
         * @since r45605 
         */
        declare function invokeOnCircular(
            clockwise: Boolean,
        ): Boolean;

        /**
         * Called to invoke onSpindleSpeed in the post engine.
         * @param spindleSpeed The new spindle speed. 
         */
        declare function invokeOnSpindleSpeed(
            spindleSpeed: Number
        ): Boolean;

        /**
         * Call to activate and setup Polar Mode.
         * @param tolerance The tolerance used for linearization. The tolerance is specified in MM. 
         * @param currentAngle The current angle of the machine axis's that is perpendicular to the tool 
         * @param polarDirection A vector representing the line which the tool can move along to cut the part. (1, 0, 0) would be the x axis. A vector of (0, 0, 0) will use the initial position of the section to determine the vector line. 
         * @param interpolateRapidMoves Set to true to interpolate rapid moves as linear moves. 
         * @param optimizeType Determines whether to output polar coordinates in TCPM mode or not. OPTIMIZE_AXIS will use the axis defintion TCPM mode. OPTIMIZE_NONE will use TCP MODE. OPTIMIZE_BOTH or OPTIMIZE_TABLES will not use TCPM mode and is the default setting. 
         * @since r45636 
         */
        declare function activatePolarMode(
            tolerance: Number,
        ): VectorPair;

        /**
         * Call to deactivate Polar Mode. This should be called in {@link onSectionEnd()} if activePolarMode has been called in onSection(). 
         * @since r45636 
         */
        declare function deactivatePolarMode(): void;

        /**
         * Call to check if Polar Mode is active.
         * @since r45636 
         */
        declare function isPolarModeActive(): Boolean;

        /**
         * Call to get the polar transformed direction from the given position. This function does not update the current position and direction. Call {@link setCurrentPositionAndDirection()} after to update. 
         * @param x The x coordinate of point to be transformed 
         * @param y The y coordinate of point to be transformed 
         * @param z The z coordinate of point to be transformed 
         * @since r45636 
         */
        declare function getPolarPosition(
            x: Number,
        ): VectorPair;

        /**
         * Call to get the polar transformed direction from the given position. This function does not enable or disable polar mode. 
         * @param position The Vector with the coordinates of the point to be transformed. 
         * @param abc A vector representing the current machine axis angles where a, b, and c map to the vector's x, y, and z respectively. 
         * @param polarDirection A vector representing the line which the tool can move along to cut the part. (1, 0, 0) would be the x axis. A vector of (0, 0, 0) will use the initial position of the section to determine the vector line. 
         * @param defaultOptimizeType the optimization type to use if polar mode was not previously activated 
         * @since r50177 
         */
        declare function getPolarCoordinates(
            position: Vector,
        ): VectorPair;

        /**
         * Call to get the frame position from the given polar position. If polar mode is not enabled then the input position will be returned.
         * @since r50217 
         */
        declare function getFramePositionFromPolar(
            x: Number,
        ): Vector;

        /**
         * Activates automatic polar mode handling.
         * The supported options are:
         * @param options Defines the polar interpolation options.
         */
        declare function activateAutoPolarMode(): void;

        /**
         * Call to set the current position and direction at the same time. 
         * @param posDir A pair of vectors containing the position and direction wanting to be set. 
         * @since r45635 
         */
        declare function setCurrentPositionAndDirection(
            posDir: VectorPair
        ): void;

        /**
         * Sets the desired exit code. The default exit code is usually fine. The behavior is unspecified if the exit code is not a value in the range [1000; 1999]. 
         */
        declare function setExitCode(
            code: Integer
        ): void;

        /**
         * Writes an error message and aborts the script. 
         */
        declare function error(
            message: String
        ): void;

        /**
         * Writes a warning. 
         */
        declare function warning(
            message: String
        ): void;

        /**
         * Writes the specified warning if the given warning id has not been used before. 
         */
        declare function warningOnce(
            message: String,
        ): void;

        /**
         * Writes the specified message to the log. 
         */
        declare function log(
            message: String
        ): void;

        /**
         * Returns a description of the current NC location. 
         */
        declare function getCurrentNCLocation(): String;

        /**
         * Returns the system unit as defined in the regional settings. 
         */
        declare function getSystemUnit(): Integer;

        /**
         * Returns the platform identifier. E.g. "WIN32" for Windows and "OSX" for Mac OS X. 
         */
        declare function getPlatform(): String;

        /**
         * Returns true if the specified character exists in the current code page. 
         */
        declare function hasSymbol(
            symbol: Integer
        ): Boolean;

        /**
         * Returns true if all the characters exist in the given text for the current code page. 
         */
        declare function isTextSupported(
            text: String
        ): Boolean;

        /**
         * Returns the current output code page id. 
         */
        declare function getCodePage(): Integer;

        /**
         * Sets the output code page.
         * The code page can be changed at any time but using different code pages in the same text file does normally not make sense. For binary output it can be relevant in some cases. 
         */
        declare function setCodePage(
            name: String
        ): void;

        /**
         * Writes the specified text to the NC output file excluding an end-of-line marker. 
         */
        declare function write(
            message: String
        ): void;

        /**
         * Writes the specified text to the NC output file including an end-of-line marker. 
         */
        declare function writeln(
            message: String
        ): void;

        /**
         * Returns the separator for word-based the line output. 
         */
        declare function getWordSeparator(): String;

        /**
         * Sets the separator for word-based line output. Defaults to a space (i.e. " "). 
         */
        declare function setWordSeparator(
            message: String
        ): void;

        /**
         * Writes the specified words to the NC output file including an end-of-line marker using the word separator (see {@link getWordSeparator()} and {@link setWordSeparator()}). Empty strings or undefined values are ignored. No line is output for completely empty lines.
         */
        declare function writeWords(
            message: String,
        ): void;

        /**
         * This method is similar to {@link writeWords()} with the exception that text is only output if the 2 argument or above results in text.
         * This function is useful when outputting block numbers to avoid output.
         */
        declare function writeWords2(
            message: String,
        ): void;

        /**
         * Returns the string output by writeWords() without end-of-line. 
         */
        declare function formatWords(
            message: String
        ): String;

        /**
         * Returns a new string with the named substrings of the first argument substituted by the specified arguments. This method supports up to 16 arguments.
         */
        declare function subst(
            message: String,
        ): String;

        /**
         * Returns the language id. 
         */
        declare function getLangId(): String;

        /**
         * Returns true if the specified text can be output in the active code page/encoding without change. 
         */
        declare function isSupportedText(
            message: String
        ): Boolean;

        /**
         * Returns the localized text for the specified native text if the result is supported in the active code page/encoding. Otherwise the original text is returned. 
         */
        declare function localize(
            message: String
        ): String;

        /**
         * Returns the localized text for the specified native text of the given section if the result is supported in the active code page/ encoding. Otherwise the original text is returned. 
         */
        declare function localize2(
            section: String,
        ): String;

        /**
         * Loads the specified locale.
         * @param langId The language id of the locale file. 
         */
        declare function loadLocale(
            langId: String
        ): Boolean;

        /**
         * Includes the specified post processing script.
         * @param path the path of the script to be included. 
         */
        declare function include(
            path: String
        ): void;

        /**
         * Returns the full path for the specified relative path if found in the include paths.
         * @param path the relative path. 
         */
        declare function findFile(
            path: String
        ): String;

        /**
         * Returns the header of the post configuration where header is the first multi-line comment.
         * @since r41192 
         */
        declare function getHeader(): String;

        /**
         * Returns the version from the header of the post configuration. Where version is the first string within the 
         * . Returns empty string if not found.
         * @since r41192 
         */
        declare function getHeaderVersion(): String;

        /**
         * Returns the commit from the header of the post configuration. Where commit is the second string within the 
         * . Returns empty string if not found.
         * @since r41192 
         */
        declare function getHeaderCommit(): String;

        /**
         * Returns the modification date as string from the header of the post configuration. Where date is the string within the {@link Date}: ... $. Returns empty string if not found.
         * @since r41192 
         */
        declare function getHeaderDate(): String;

        /**
         * Returns the modification date as {@link Date} from the header of the post configuration. Where date is the string within the {@link Date}: ... $. Returns undefined if not found.
         * @since r41192 
         */
        declare function getHeaderDate2(): Date;

        /**
         * Returns the string following the given keyword from the header of the post configuration. Returns empty string if not found.
         * @param keyword The keyword to look for. The keyword must match this regular expression [a-zA-Z][a-zA-Z0-9_]*.
         * @since r41192 
         */
        declare function getHeaderSnippet(
            keyword: String
        ): String;

        /**
         * Returns the path on the intermediate file.
         * @since r37310 
         */
        declare function getIntermediatePath(): String;

        /**
         * Returns the path on the destination file. 
         */
        declare function getOutputPath(): String;

        /**
         * Returns the path of the simulation stream.
         * @since 45850 
         */
        declare function getSimulationStreamPath(): String;

        /**
         * Returns the path on the configuration folder. 
         */
        declare function getConfigurationFolder(): String;

        /**
         * Returns the path on the configuration file. 
         */
        declare function getConfigurationPath(): String;

        /**
         * Returns the folder path on the running post processor. 
         */
        declare function getPostProcessorFolder(): String;

        /**
         * Returns the path on the running post processor. 
         */
        declare function getPostProcessorPath(): String;

        /**
         * Returns the cascading path set by the previous post. By default this will be the {@link getOutputPath()}.
         * @since 41546 
         */
        declare function getCascadingPath(): String;

        /**
         * Sets the path used as the primary source for a cascading post. By default this will be the {@link getOutputPath()}. A cascading post is a specific post that takes as input the output from a previous post. The cascading post is run after the onTerminate() entry function.
         * @since 41546 
         */
        declare function setCascadingPath(): void;

        /**
         * Returns the security level in which is post processor is running.
         * @since 41349 
         */
        declare function getSecurityLevel(): Integer;

        /**
         * Exports the toolpath data to the given file.
         * Security: Requires security level 0.
         * @param path The destination path. 
         * @param format The file format. 
         */
        declare function exportNCAs(
            path: String,
        ): void;

        /**
         * Runs the specified command. Note that you cannot open the primary output file until the onTerminate() is invoked. Post processor waits for the program to complete.
         * Security: Requires security level 0.
         * Security: Requires security level 0. 
         * @param path The path of the command. 
         * @param arguments The arguments to be parsed to the command. 
         * @param hide Hide application. 
         * @param workingFolder The working folder.
         */
        declare function execute(
            path: String,
        ): Integer;

        /**
         * Runs the specified command. Same as {@link execute()} but doesn't wait for application to complete.
         * Security: Requires security level 0.
         * Security: Requires security level 0. 
         * @param path The path of the command. 
         * @param arguments The arguments to be parsed to the command. 
         * @param hide Hide application. 
         * @param workingFolder The working folder.
         */
        declare function executeNoWait(
            path: String,
        ): void;

        /**
         * Sets the EOL style. Windows EOL style CRLF is the default.
         * @param eol The EOL to use. Only these are allowed CR, LF, CRLF, and LFCR. 
         */
        declare function setEOL(
            eol: String
        ): void;

        /**
         * Returns true if the output is being redirected. 
         */
        declare function isRedirecting(): Boolean;

        /**
         * Closes any redirection and switches to normal output. If redirection to buffer is active the buffer is cleared. 
         */
        declare function closeRedirection(): void;

        /**
         * Redirects output to the specified file.
         * Security: Requires security level 0.
         */
        declare function redirectToFile(
            path: String
        ): void;

        /**
         * Redirects the output to the global buffer. Use {@link getRedirectionBuffer()} to access the current buffer content. Use {@link getRedirectionBuffer2()} to clear the buffer during redirection. Close redirection again using {@link closeRedirection()}. 
         */
        declare function redirectToBuffer(): void;

        /**
         * Returns the current content of the redirection buffer. 
         */
        declare function getRedirectionBuffer(): String;

        /**
         * Returns the current content of the redirection buffer.
         * @param clear Clears the redirection buffer if true. 
         */
        declare function getRedirectionBuffer2(
            clear: Boolean
        ): String;

        /**
         * Registers post processing of the output file using the specified executable. 
         */
        declare function registerPostProcessing(
            path: String
        ): void;

        /**
         * Returns the workpiece bounding box.
         * @since r19573 
         */
        declare function getWorkpiece(): BoundingBox;

        /**
         * Returns the fixture bounding box.
         * @since r40294 
         */
        declare function getFixture(): BoundingBox;

        /**
         * Returns list of the available machine configurations. 
         */
        declare function getMachineConfigurations(): String;

        /**
         * Returns the specified machine configuration by name.
         * @param name The name of the machine configuration. 
         */
        declare function getMachineConfigurationByName(
            name: String
        ): MachineConfiguration;

        /**
         * Loads the specified machine configuration.
         * @param path The path of the machine configuration. 
         */
        declare function loadMachineConfiguration(
            path: String
        ): MachineConfiguration;

        /**
         * Returns true if user interaction is allowed. 
         */
        declare function isInteractionAllowed(): Boolean;

        /**
         * Alerts the user and wait for user.
         * @param title The title. 
         * @param description The description. 
         */
        declare function alert(
            title: String,
        ): void;

        /**
         * Waits for the user to hit a key. In console mode ESC or RETURN cancels. Not supported in graphical user interaction mode.
         * @param title The title. 
         * @param description The description.
         */
        declare function promptKey(
            title: String,
        ): String;

        /**
         * Waits for the user to hit a key. In console mode ESC or RETURN cancels or returns the first character in the accept string. The following accept modes are available in graphical user interface:
         * @param title The title. 
         * @param description The description. 
         * @param accept The allowed characters. Case is ignored. The first character in the string is the default.
         */
        declare function promptKey2(
            title: String,
        ): String;

        /**
         * Waits for the user to hit a key. In console mode ESC or RETURN cancels or returns the first character in the accept string. The following accept modes are available in graphical user interface:
         * @param title The title. 
         * @param description The description. 
         * @param accept The allowed answers. Case is ignored. The first character in the string is the default. 
         * @param keys The allowed characters used in console mode. Case is ignored. The first character in the string is the default.
         */
        declare function promptKey3(
            title: String,
        ): String;

        /**
         * Waits for user to enter a text string. Not supported in graphical user interface mode.
         */
        declare function promptText(
            title: String,
        ): String;

        /**
         * Converts the specified text to a float. Raises an exception if all the specified text cannot be converted.
         * @param text The text to be converted. 
         */
        declare function getAsFloat(
            text: String
        ): Number;

        /**
         * Returns true if the specified string only contains the specified characters.
         * @param text The string. 
         * @param permitted The permitted characters. 
         */
        declare function isSafeText(
            text: String,
        ): Boolean;

        /**
         * Returns a new string with all characters not present in keep removed.
         * @param text The original text. 
         * @param keep The characters to be preserved in the string. 
         */
        declare function filterText(
            text: String,
        ): String;

        /**
         * Translates the specified string into a new string. The characters in src will be mapped to the character at the same index in dest.
         * @param text The original text. 
         * @param src The chars to be mapped. 
         * @param dest The destination characters. 
         */
        declare function translateText(
            text: String,
        ): String;

        /**
         * Returns the text loaded from the specified url. The url is relative to the configuration script. The file size is limited to 1Mb.
         * @param url The url. 
         * @param encoding The encoding (ascii, utf-7, utf-8, or utf-16). Alternative you can specify the code page as a string (e.g. 437). 
         */
        declare function loadText(
            url: String,
        ): String;

        /**
         * Returns the output unit. 
         */
        declare function getOutputUnit(): Integer;

        /**
         * Sets the output unit. 
         */
        declare function setOutputUnit(
            unit: Integer
        ): void;

        /**
         * Returns the dog-leg flag. The default is false. 
         */
        declare function getDogLeg(): Boolean;

        /**
         * Sets the dog-leg style. 3-axis rapid traversal will be substituted with dog-leg style rapid traversal when this flag is set to true. 
         */
        declare function setDogLeg(
            dogLeg: Boolean
        ): void;

        /**
         * Returns the current rotation. 
         */
        declare function getRotation(): Matrix;

        /**
         * Sets the rotation. This is normally used for rotating around the tool axis. 
         */
        declare function setRotation(
            rotation: Matrix
        ): void;

        /**
         * Cancels any active output frame rotation defined by {@link setRotation()}.
         * @since r42292 
         */
        declare function cancelRotation(): void;

        /**
         * Returns the current translation. 
         */
        declare function getTranslation(): Vector;

        /**
         * Cancels any active output frame transformation defined by {@link setRotation()} and {@link setTranslation()}. 
         */
        declare function cancelTransformation(): void;

        /**
         * Sets the translation. 
         */
        declare function setTranslation(
            translation: Vector
        ): void;

        /**
         * Cancels any active output frame translation defined by {@link setTranslation()}.
         * @since r42292 
         */
        declare function cancelTranslation(): void;

        /**
         * Returns the specified section position in the current frame specified using {@link setRotation()} and {@link setTranslation()}. 
         */
        declare function getFramePosition(
            position: Vector
        ): Vector;

        /**
         * Returns the specified section direction in the current frame specified using {@link setRotation()}. 
         */
        declare function getFrameDirection(
            direction: Vector
        ): Vector;

        /**
         * Returns the specified frame position in the current section frame. Reverse mapping of {@link getFramePosition()}. 
         */
        declare function getSectionFramePosition(
            framePosition: Vector
        ): Vector;

        /**
         * Returns the specified frame direction in the current section frame. Reverse mapping of {@link getFrameDirection()}. 
         */
        declare function getSectionFrameDirection(
            frameDirection: Vector
        ): Vector;

        /**
         * Returns the specified position in the current section as the corresponding global position. 
         */
        declare function getGlobalPosition(
            p: Vector
        ): Vector;

        /**
         * Returns the specified position in the current section in the WCS. You likely want to use {@link getGlobalPosition()} instead. 
         */
        declare function getWCSPosition(
            p: Vector
        ): Vector;

        /**
         * Returns the specified global position as the corresponding position in the current section. 
         */
        declare function getSectionPosition(
            p: Vector
        ): Vector;

        /**
         * Returns the current position in the global coordinate system. 
         */
        declare function getCurrentGlobalPosition(): Vector;

        /**
         * Returns the current position. 
         */
        declare function getCurrentPosition(): Vector;

        /**
         * Sets the current position. 
         */
        declare function setCurrentPosition(
            currentPosition: Vector
        ): void;

        /**
         * Sets only the current X position. 
         */
        declare function setCurrentPositionX(
            x: Number
        ): void;

        /**
         * Sets only the current Y position. 
         */
        declare function setCurrentPositionY(
            y: Number
        ): void;

        /**
         * Sets only the current Z position. 
         */
        declare function setCurrentPositionZ(
            z: Number
        ): void;

        /**
         * Returns the tool axis if the section is not optimized for the machine. Returns the rotary angles if the section is optimnized for the machine. 
         */
        declare function getCurrentDirection(): Vector;

        /**
         * Sets the tool axis if the section is not optimized for the machine. Sets the rotary angles if the section is optimnized for the machine. 
         */
        declare function setCurrentDirection(
            currentDirection: Vector
        ): void;

        /**
         * Returns the current ABC position.
         * @since r45909 
         */
        declare function getCurrentABC(): Vector;

        /**
         * Returns the current tool axis.
         * @since r45909 
         */
        declare function getCurrentToolAxis(): Vector;

        /**
         * Sets the current tool axis.
         * @since r45909 
         */
        declare function setCurrentToolAxis(
            currentToolAxis: Vector
        ): void;

        /**
         * Returns the current spindle speed.
         * @since r42066 
         */
        declare function getCurrentSpindleSpeed(): Number;

        /**
         * Sets the current spindle speed.
         * @since r42066 
         */
        declare function setCurrentSpindleSpeed(
            spindleSpeed: Number
        ): void;

        /**
         * Called to pass the current machine angles back to the post engine.
         * This function allows machine angles chosen in a post script to be passed back to the post engine. This is used to drive simulation and should accompany calls to {@link setRotation()}.
The machine angles set by this function will be cleared by a call to {@link cancelRotation()} or {@link cancelTransformation()} 
         * @param abc A vector representing the machine axis angles where a, b, and c map to the vector's x, y, and z respectively. 
         * @since r45672 
         */
        declare function setCurrentABC(
            abc: Vector
        ): void;

        /**
         * Tells the post processor to skip the remaining of the current section. {@link onSectionEnd()} will still be invoked. 
         */
        declare function skipRemainingSection(): void;

        /**
         * Returns true if the current spindle direction is clockwise. The spindle may be stopped. The direction is assumed to be clockwise by default. 
         */
        declare function isClockwiseSpindleDirection(): Boolean;

        /**
         * Returns true if the spindle is active. The direction is assumed to be inactive by default. 
         */
        declare function isSpindleActive(): Boolean;

        /**
         * Returns true if the coolant is active. The coolant is assumed to be inactive by default. 
         */
        declare function isCoolantActive(): Boolean;

        /**
         * Returns whether the section is a probing operation
         * @param section The section to check for a probing operation. section is optional and if omitted currentSection will be used. Since r45843
         * @since r45600 
         */
        declare function isProbeOperation(
            section: Section
        ): Boolean;

        /**
         * Returns whether the current section is an inspection operation
         * @param section The section to check for an inspection operation. section is optional and if omitted currentSection will be used. Since r45843
         * @since r45600 
         */
        declare function isInspectionOperation(
            section: Section
        ): Boolean;

        /**
         * Returns whether the current section is a deposition operation
         * @param section The section to check for a deposition operation. section is optional and if omitted currentSection will be used.
         * @since r45843 
         */
        declare function isDepositionOperation(
            section: Section
        ): Boolean;

        /**
         * Returns whether the current section is a drilling operation
         * @param section The section to check for a drilling operation. section is optional and if omitted currentSection will be used. 
         * @param checkBoringCycles Set to true if boring cycles with a shift value should be considered a drilling cycle, set to false otherwise
         * @since r45843 
         */
        declare function isDrillingCycle(
            section: Section,
        ): Boolean;

        /**
         * Returns whether the current section is a tapping operation
         * @param section The section to check for a tapping operation. section is optional and if omitted currentSection will be used.
         * @since r45843 
         */
        declare function isTappingCycle(
            section: Section
        ): Boolean;

        /**
         * Returns whether the current section is an axial center drilling operation
         * @param section The section to check for an axial center drilling operation. section is optional and if omitted currentSection will be used. 
         * @param checkLiveTool Set to true if live tools are respected and not considered for axial drilling cycles, false ignores the live tool setting
         * @since r45843 
         */
        declare function isAxialCenterDrilling(
            section: Section,
        ): Boolean;

        /**
         * Returns whether the current section is a milling cycle
         * @param section The section to check for a milling cycle. section is optional and if omitted currentSection will be used. 
         * @param checkBoringCycles Set to true if boring cycles with a shift value should be considered a milling cycle, set to false otherwise
         * @since r45843 
         */
        declare function isMillingCycle(
            section: Section,
        ): Boolean;

        /**
         * Returns true if the speed-feed asynchronization is active. The synchronization is assumed to be inactive by default. 
         */
        declare function isSpeedFeedSynchronizationActive(): Boolean;

        /**
         * Returns true if the program is a 3D program (i.e. the tool axis points along the Z-axis for all the sections). 
         */
        declare function is3D(): Boolean;

        /**
         * Returns true if the post processor is in multi-axis mode. 
         */
        declare function isMultiAxis(): Boolean;

        /**
         * Returns true if the program is a multi-channel program.
         * @since r42045 
         */
        declare function isMultiChannelProgram(): Boolean;

        /**
         * Returns the number of channels for a multi-channel program.
         * @since r42045 
         */
        declare function getNumberOfChannels(): Integer;

        /**
         * Returns the id for the current channel.
         * @since r42045 
         */
        declare function getCurrentChannel(): Integer;

        /**
         * Returns the number of sync groups in the program.
         * @since r42045 
         */
        declare function getNumberOfSyncGroups(
            channel: Integer
        ): Integer;

        /**
         * Returns the multi-channel sync group for the current channel. Starting from 0.
         * @since r42045 
         */
        declare function getCurrentSyncGroup(): Integer;

        /**
         * Returns the number of records. 
         */
        declare function getNumberOfRecords(): Integer;

        /**
         * Returns the specified record. 
         */
        declare function getRecord(
            id: Integer
        ): Record;

        /**
         * Returns the id of the current section. Returns -1 is no section is active. 
         */
        declare function getCurrentSectionId(): Integer;

        /**
         * Returns the number of sections. 
         */
        declare function getNumberOfSections(): Integer;

        /**
         * Returns the specified section. The index must be in the range [0; {@link getNumberOfSections()}]. 
         */
        declare function getSection(
            index: Integer
        ): Section;

        /**
         * Returns the previous section. Must be called within a section. 
         */
        declare function getPreviousSection(): Section;

        /**
         * Returns true if the current section is succeeded by a section. 
         */
        declare function hasNextSection(): Boolean;

        /**
         * Returns the next section. 
         */
        declare function getNextSection(): Section;

        /**
         * Returns the tools. The order of the tools within the tool table is unspecified. Deprecated, use getToolList instead. 
         */
        declare function getToolTable(): ToolTable;

        /**
         * Returns true if the current cycle is a well-known cycle. 
         */
        declare function isWellKnownCycle(): Boolean;

        /**
         * Prints the tool table. 
         * @param orderBy An enum of the column to order the table by. It can be:
TOOL_NUMBER_COL
DIAMETER_COL
CORNER_RADIUS_COL
TAPER_COL
ZMIN_COL
DESCRIPTION_COL
TOOL_TABLE_NUM_COLS 


         * @since r45600 
         */
        declare function writeToolTable(
            orderBy: Integer
        ): void;

        /**
         * Returns the id of the current record. 
         */
        declare function getCurrentRecordId(): Integer;

        /**
         * Returns the machining distance for the specified tool at the current position. 
         */
        declare function getMachiningDistance(
            tool: Integer
        ): Number;

        /**
         * Returns true if the current motion is a result of an expanded motion. Expanded motion is for instance produced by {@link expandCyclePoint()}. 
         */
        declare function isExpanding(): Boolean;

        /**
         * Returns the end position for the current motion. 
         */
        declare function getEnd(): Vector;

        /**
         * Returns the length of the current motion. 
         */
        declare function getLength(): Number;

        /**
         * Returns the center for the current circular motion. Returns an unspecified value if the current motion is not circular. 
         */
        declare function getCircularCenter(): Vector;

        /**
         * Returns the distance from the circular start point to the circular center for the current circular motion. Returns an unspecified value if the current motion is not circular. 
         * @since r45991 
         */
        declare function getCircularOffset(): Vector;

        /**
         * Returns the start radius for the current circular motion. Returns an unspecified value if the current motion is not circular. The start radius can only be different from the end radius returned by {@link getCircularRadius()} for spiral motion. 
         */
        declare function getCircularStartRadius(): Number;

        /**
         * Returns the end radius for the current circular motion. Returns an unspecified value if the current motion is not circular. 
         */
        declare function getCircularRadius(): Number;

        /**
         * Returns the sweep for the current circular motion. Returns an unspecified value if the current motion is not circular. 
         */
        declare function getCircularSweep(): Number;

        /**
         * Returns the chord length for the current circular motion. Returns an unspecified value if the current motion is not circular. Returns 0 for full circular motion. 
         */
        declare function getCircularChordLength(): Number;

        /**
         * Returns the arc length for the current circular motion. Returns an unspecified value if the current motion is not circular. 
         * @since r45991 
         */
        declare function getCircularArcLength(): Number;

        /**
         * Returns true if the current circular motion is clockwise. Returns an unspecified value if the current motion is not circular. 
         */
        declare function isClockwise(): Boolean;

        /**
         * Returns true if the current circular motion is a full circle (360deg). Returns an unspecified value if the current motion is not circular. 
         */
        declare function isFullCircle(): Boolean;

        /**
         * Returns true if the current circular motion is helical. Returns an unspecified value if the current motion is not circular. 
         */
        declare function isHelical(): Boolean;

        /**
         * Returns true if the current circular motion is a spiral (i.e. the start and end radii do not match). Returns an unspecified value if the current motion is not circular. 
         */
        declare function isSpiral(): Boolean;

        /**
         * The CCW-normal of the circular plane. However, the normal is flipped if {@link isClockwise()} is true. This behavior ensures direct compatibility with RS274 for the right hand G17, G18, and G19 plane convention.
         */
        declare function getCircularNormal(): Vector;

        /**
         * Returns the circular plane. Returns -1 is the plane is not in a primary plane. 
         */
        declare function getCircularPlane(): Integer;

        /**
         * Returns the helical offset of the current circular motion. Returns (0, 0, 0) for non-helical motion. Returns an unspecified value if the current motion is not circular. 
         */
        declare function getHelicalOffset(): Vector;

        /**
         * Returns the helical distance of the current circular motion. Returns 0 for non-helical motion. Returns an unspecified value if the current motion is not circular. 
         */
        declare function getHelicalDistance(): Number;

        /**
         * Returns the helical pitch of the current circular motion. Returns 0 for non-helical motion. Returns an unspecified value if the current motion is not circular. 
         */
        declare function getHelicalPitch(): Number;

        /**
         * Returns true if the current record can be linearized using {@link getPositionU()}. Only circular records can currently be linearized. The tolerance is specified in MM. 
         */
        declare function canLinearize(): Boolean;

        /**
         * Linearizes the current motion to the specified tolerance. Use {@link canLinearize()} to determine if the current record can be linearized. The start position is not output. 
         */
        declare function linearize(
            tolerance: Number
        ): void;

        /**
         * Returns the number of segments required to linearize the current record. 
         */
        declare function getNumberOfSegments(
            tolerance: Number
        ): Integer;

        /**
         * Returns the position at the specified u coordinate for the current motion. The u coordinate is clamped to the range [0; 1]. 
         */
        declare function getPositionU(
            u: Number
        ): Vector;

        /**
         * Returns the circular motion description for the current circular record. 
         */
        declare function getCircularMotion(): CircularMotion;

        /**
         * Returns the current feedrate. 
         */
        declare function getFeedrate(): Number;

        /**
         * Returns the active feedrate mode.
         */
        declare function getActiveFeedMode(): Integer;

        /**
         * Returns the current movement type. 
         */
        declare function getMovement(): Integer;

        /**
         * Returns the current power mode (water jet, laser cutter, and plasma cutter). 
         */
        declare function getPower(): Boolean;

        /**
         * Returns the current spindle speed. 
         */
        declare function getSpindleSpeed(): Number;

        /**
         * Returns whether the section's spindle speed or spindle mode (RPM/SFM) is different than the previous section's spindle speed or mode
         * @param section The section to check for a spindle speed/mode change. section is optional and if omitted currentSection will be used.
         * @since r45843 
         */
        declare function isSpindleSpeedDifferent(
            section: Section
        ): Boolean;

        /**
         * Returns the current radius compensation 
         */
        declare function getRadiusCompensation(): Integer;

        /**
         * Returns the current compensation offset. 
         */
        declare function getCompensationOffset(): Integer;

        /**
         * Returns true if the section has a previous record. 
         */
        declare function hasPreviousRecord(): Boolean;

        /**
         * Returns the previous record for the current section. 
         */
        declare function getPreviousRecord(): Record;

        /**
         * Returns true if the section has a next record. 
         */
        declare function hasNextRecord(): Boolean;

        /**
         * Returns the next record for the current section. 
         */
        declare function getNextRecord(): Record;

        /**
         * Returns the first tool. 
         */
        declare function getFirstTool(): Tool;

        /**
         * Specifies that the entry function should be written. 
         */
        declare function setWriteInvocations(
            writeInvocations: Boolean
        ): void;

        /**
         * Specifies that a stack dump should be written when text is output by the post processor. 
         */
        declare function setWriteStack(
            writeStack: Boolean
        ): void;

        /**
         * Writes the notes for the current section as comments.
         * @since r45600 
         */
        declare function writeSectionNotes(): void;

        /**
         * Writes the notes for the current setup as comments.
         * @since r45600 
         */
        declare function writeSetupNotes(): void;

        /**
         * Returns true if the current cycle point is the first motion for the current cycle. Returns false if cycle is not active. 
         */
        declare function isFirstCyclePoint(): Boolean;

        /**
         * Returns true if the current cycle point is the last motion for the current cycle. Returns false if cycle is not active. 
         */
        declare function isLastCyclePoint(): Boolean;

        /**
         * Returns the id of the current cycle point. Returns 0 if cycle is not active.
         * @since r19569 
         */
        declare function getCyclePointId(): Integer;

        /**
         * Returns the number of cycle points for the current cycle.
         * @since r19569 
         */
        declare function getNumberOfCyclePoints(): Integer;

        /**
         * Returns the specified cycle point for the current cycle.
         * @param index The index of the point. Must be less than the value returned by getNumberOfCyclePoints().
         * @since r19569 
         */
        declare function getCyclePoint(
            index: Integer
        ): Vector;

        /**
         * Invoke this method to tell the post processor about state changes without triggering invocation of {@link onCommand()}. This method must be invoked when a state doesn't originate from an invocation of an entry function. If this method is not invoked the post processor may not know certain current state (e.g. whether the coolant is active). For instance, the state is important when the post processor is asked to expand cycles. 
         */
        declare function onImpliedCommand(
            command: Integer
        ): void;

        /**
         * Returns true if the specified parameter has been defined globally.
         * @since r16737 
         */
        declare function hasGlobalParameter(
            name: String
        ): Boolean;

        /**
         * Returns the first value of the specified global parameter.
         * e.g.
         * @param name The name of the parameter to return 
         * @param defaultValue (optional, since r45606) The default value of the parameter to return if it is not defined.
         * @since r16737 
         */
        declare function getGlobalParameter(
            name: String,
        ): Value;

        /**
         * Sets the supported post features based on minimum post engine revision.
         * Each property in features must map a feature constant name to the minimum engine revision that supports that feature. A feature is enabled when the current engine revision is greater than or equal to the specified revision.
         * Supported feature constants are:
         * - FEATURE_TOOL_CALL_CYCLE Sincer50279

         * - FEATURE_MACHINE_ROTARY_ANGLES Sincer50328

         * - FEATURE_POLAR_MODE Sincer50356
Parameters
  
    featuresMap of feature constants to minimum engine revisions.
  
  

e.g.
         * @since r50353 
         */
        declare function setSupportedFeatures(): void;

        /**
         * Returns the value of the specified post property. If the specified property doesn't exist, it returns the default value if specified, otherwise it returns undefined.
         * You can retrieve an operation property of a specific section by using the 'section.getProperty()' function. It takes the same arguments as {@link getProperty()}. Since r45811.
         * e.g.
         * @param property The property object or the property name. 
         * @param defaultValue (optional) The default value to return if the property does not exist. Since r45811.
         * @since r45702 
         */
        declare function getProperty(): Value;

        /**
         * Sets the value of the specified post property. The specified property must exist.
         * e.g.
         * @param property The property object or the property name. 
         * @param value The new value of the property.
         * @since r45811 
         */
        declare function setProperty(): void;

        /**
         * Returns true if the specified parameter has been defined.
         * Only parameters before the current record will be checked. 
         */
        declare function hasParameter(
            name: String
        ): Boolean;

        /**
         * Returns the value of the specified parameter.
         * Only parameters before the current record will be checked.
         * e.g.
         * @param name The name of the parameter to return. 
         * @param defaultValue (optional, since r45606) The default value of the parameter to return if it is not defined.
         */
        declare function getParameter(
            name: String,
        ): Value;

        /**
         * Registers a termination handler to be called after onTerminate(). The termination handlers are called in the opposite order of registration.
         * @param function The termination handler. Must be a function.
         * @since 41664 
         */
        declare function registerTerminationHandler(): void;

        /**
         * Returns the specified angle in degrees.
         * @param radians the angle in radians 
         */
        declare function toDeg(
            radians: Number
        ): Number;

        /**
         * Returns the specified angle in radians.
         * @param degrees the angle in degrees. 
         */
        declare function toRad(
            degrees: Number
        ): Number;

        /**
         * Returns the given value and spatial unit in the current spatial unit.
         * The value can start with a sign (+ or -) followed by the digits and decimal ('.') and end with the unit.
         * The supported units are: yd, ft, in, mil or mils, thou, m, cm, mm, um, nm
         * yd = 3 ft ft = 12 in in = 1000 mil in = 1000 thou
         * m = 100 cm cm = 10 mm mm = 1000 um um = 1000 nm 
         */
        declare function parseSpatial(
            value: String
        ): Number;

        /**
         * Returns the plane id for the given direction vector.
         * {@link Vector(0, 0, 1)}: PLANE_XY {@link Vector(0, 1, 0)}: PLANE_ZX {@link Vector(1, 0, 0)}: PLANE_YZ
         */
        declare function getPlane(
            direction: Vector
        ): Integer;

        /**
         * Returns the standard G-code plane number (17, 18, or 19).
         * PLANE_XY: 17 PLANE_ZX: 18 PLANE_YZ: 19
         * Returns 0 for all other planes. 
         */
        declare function getISOPlane(
            plane: Integer
        ): Integer;

        /**
         * Returns true if the 2 specified normalized vectors are in the same direction.
         * The built-in implementation is:
         * You can easily change the post behavior by adding your own implementation of the function. 
         */
        declare function isSameDirection(): Boolean;

        /**
         * Returns the array of sections which use the given channel.
         * @param channel The channel.
         * @since r42040 
         */
        declare function getSectionsInChannel(
            channel: Number
        ): Array<any>;

        /**
         * Validates the property definitions. 
         */
        declare function validatePropertyDefinitions(): Boolean;

        /**
         * Returns true if all user defined properties are valid according to the property definitions. 
         */
        declare function validateProperties(): Boolean;

        /**
         * Get the program name as an {@link Integer} if it is within the [min, max] range, or error otherwise.
         * @param min The minimum possible value for program name (inclusive). 
         * @param max The maximum possible value for program name (inclusive).
         * @since r42272 
         */
        declare function getProgramNameAsInt(
            min: Integer,
        ): Integer;

        /**
         * Get the program name as a {@link String}, truncated if it's longer than the character limit.
         * @param charLimit The maximum number of characters for program name.
         * @since r42272 
         */
        declare function getProgramNameAsString(
            charLimit: Integer
        ): String;

        /**
         * Returns whether a tool change is needed, based on the given criteria as arguments. If "description" is provided as an argument, then the tool description must be defined or an error will be generated.
         * @param section The section to determine if a tool change is needed. section is optional and if omitted currentSection will be used. Since r45843 
         * @param arguments The function can take any number of string arguments, which are the comparison criteria for the current and previous tools. These can be "number", "description", "diameter", or any other property on the tool object. The tool number is checked if there are no arguments.
         * @since r45600 
         */
        declare function isToolChangeNeeded(
            section: Section,
        ): Boolean;

        /**
         * Returns the next tool with a different tool number.
         */
        declare function getNextTool(
            number: Integer
        ): Tool;

        /**
         * Returns the next tool, based on the given criteria as arguments.
         * @param section The section to retrieve the next tool for. section is optional and if omitted currentSection will be used. Since r45843 
         * @param firstTool Returns the first tool if the end of program is reached when set to true. Returns undefined when set to false. Since r45843 
         * @param arguments The function can take any number of string arguments, which are the comparison criteria for the current and next tools. These can be "number", "description", "diameter", or any other property on the tool object. The tool number is checked if there are no arguments.
         * @since r45600 
         */
        declare function getNextTool(
            section: Section,
        ): Tool;

        /**
         * Returns an array of tools used in the program deemed different based on the 'arguments' and 'flag' criteria.
         * @param arguments The function can take any number of string arguments, which are the comparison criteria for determining if a tool is different from a previously found tool. These can be "number", "description", "lengthCompensation", or any other property in the tool object. The tool number is checked if there are no arguments. 
         * @param flag A bitwise of SORT or NOSORT and DUPLICATES or NODUPLICATES. For example, SORT | NODUPLICATES (which is the default setting). Sorting is based on the first property passed in 'arguments'. If the first property matches during a sort, then the tools will be listed in the order that they are used.
         * @since r45948 
         */
        declare function getToolList(
            arguments: String,
        ): Array<any>;

        /**
         * Returns whether a new work plane is needed for the given section.
         * @param section The section to check the work plane. section is optional and if omitted currentSection will be used.
         * @since r42290 
         */
        declare function isNewWorkPlane(
            section: Section
        ): Boolean;

        /**
         * Returns whether a new work offset is used for the given section.
         * @param section The section to check the work offset. section is optional and if omitted currentSection will be used.
         * @since r45843 
         */
        declare function isNewWorkOffset(
            section: Section
        ): Boolean;

        /**
         * Returns the Z range for the currently active tool, for the current and upcoming consecutive sections that use this tool. Returns undefined if the tool does not have a Z axis (e.g. on 2D operations).
         * @since r45600 
         */
        declare function toolZRange(): Range;

        /**
         * Returns an array with values in the requested range.
         * range(end) range(first, end) range(first, end, step)
         */
        declare function range(
            first: Number,
        ): Array<any>;

        /**
         * Returns an array with values in the requested interval. Also see {@link range()}. from must be less than or equal to to.
         * @param from The first value (inclusive). 
         * @param to The last value (inclusive).
         */
        declare function interval(
            from: Number,
        ): Array<any>;

        /**
         * Flattens the specified array.
         * @param array The array to flatten.
         */
        declare function flatten(
            array: Array<any> // This generic type have been defaulted to any types.
        ): Array<any>;

        /**
         * Returns the quadrant for the specified angle (0 -&gt; 3). 
         */
        declare function getQuadrant(
            angle: Number
        ): Integer;

        /**
         * Returns the specified value if the condition is true and otherwise the empty string. 
         */
        declare function conditional(): String;

        /**
         * Raises an exception if the expression is false. Use this function to make sure that you have protected your code against invalid cases.
         * @param expression The boolean expression. 
         * @param message Optional description of the constraint.
         */
        declare function validate(
            expression: Value,
        ): void;

        /**
         * Writes the specified debug message to the log if debugging mode is enabled (i.e. debugMode is true).
         * Multiple arguments are allowed 
         * @since r41291 where the arguments will be concatenated using formatWords(). 
         */
        declare function debug(
            message: String
        ): void;

        /**
         * Returns the specified spatial value and unit in the internal unit.
         * Note: IN values are scaled with 25 relative to MM.
         * @param value The value. 
         * @param unit The unit of the value. 
         */
        declare function spatial(
            value: Number,
        ): Number;

        /**
         * Returns the specified value in the output unit.
         * Note: The unit conversion scale used is 25mm to 1in and not 25.4.
         * @param value The value. 
         * @param unit The unit of the value. 
         */
        declare function toUnit(
            value: Number,
        ): Number;

        /**
         * Returns the specified value in the output unit.
         * Note: The unit conversion scale used is 25.4.
         * @param value The value. 
         * @param unit The unit of the value. 
         */
        declare function toPreciseUnit(
            value: Number,
        ): Number;

        /**
         * Returns the inverse time (F). 
         */
        declare function getInverseTime(
            distance: Number,
        ): Number;

        /**
         * Returns an error message if the current cycle is not supported. 
         */
        declare function cycleNotSupported(): void;

        /**
         * Returns true if the workpiece stock is defined. 
         */
        declare function isWorkpieceDefined(): Boolean;

        /**
         * Returns true for turning toolpath. 
         */
        declare function isTurning(): Boolean;

        /**
         * Returns true for milling toolpath. 
         */
        declare function isMilling(): Boolean;

        /**
         * Returns true for jet toolpath (waterjet, laser, and plasma cutting). 
         */
        declare function isJet(): Boolean;

        /**
         * Returns true if the current section is the first section. 
         */
        declare function isFirstSection(): Boolean;

        /**
         * Returns true if the current section is the last section. 
         */
        declare function isLastSection(): Boolean;

        /**
         * {@link onExpandedRapid()} calls the {@link onRapid()} entry function and lets the post processor know that the current position has changed at the same time. You should generally not call {@link onRapid()} directly. 
         */
        declare function onExpandedRapid(
            x: Number,
        ): void;

        /**
         * {@link onExpandedLinear()} calls the {@link onLinear()} entry function and lets the post processor know that the current position has changed at the same time. You should generally not call {@link onLinear()} directly. 
         */
        declare function onExpandedLinear(
            x: Number,
        ): void;

        /**
         * {@link onExpandedSpindleSpeed()} calls the {@link onSpindleSpeed()} entry function and lets the post processor know that the current spindle speed has changed at the same time. You should generally not call {@link onSpindleSpeed()} directly. 
         */
        declare function onExpandedSpindleSpeed(
            spindleSpeed: Number
        ): void;

        /**
         * Constructs a new machine configuration.
         * The supported specifiers are:
         * @param specifiers The specifiers. 
         */
        declare function createMachineConfiguration(
            specifiers: Map<any, any> // This generic type have been defaulted to any types.
        ): MachineConfiguration;

        /**
         * Returns a description of the specified machine configuration.
         * @param machine The machine configuration. 
         */
        declare function getMachineConfigurationAsText(
            machine: MachineConfiguration
        ): String;

        /**
         * Constructs a new machine axis.
         * The supported specifiers are:
         * @param specifiers The axis specifiers.
         */
        declare function createAxis(
            specifiers: Map<any, any> // This generic type have been defaulted to any types.
        ): Axis;

        /**
         * Constructs the format specification for the given values.
         * The supported specifiers are:
         * The following specifiers are deprecated since r45892.
         * You cannot mix the new specifiers with the deprecated specifiers in a single command.
         * - The following specifiers are supported since r45892.
         * @param specifiers The format specifiers.
         */
        declare function createFormat(
            specifiers: Map<any, any> // This generic type have been defaulted to any types.
        ): FormatNumber;

        /**
         * Creates a new {@link OutputVariable} instance.
         * The supported specifiers are:
         * @param specifiers The specifiers. 
         * @param format The desired format.
         * @since r45877 
         */
        declare function createOutputVariable(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): OutputVariable;

        /**
         * Creates a new {@link Variable} instance. DEPRECATED, use createOutputVariable.
         * The supported specifiers are:
         * @param specifiers The specifiers. 
         * @param format The desired format.
         */
        declare function createVariable(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): Variable;

        /**
         * Creates a new {@link IncrementalVariable} instance. DEPRECATED, use createOutputVariable.
         * The supported specifiers are:
         * @param specifiers The specifiers. 
         * @param format The desired format.
         */
        declare function createIncrementalVariable(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): IncrementalVariable;

        /**
         * Creates a new {@link ReferenceVariable} instance. DEPRECATED, use createOutputVariable.
         * The supported specifiers are:
         * The supported specifiers are:
         * @param specifiers The behavior specifiers. 
         * @param format The instance of type Format used to format the output values.
         */
        declare function createReferenceVariable(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): ReferenceVariable;

        /**
         * Creates a new {@link Modal} instance. DEPRECATED, use createOutputVariable.
         * The supported specifiers are:
         * @param specifiers The format specifiers. 
         * @param format The desired format (optional).
         */
        declare function createModal(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): Modal;

        /**
         * Creates a new {@link ModalGroup} instance.
         * The supported specifiers are:
         * @param specifiers The specifiers. 
         * @param groups The code groups (e.g. [[1, 2, 3, 4], [17, 18, 19]]). 
         * @param format The format. 
         */
        declare function createModalGroup(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): ModalGroup;

        /**
         * Repositions to cycle clearance plane. Attention: The tool is not moved over the hole if not required. The spindle axis must be along the Z-axis.
         * @param cycle The cycle parameters. 
         * @param x The X position of the cycle. 
         * @param y The Y position of the cycle. 
         * @param z The Z position of the cycle. 
         */
        declare function repositionToCycleClearance(
            cycle: Map<any, any>, // This generic type have been defaulted to any types.
        ): void;

        /**
         * May be invoked in cycle mode to expand a well-known cycle. Used when a specific cycle is not supported by a control.
         * @param x The X coordiante of the cycle point. 
         * @param y The Y coordiante of the cycle point. 
         * @param z The Z coordiante of the cycle point. 
         */
        declare function expandCyclePoint(
            x: Number,
        ): void;

        /**
         * Returns true if the given cycle is a probing cycle.
         * @param uri The identifier of the cycle.
         * @since r41188 
         */
        declare function isProbingCycle(
            uri: String
        ): Boolean;

        /**
         * Returns true if the given cycle is a sub spindle cycle.
         * @param uri The identifier of the cycle.
         * @since r41188 
         */
        declare function isSubSpindleCycle(
            uri: String
        ): Boolean;

        /**
         * Returns true if the specified command is well-known. 
         */
        declare function isWellKnownCommand(
            command: Integer
        ): Boolean;

        /**
         * Returns the string id for the specified command. 
         */
        declare function getCommandStringId(
            command: Integer
        ): String;

        /**
         * Returns true if the specified command can be safely ignored in normal cases. 
         */
        declare function canIgnoreCommand(
            command: Integer
        ): Boolean;

        /**
         * Writes an error message for the specified command. 
         */
        declare function onUnsupportedCommand(
            command: Integer
        ): void;

        /**
         * Processes Manual NC commands that are not handled in onManualNC. 
         * @param command The Manual NC command. 
         * @param value The value. 
         * @since 42292 
         */
        declare function expandManualNC(
            command: Integer,
        ): void;

        /**
         * Writes an error message for the specified coolant. 
         */
        declare function onUnsupportedCoolant(
            coolant: Integer
        ): void;

        /**
         * Converts the specified text to an integer. Raises an exception if all the specified text cannot be converted.
         * @param text The text to be converted. 
         */
        declare function getAsInt(
            text: String
        ): Integer;

        /**
         * Returns the specified coolant as a string. 
         */
        declare function getCoolantName(
            coolant: Integer
        ): String;

        /**
         * Returns the specified material as a string. 
         */
        declare function getMaterialName(
            material: Integer
        ): String;

        /**
         * Returns the specified tool as a string. 
         */
        declare function getToolTypeName(): String;

        /**
         * Invoked during post processing when the machine configuration changes. 
         */
        declare function onMachine(): void;

        /**
         * Invoked during initialization before the first section. 
         */
        declare function onOpen(): void;

        /**
         * The {@link onCycle()} entry function is invoked for each cycle. 
         */
        declare function onCycle(): void;

        /**
         * onCyclePoint() is the entry function for cycle positions. The function expands all well-known cycles by default. The specified position is the center of the hole for drilling or equivalent cycles. You should always use cycle.bottom as the bottom coordinate if defined.
         * @param x The x coordinate for the drill position. 
         * @param y The y coordinate for the drill position. 
         * @param z The z coordinate for the drill position. 
         */
        declare function onCyclePoint(
            x: Number,
        ): void;

        /**
         * The {@link onCyclePath()} entry function is invoked at the beginning of cycle toolpath. 
         */
        declare function onCyclePath(): void;

        /**
         * The {@link onCyclePathEnd()} entry function is invoked at the ending of cycle toolpath. 
         */
        declare function onCyclePathEnd(): void;

        /**
         * The {@link onCycleEnd()} entry function is invoked on cycle termination. 
         */
        declare function onCycleEnd(): void;

        /**
         * Parameter entry function.
         * @param name The name of the parameter. 
         * @param value The value of the parameter. 
         */
        declare function onParameter(
            name: String,
        ): void;

        /**
         * Pass-through entry function.
         * @param value The value. 
         */
        declare function onPassThrough(
            value: Value
        ): void;

        /**
         * Entry function for comments.
         * @param comment The comment. 
         */
        declare function onComment(
            comment: String
        ): void;

        /**
         * Entry function invoked for linear motion at rapid traverse.
         * @param x The X coordinate of the end position. 
         * @param y The Y coordinate of the end position. 
         * @param z The Z coordinate of the end position. 
         */
        declare function onRapid(
            x: Number,
        ): void;

        /**
         * Entry function invoked for linear motion at feed.
         * @param x The X coordinate of the end position. 
         * @param y The Y coordinate of the end position. 
         * @param z The Z coordinate of the end position. 
         * @param feed The feedrate. 
         */
        declare function onLinear(
            x: Number,
        ): void;

        /**
         * Entry function invoked for linear extrusion motion at feed.
         * @param x The X coordinate of the end position. 
         * @param y The Y coordinate of the end position. 
         * @param z The Z coordinate of the end position. 
         * @param feed The feedrate. 
         * @param extrusionLength The extrusion length. 
         */
        declare function onLinearExtrude(
            x: Number,
        ): void;

        /**
         * Entry function for circular motion.
         * @param clockwise Specifies that the motion is clockwise. 
         * @param cx The X coordinate of the center position. 
         * @param cy The Y coordinate of the center position. 
         * @param cz The Z coordinate of the center position. 
         * @param x The X coordinate of the end position. 
         * @param y The Y coordinate of the end position. 
         * @param z The Z coordinate of the end position. 
         * @param feed The feedrate. 
         */
        declare function onCircular(
            clockwise: Boolean,
        ): void;

        /**
         * Entry function invoked for circular extrusion motion at feed.
         * @param clockwise Specifies that the motion is clockwise. 
         * @param cx The X coordinate of the center position. 
         * @param cy The Y coordinate of the center position. 
         * @param cz The Z coordinate of the center position. 
         * @param x The X coordinate of the end position. 
         * @param y The Y coordinate of the end position. 
         * @param z The Z coordinate of the end position. 
         * @param feed The feedrate. 
         * @param extrusionLength The extrusion length. 
         */
        declare function onCircularExtrude(
            clockwise: Boolean,
        ): void;

        /**
         * Entry function invoked for linear rapid 5-axis motion at rapid traverse. The tool axis may also be returned in machine angles depending on the mode in accordance with the active machine configuration.
         * @param x The x coordinate of the position. 
         * @param y The y coordinate of the position. 
         * @param z The z coordinate of the position. 
         * @param dx The dx coordinate of the tool axis direction. 
         * @param dy The dy coordinate of the tool axis direction. 
         * @param dz The dz coordinate of the tool axis direction. 
         */
        declare function onRapid5D(
            x: Number,
        ): void;

        /**
         * Entry function invoked for linear 5-axis motion at feed. The tool axis may also be returned in machine angles depending on the mode in accordance with the active machine configuration.
         * @param x The x coordinate of the position. 
         * @param y The y coordinate of the position. 
         * @param z The z coordinate of the position. 
         * @param dx The dx coordinate of the tool axis direction. 
         * @param dy The dy coordinate of the tool axis direction. 
         * @param dz The dz coordinate of the tool axis direction. 
         * @param feed The feedrate value in the feedrate mode specified by 'feedMode'. 
         * @param feedMode The active feedrate mode. It can be FEED_PER_MINUTE or FEED_INVERSE_TIME. 
         */
        declare function onLinear5D(
            x: Number,
        ): void;

        /**
         * DEPRECATED. Entry function invoked for 5-axis motion when machine axis rewind is required.
         * @param a The A coordinate of the machine orientation. 
         * @param b The B coordinate of the machine orientation. 
         * @param c The C coordinate of the machine orientation. 
         */
        declare function onRewindMachine(
            a: Number,
        ): void;

        /**
         * Entry function invoked for 5-axis motion when machine axis rewind is required. This is called before performing a machine rewind in the kernel, as {@link onRewindMachine()} is now deprecated.
         * @param a The A coordinate of the machine orientation. 
         * @param b The B coordinate of the machine orientation. 
         * @param c The C coordinate of the machine orientation.
         * @since r45400 
         */
        declare function onRewindMachineEntry(
            a: Number,
        ): void;

        /**
         * Entry function invoked during a machine rewind procedure. It needs to output the code for retracting to safe position before indexing rotaries(usually a retract in Z).
         * @since r45400 
         */
        declare function onMoveToSafeRetractPosition(): void;

        /**
         * Entry function invoked during a machine rewind procedure. It needs to output the code for returning from safe position after indexing rotaries.
         * @param x The X coordinate to return to. 
         * @param y The Y coordinate to return to. 
         * @param z The Z coordinate to return to.
         * @since r45400 
         */
        declare function onReturnFromSafeRetractPosition(
            x: Number,
        ): void;

        /**
         * Entry function invoked during a machine rewind procedure. It needs to output the code for rotating the axes to new position above re-entry position.
         * @param x The X coordinate to rotate to. 
         * @param y The Y coordinate to rotate to. 
         * @param z The Z coordinate to rotate to. 
         * @param a The A coordinate to rotate to. 
         * @param b The B coordinate to rotate to. 
         * @param c The C coordinate to rotate to.
         * @since r45400 
         */
        declare function onRotateAxes(
            x: Number,
        ): void;

        /**
         * Entry function invoked when the movement type changes. 
         */
        declare function onMovement(
            movement: Integer
        ): void;

        /**
         * Entry function invoked when the power mode changes (used for water jet, laser cutter, and plasma cutter). 
         */
        declare function onPower(
            power: Boolean
        ): void;

        /**
         * Entry function invoked when the radius compensation mode changes. 
         */
        declare function onRadiusCompensation(): void;

        /**
         * Entry function invoked when the feedrate mode changes. 
         */
        declare function onFeedMode(
            mode: Integer
        ): void;

        /**
         * Entry function invoked when a Manual NC command is encountered. If the onManualNC function is not defined, then the appropriate function for the Manual NC command is called.
         * @param command The Manual NC command. 
         * @param value The value.
         * @since r42292 
         */
        declare function onManualNC(
            command: Integer,
        ): void;

        /**
         * Entry function invoked when the tool compensation changes. Only used for specific tool types for which dual compensation is defined. 
         */
        declare function onToolCompensation(
            compensation: Integer
        ): void;

        /**
         * Entry function invoked for dwelling.
         * @param time The dwelling time in seconds. 
         */
        declare function onDwell(
            time: Number
        ): void;

        /**
         * Entry function invoked for spindle speed changes.
         * @param spindleSpeed The spindle speed in RPM. 
         */
        declare function onSpindleSpeed(
            spindleSpeed: Number
        ): void;

        /**
         * Entry function invoked at the start of a layer in an additive FFF or DED toolpath.
         * @param layerNumber The layer number. 
         */
        declare function onLayer(
            layerNumber: Number
        ): void;

        /**
         * Entry function invoked at the end of a layer in a DED toolpath.
         * @param layerNumber The layer number. 
         */
        declare function onLayerEnd(
            layerNumber: Number
        ): void;

        /**
         * Entry function invoked when the extrusion length is reset in an additive FFF toolpath.
         * @param length The extrusion length. 
         */
        declare function onExtrusionReset(
            length: Number
        ): void;

        /**
         * Entry function invoked when the extruder is changed in an additive FFF toolpath.
         * @param extruderId The id of the extruder to change to. 
         */
        declare function onExtruderChange(
            extruderId: Number
        ): void;

        /**
         * Entry function invoked when the extruder temperature is changed in an additive FFF toolpath.
         * @param temp The target temperature. 
         * @param wait If true, wait to reach the desired temperature before continuing. 
         * @param extruderId The extruder id. 
         */
        declare function onExtruderTemp(
            temp: Number,
        ): void;

        /**
         * Entry function invoked when the bed temperature is changed in an additive FFF toolpath.
         * @param temp The target temperature. 
         * @param wait If true, wait to reach the desired temperature before continuing. 
         */
        declare function onBedTemp(
            temp: Number,
        ): void;

        /**
         * Entry function invoked when the fan speed is changed in an additive FFF toolpath.
         * @param speed The fan speed. 
         * @param fanId The fan id. 
         */
        declare function onFanSpeed(
            speed: Number,
        ): void;

        /**
         * Entry function invoked when the max axis acceleration is changed in an additive FFF toolpath.
         * @param xAxis The X axis max acceleration. 
         * @param yAxis The Y axis max acceleration. 
         * @param zAxis The Z axis max acceleration. 
         * @param eAxis The E axis max acceleration. 
         */
        declare function onMaxAcceleration(
            xAxis: Number,
        ): void;

        /**
         * Entry function invoked when the acceleration is changed in an additive FFF toolpath.
         * @param travel Travel acceleration. Used for moves that include no extrusion. 
         * @param printing Printing acceleration. Used for moves that include extrusion (i.e., which employ the current tool). 
         * @param retract Retract acceleration. Used for extruder retraction moves. 
         */
        declare function onAcceleration(
            travel: Number,
        ): void;

        /**
         * Entry function invoked when the axis jerk is changed in an additive FFF toolpath.
         * @param xAxis The X axis jerk. 
         * @param yAxis The Y axis jerk. 
         * @param zAxis The Z axis jerk. 
         * @param eAxis The E axis jerk. 
         */
        declare function onJerk(
            xAxis: Number,
        ): void;

        /**
         * Entry function invoked for well-known commands.
         * The known commands are:
         * - COMMAND_STOP - Program stop (M00)
         * - COMMAND_OPTIONAL_STOP - Optional program stop (M01)
         * - COMMAND_END - Program end (M02)
         * - COMMAND_SPINDLE_CLOCKWISE - Clockwise spindle direction (M03)
         * - COMMAND_SPINDLE_COUNTERCLOCKWISE - Counterclockwise spindle direction (M04)
         * - COMMAND_START_SPINDLE - Start spindle M03 (clockwise) or M04 (counterclockwise)
         * - COMMAND_STOP_SPINDLE - Stop spindle (M05)
         * - COMMAND_ORIENTATE_SPINDLE - Orientate spindle - +X direction by default (M19)
         * - COMMAND_LOAD_TOOL - Tool change (M06)
         * - COMMAND_COOLANT_ON - Coolant on (M08)
         * - COMMAND_COOLANT_OFF - Coolant off (M09)
         * - COMMAND_ACTIVATE_SPEED_FEED_SYNCHRONIZATION - Active feed-speed synchronization
         * - COMMAND_DEACTIVATE_SPEED_FEED_SYNCHRONIZATION - Deactive feed-speed synchronization
         * - COMMAND_LOCK_MULTI_AXIS - Locks the 4th and 5th axes
         * - COMMAND_UNLOCK_MULTI_AXIS - Unlocks the 4th and 5th axes
         * - COMMAND_EXACT_STOP - Exact stop
         * - COMMAND_START_CHIP_TRANSPORT - Close chip transport
         * - COMMAND_STOP_CHIP_TRANSPORT - Stop chip transport
         * - COMMAND_OPEN_DOOR - Open primary door
         * - COMMAND_CLOSE_DOOR - Close primary door
         * - COMMAND_BREAK_CONTROL - Break control
         * - COMMAND_TOOL_MEASURE - Measure tool
         * - COMMAND_CALIBRATE - Run calibration cycle
         * - COMMAND_VERIFY - Verify part/tool/machine integrity
         * - COMMAND_CLEAN - Run cleaning cycle
         * - COMMAND_ALARM - Alarm
         * - COMMAND_ALERT - Alert
         * - COMMAND_CHANGE_PALLET - Change pallet
         * - COMMAND_POWER_ON - Power on
         * - COMMAND_POWER_OFF - Power off
         * - COMMAND_MAIN_CHUCK_OPEN - Open main chuck Sincer41179

         * - COMMAND_MAIN_CHUCK_CLOSE - Close main chuck Sincer41179

         * - COMMAND_SECONDARY_CHUCK_OPEN - Open secondary chuck Sincer41179

         * - COMMAND_SECONDARY_CHUCK_CLOSE - Close secondary chuck Sincer41179

         * - COMMAND_SECONDARY_SPINDLE_SYNCHRONIZATION_ACTIVATE - Activate spindle synchronization Sincer41179

         * - COMMAND_SECONDARY_SPINDLE_SYNCHRONIZATION_DEACTIVATE - Deactivate spindle synchronization Sincer41179

         * - COMMAND_SYNC_CHANNEL - Sync channels Sincer42013

         * - COMMAND_PROBE_ON - Probe on Sincer42013

         * - COMMAND_PROBE_OFF - Probe off Sincer42013 

         * @param command The well-known command.
         */
        declare function onCommand(
            command: Integer
        ): void;

        /**
         * This function must set the spindle orientation. The function may be invoked when expanding cycles.
         * @param angle The requested spindle orientation. 
         */
        declare function onOrientateSpindle(
            angle: Number
        ): void;

        /**
         * Entry function invoked at the end of each section. 
         */
        declare function onSectionEnd(): void;

        /**
         * Invoked after the last section. 
         */
        declare function onClose(): void;

        declare const outputUnit: Integer;

        declare const currentSection: Section;

        declare const highFeedMapping: Integer;

        declare const highFeedrate: Number;

        declare const lineNumber: Integer;

        declare const initialCyclePosition: Vector;

        declare const abortOnDeprecation: Boolean;

        declare const end: Vector;

        declare const length: Number;

        /* Deprecated since r45991. Use circularCenter instead. */
        declare const center: Vector;

        /* Deprecated since r45991. Use circularNormal instead. */
        declare const normal: Vector;

        /* Deprecated since r45991. Use circularPlane instead. */
        declare const plane: Integer;

        /* Deprecated since r45991. Use circularRadius instead. */
        declare const radius: Number;

        /* Deprecated since r45991. Use circularSweep instead. */
        declare const sweep: Number;

        /* Deprecated since r45991. Use circularClockwise instead. */
        declare const clockwise: Boolean;

        /* Deprecated since r45991. Use circularChordLength instead. */
        declare const chordLength: Number;

        /* Deprecated since r45991. Use circularFullCircle instead. */
        declare const fullCircle: Boolean;

        /* Deprecated since r45991. Use circularHelical instead. */
        declare const helical: Boolean;

        /* Deprecated since r45991. Use circularHelicalOffset instead. */
        declare const helicalOffset: Vector;

        /* Deprecated since r45991. Use circularHelicalDistance instead. */
        declare const helicalDistance: Number;

        /* The center of the current circular motion. Since r45991. */
        declare const circularCenter: Vector;

        /* The circular plane normal of the current circular motion. Since r45991. */
        declare const circularNormal: Vector;

        declare const circularOffset: Vector;

        declare const circularPlane: Integer;

        /* The radius of the current circular motion. Since r45991. */
        declare const circularRadius: Number;

        /* The starting radius of the current circular motion. Since r45991. */
        declare const circularStarttRadius: Number;

        /* The sweep of the current circular motion. Since r45991. */
        declare const circularSweep: Number;

        /* Specifies that the current cicular motion is clockwise. Since r45991. */
        declare const circularClockwise: Boolean;

        declare const circularChordLength: Number;

        /* The arc length of the current circular motion. Since r45991. */
        declare const circularArcLength: Number;

        declare const circularFullCircle: Boolean;

        /* Specifies that the currect circular motion is helical. Since r45991. */
        declare const circularHelical: Boolean;

        /* Specifies that the currect circular motion is a spiral. Since r45991. */
        declare const circularSpiral: Boolean;

        /* The helical offset for the current circular motion. Since r45991. */
        declare const circularHelicalOffset: Vector;

        declare const circularHelicalDistance: Number;

        declare const movement: Integer;

        declare const radiusCompensation: Integer;

        declare const description: String;

        declare const vendor: String;

        declare const vendorUrl: String;

        declare const legal: String;

        declare const unit: Integer;

        declare const programName: String;

        declare const programNameIsInteger: Boolean;

        declare const debugMode: Boolean;

        declare const preventPost: Boolean;

        declare const filename: String;

        declare const extension: String;

        declare const version: String;

        declare const certificationLevel: Integer;

        declare const revision: Integer;

        declare const minimumRevision: Integer;

        declare const deprecated: Boolean;

        declare const capabilities: Integer;

        declare const tolerance: Number;

        declare const mapWorkOrigin: Boolean;

        declare const mapToWCS: Boolean;

        declare const allowMachineChangeOnSection: Boolean;

        declare const minimumChordLength: Number;

        declare const minimumCircularRadius: Number;

        declare const maximumCircularRadius: Number;

        declare const minimumCircularSweep: Number;

        declare const maximumCircularSweep: Number;

        declare const maxRotaryDeltaAngle: Number;

        declare const circularInputTolerance: Number;

        declare const circularMergeTolerance: Number;

        declare const allowHelicalMoves: Boolean;

        declare const allowSpiralMoves: Boolean;

        declare const allowedCircularPlanes: Integer;

        declare const circularOutputAccuracy: Integer;

        declare const linearOutputAccuracy: Integer;

        declare const rotaryOutputAccuracy: Integer;

        declare const bufferRotaryMoves: Boolean;

        declare const controlSupportsTWP: Boolean;

        declare const eulerConvention: Integer;

        declare const workPlaneCalculationMethod: Integer;

        declare const allowFeedPerRevolutionDrilling: Integer;

        declare const machineParameters: MachineParameters;

        declare const properties: PropertySet;

        /* NUL ASCII control code. */
        declare const NUL: String;

        /* SOH ASCII control code. */
        declare const SOH: String;

        /* STX ASCII control code. */
        declare const STX: String;

        /* ETX ASCII control code. */
        declare const ETX: String;

        /* EOT ASCII control code. */
        declare const EOT: String;

        /* ENQ ASCII control code. */
        declare const ENQ: String;

        /* ACK ASCII control code. */
        declare const ACK: String;

        /* BEL ASCII control code. */
        declare const BEL: String;

        /* BS ASCII control code. */
        declare const BS: String;

        /* TAB ASCII control code. */
        declare const TAB: String;

        /* LF ASCII control code. */
        declare const LF: String;

        /* VT ASCII control code. */
        declare const VT: String;

        /* FF ASCII control code. */
        declare const FF: String;

        /* CR ASCII control code. */
        declare const CR: String;

        /* SO ASCII control code. */
        declare const SO: String;

        /* SI ASCII control code. */
        declare const SI: String;

        /* DLE ASCII control code. */
        declare const DLE: String;

        /* DC1 ASCII control code. */
        declare const DC1: String;

        /* DC2 ASCII control code. */
        declare const DC2: String;

        /* DC3 ASCII control code. */
        declare const DC3: String;

        /* DC4 ASCII control code. */
        declare const DC4: String;

        /* NAK ASCII control code. */
        declare const NAK: String;

        /* SYN ASCII control code. */
        declare const SYN: String;

        /* ETB ASCII control code. */
        declare const ETB: String;

        /* CAN ASCII control code. */
        declare const CAN: String;

        /* EM ASCII control code. */
        declare const EM: String;

        /* SUB ASCII control code. */
        declare const SUB: String;

        /* ESC ASCII control code. */
        declare const ESC: String;

        /* FS ASCII control code. */
        declare const FS: String;

        /* GS ASCII control code. */
        declare const GS: String;

        /* RS ASCII control code. */
        declare const RS: String;

        /* US ASCII control code. */
        declare const US: String;

        /* The default end-of-line marker. */
        declare const EOL: String;

        /* Space string. */
        declare const SP: String;

        /* File path separator. */
        declare const PATH_SEPARATOR: String;

        /* Inch unit. */
        declare const IN: Integer;

        /* Millimeters unit. */
        declare const MM: Integer;

        /* Circular XY plane. */
        declare const PLANE_XY: Integer;

        /* Circular XZ plane. Deprecated use PLANE_ZX instead. */
        declare const PLANE_XZ: Integer;

        /* Circular ZX plane. */
        declare const PLANE_ZX: Integer;

        /* Circular YZ plane. */
        declare const PLANE_YZ: Integer;

        /* X coordinate index. */
        declare const X: Integer;

        /* Y coordinate index. */
        declare const Y: Integer;

        /* Z coordinate index. */
        declare const Z: Integer;

        /* A rotary index. */
        declare const A: Integer;

        /* B rotary index. */
        declare const B: Integer;

        /* C rotary index. */
        declare const C: Integer;

        /* All rotaries index. */
        declare const ABC: Integer;

        /* YZ-plane. */
        declare const TOOL_AXIS_X: Integer;

        /* ZX-plane. */
        declare const TOOL_AXIS_Y: Integer;

        /* XY-plane. */
        declare const TOOL_AXIS_Z: Integer;

        /* Center radius compensation. */
        declare const RADIUS_COMPENSATION_OFF: Integer;

        /* Left radius compensation. */
        declare const RADIUS_COMPENSATION_LEFT: Integer;

        /* Right radius compensation. */
        declare const RADIUS_COMPENSATION_RIGHT: Integer;

        /* Don't linearize moves around multi-axis singularities.  More... */
        declare const SINGULARITY_LINEARIZE_OFF: Integer;

        declare const SINGULARITY_LINEARIZE_LINEAR: Integer;

        /* Keep rotary axes in line during multi-axis singularity linearization.  More... */
        declare const SINGULARITY_LINEARIZE_ROTARY: Integer;

        /* Coolant disabled. */
        declare const COOLANT_DISABLED: Integer;

        /* Flood coolant mode. */
        declare const COOLANT_FLOOD: Integer;

        /* Mist coolant mode. */
        declare const COOLANT_MIST: Integer;

        /* Coolant through tool mode. Deprecated use COOLANT_THROUGH_TOOL instead. */
        declare const COOLANT_TOOL: Integer;

        /* Coolant through tool mode. */
        declare const COOLANT_THROUGH_TOOL: Integer;

        /* Air mode. */
        declare const COOLANT_AIR: Integer;

        /* Air through tool mode. */
        declare const COOLANT_AIR_THROUGH_TOOL: Integer;

        /* Suction mode. */
        declare const COOLANT_SUCTION: Integer;

        /* Flood and mist coolant mode. */
        declare const COOLANT_FLOOD_MIST: Integer;

        /* Flood and through tool coolant mode. */
        declare const COOLANT_FLOOD_THROUGH_TOOL: Integer;

        /* Unspecified material. */
        declare const MATERIAL_UNSPECIFIED: Integer;

        /* High-speed steel material. */
        declare const MATERIAL_HSS: Integer;

        /* TI coated material. */
        declare const MATERIAL_TI_COATED: Integer;

        /* Carbide material. */
        declare const MATERIAL_CARBIDE: Integer;

        /* Ceramics material. */
        declare const MATERIAL_CERAMICS: Integer;

        /* Unspecified tool. */
        declare const TOOL_UNSPECIFIED: Integer;

        /* Drill. */
        declare const TOOL_DRILL: Integer;

        /* Center drill. */
        declare const TOOL_DRILL_CENTER: Integer;

        /* Spot drill. */
        declare const TOOL_DRILL_SPOT: Integer;

        /* Block drill. */
        declare const TOOL_DRILL_BLOCK: Integer;

        /* Flat end-mill. */
        declare const TOOL_MILLING_END_FLAT: Integer;

        /* Ball end-mill. */
        declare const TOOL_MILLING_END_BALL: Integer;

        /* Bullnose mill. */
        declare const TOOL_MILLING_END_BULLNOSE: Integer;

        /* Chamfer mill. */
        declare const TOOL_MILLING_CHAMFER: Integer;

        /* Face mill. */
        declare const TOOL_MILLING_FACE: Integer;

        /* Slot mill. */
        declare const TOOL_MILLING_SLOT: Integer;

        /* Radius mill. */
        declare const TOOL_MILLING_RADIUS: Integer;

        /* Dovetail mill. */
        declare const TOOL_MILLING_DOVETAIL: Integer;

        /* Tapered mill. */
        declare const TOOL_MILLING_TAPERED: Integer;

        /* Lollipop mill. */
        declare const TOOL_MILLING_LOLLIPOP: Integer;

        /* Right tap tool. */
        declare const TOOL_TAP_RIGHT_HAND: Integer;

        /* Left tap tool. */
        declare const TOOL_TAP_LEFT_HAND: Integer;

        /* Reamer tool. */
        declare const TOOL_REAMER: Integer;

        /* Boring bar tool. */
        declare const TOOL_BORING_BAR: Integer;

        /* Counterbore tool. */
        declare const TOOL_COUNTER_BORE: Integer;

        /* Countersink tool. */
        declare const TOOL_COUNTER_SINK: Integer;

        /* Holder. */
        declare const TOOL_HOLDER_ONLY: Integer;

        /* General turning tool. */
        declare const TOOL_TURNING_GENERAL: Integer;

        /* Thread turning tool. */
        declare const TOOL_TURNING_THREADING: Integer;

        /* Groove turning tool. */
        declare const TOOL_TURNING_GROOVING: Integer;

        /* Boring turning tool. */
        declare const TOOL_TURNING_BORING: Integer;

        /* Custom turning tool. */
        declare const TOOL_TURNING_CUSTOM: Integer;

        /* Probe. */
        declare const TOOL_PROBE: Integer;

        /* Wire. */
        declare const TOOL_WIRE: Integer;

        /* Water jet. */
        declare const TOOL_WATER_JET: Integer;

        /* Laser cutter. */
        declare const TOOL_LASER_CUTTER: Integer;

        /* Welder. */
        declare const TOOL_WELDER: Integer;

        /* Grinder. */
        declare const TOOL_GRINDER: Integer;

        /* Form mill. */
        declare const TOOL_MILLING_FORM: Integer;

        /* Plasma cutter. */
        declare const TOOL_PLASMA_CUTTER: Integer;

        /* Marker tool. */
        declare const TOOL_MARKER: Integer;

        /* Thread mill. */
        declare const TOOL_MILLING_THREAD: Integer;

        /* Turning tool compensation. */
        declare const TOOL_COMPENSATION_INSERT_CENTER: Integer;

        /* Turning tool compensation. */
        declare const TOOL_COMPENSATION_TIP: Integer;

        /* Turning tool compensation. */
        declare const TOOL_COMPENSATION_TIP_CENTER: Integer;

        /* Turning tool compensation. */
        declare const TOOL_COMPENSATION_TIP_TANGENT: Integer;

        /* Has parameter flag. */
        declare const HAS_PARAMETER: Integer;

        /* Has rapid flag. */
        declare const HAS_RAPID: Integer;

        /* Has linear flag. */
        declare const HAS_LINEAR: Integer;

        /* Has dwell flag. */
        declare const HAS_DWELL: Integer;

        /* Has circular flag. */
        declare const HAS_CIRCULAR: Integer;

        /* Has cycle flag. */
        declare const HAS_CYCLE: Integer;

        /* Has well-known command flag. */
        declare const HAS_WELL_KNOWN_COMMAND: Integer;

        /* Has comment flag. */
        declare const HAS_COMMENT: Integer;

        /* Invalid record type. */
        declare const RECORD_INVALID: Integer;

        /* Well-known command (onCommand). */
        declare const RECORD_WELL_KNOWN_COMMAND: Integer;

        /* Machine configuration change (onMachine). */
        declare const RECORD_MACHINE_COMMAND: Integer;

        /* Spindle speed (onSpindlSpeed) */
        declare const RECORD_SPINDLE_SPEED: Integer;

        /* Parameter (onParameter). */
        declare const RECORD_PARAMETER: Integer;

        /* Linear motion (onLinear, onRapid, onCyclePoint). */
        declare const RECORD_LINEAR: Integer;

        /* Linear 5-axis motion (onLinear5D, onRapid5D). */
        declare const RECORD_LINEAR_5D: Integer;

        /* Linear 5-axis motion (onLinear5D, onRapid5D). */
        declare const RECORD_LINEAR_ZXN: Integer;

        /* Linear motion with extrude (onLinearExtrude). */
        declare const RECORD_LINEAR_EXTRUDE: Integer;

        /* Circular motion (onCircular). */
        declare const RECORD_CIRCULAR: Integer;

        /* Dwell (onDwell). */
        declare const RECORD_DWELL: Integer;

        /* Cycle (onCycle). */
        declare const RECORD_CYCLE: Integer;

        /* End of cycle (onCycleEnd). */
        declare const RECORD_CYCLE_OFF: Integer;

        /* Comment (onComment, onManualNC). */
        declare const RECORD_COMMENT: Integer;

        /* Comment (onComment, onManualNC). */
        declare const RECORD_WIDE_COMMENT: Integer;

        /* Pass through (onPassThrough, onManualNC) */
        declare const RECORD_PASS_THROUGH: Integer;

        /* Pass through (onPassThrough, onManualNC) */
        declare const RECORD_WIDE_PASS_THROUGH: Integer;

        /* Start of operation (onSection, onSectionSpecialCycle). */
        declare const RECORD_OPERATION: Integer;

        /* End of operation (onSectionEnd, onSectionEndSpecialCycle). */
        declare const RECORD_OPERATION_END: Integer;

        /* Circular motion with extrude (onCircularExtrude). */
        declare const RECORD_CIRCULAR_EXTRUDE: Integer;

        /* Invalid (well-known command). */
        declare const COMMAND_INVALID: Integer;

        /* Program stop (well-known command M00). */
        declare const COMMAND_STOP: Integer;

        /* Optional program stop (well-known command M01). */
        declare const COMMAND_OPTIONAL_STOP: Integer;

        /* Program end (well-known command M02). */
        declare const COMMAND_END: Integer;

        /* Clockwise spindle direction (well-known command M03). */
        declare const COMMAND_SPINDLE_CLOCKWISE: Integer;

        /* Counterclockwise spidle direction (well-known command M04). */
        declare const COMMAND_SPINDLE_COUNTERCLOCKWISE: Integer;

        declare const COMMAND_START_SPINDLE: Integer;

        /* Spindle stop (well-known command M05). */
        declare const COMMAND_STOP_SPINDLE: Integer;

        declare const COMMAND_ORIENTATE_SPINDLE: Integer;

        /* Tool change (M06). */
        declare const COMMAND_LOAD_TOOL: Integer;

        /* Coolant on (M08). */
        declare const COMMAND_COOLANT_ON: Integer;

        /* Coolant off (M09). */
        declare const COMMAND_COOLANT_OFF: Integer;

        /* Activate speed-feed synchronization (well-known command). */
        declare const COMMAND_ACTIVATE_SPEED_FEED_SYNCHRONIZATION: Integer;

        /* Deactivate speed-feed synchronization (well-known command). */
        declare const COMMAND_DEACTIVATE_SPEED_FEED_SYNCHRONIZATION: Integer;

        /* Locks the 4th and 5th axes. This command is optional. */
        declare const COMMAND_LOCK_MULTI_AXIS: Integer;

        /* Unlocks the 4th and 5th axes. This command is optional. */
        declare const COMMAND_UNLOCK_MULTI_AXIS: Integer;

        /* Exact stop. This command is optional. */
        declare const COMMAND_EXACT_STOP: Integer;

        /* Close chip transport. */
        declare const COMMAND_START_CHIP_TRANSPORT: Integer;

        /* Stop chip transport. */
        declare const COMMAND_STOP_CHIP_TRANSPORT: Integer;

        /* Open primary door. */
        declare const COMMAND_OPEN_DOOR: Integer;

        /* Close primary door. */
        declare const COMMAND_CLOSE_DOOR: Integer;

        /* Break control. */
        declare const COMMAND_BREAK_CONTROL: Integer;

        /* Measure tool. */
        declare const COMMAND_TOOL_MEASURE: Integer;

        /* Run calibration cycle. */
        declare const COMMAND_CALIBRATE: Integer;

        /* Verify part/tool/machine integrity. */
        declare const COMMAND_VERIFY: Integer;

        /* Run cleaning cycle. */
        declare const COMMAND_CLEAN: Integer;

        /* Alarm. */
        declare const COMMAND_ALARM: Integer;

        /* Alert. */
        declare const COMMAND_ALERT: Integer;

        /* Change pallet. */
        declare const COMMAND_CHANGE_PALLET: Integer;

        /* Power on. */
        declare const COMMAND_POWER_ON: Integer;

        /* Power off. */
        declare const COMMAND_POWER_OFF: Integer;

        /* Open main chuck.  More... */
        declare const COMMAND_MAIN_CHUCK_OPEN: Integer;

        /* Close main chuck.  More... */
        declare const COMMAND_MAIN_CHUCK_CLOSE: Integer;

        /* Open secondary chuck.  More... */
        declare const COMMAND_SECONDARY_CHUCK_OPEN: Integer;

        /* Close secondary chuck.  More... */
        declare const COMMAND_SECONDARY_CHUCK_CLOSE: Integer;

        /* Activate spindle synchronization.  More... */
        declare const COMMAND_SECONDARY_SPINDLE_SYNCHRONIZATION_ACTIVATE: Integer;

        /* Deactivate spindle synchronization.  More... */
        declare const COMMAND_SECONDARY_SPINDLE_SYNCHRONIZATION_DEACTIVATE: Integer;

        /* Sync channels. */
        declare const COMMAND_SYNC_CHANNELS: Integer;

        /* Probe on. */
        declare const COMMAND_PROBE_ON: Integer;

        /* Probe off. */
        declare const COMMAND_PROBE_OFF: Integer;

        /* Rapid movement type. */
        declare const MOVEMENT_RAPID: Integer;

        /* Lead-in movement type. */
        declare const MOVEMENT_LEAD_IN: Integer;

        /* Cutting movement type. */
        declare const MOVEMENT_CUTTING: Integer;

        /* Lead-out movement type. */
        declare const MOVEMENT_LEAD_OUT: Integer;

        /* Transition linking movement type. */
        declare const MOVEMENT_LINK_TRANSITION: Integer;

        /* Additive bridging movement type. */
        declare const MOVEMENT_BRIDGING: Integer;

        /* Direction linking movement type. */
        declare const MOVEMENT_LINK_DIRECT: Integer;

        /* Helical ramp movement type. */
        declare const MOVEMENT_RAMP_HELIX: Integer;

        /* Helical jet pierce movement type. */
        declare const MOVEMENT_PIERCE_CIRCULAR: Integer;

        /* Profile ramp movement type. */
        declare const MOVEMENT_RAMP_PROFILE: Integer;

        /* Profile jet pierce movement type. */
        declare const MOVEMENT_PIERCE_PROFILE: Integer;

        /* Zig-zag ramp movement type. */
        declare const MOVEMENT_RAMP_ZIG_ZAG: Integer;

        /* Linear jet pierce movement type. */
        declare const MOVEMENT_PIERCE_LINEAR: Integer;

        /* Ramp movement type. */
        declare const MOVEMENT_RAMP: Integer;

        /* Plunge movement type. */
        declare const MOVEMENT_PLUNGE: Integer;

        /* Plunge jet pierce movement type. */
        declare const MOVEMENT_PIERCE: Integer;

        /* Predrill movement type. */
        declare const MOVEMENT_PREDRILL: Integer;

        /* Extended movement type. */
        declare const MOVEMENT_EXTENDED: Integer;

        /* Reduced cutting feed movement type. */
        declare const MOVEMENT_REDUCED: Integer;

        /* Finish cutting movement type. */
        declare const MOVEMENT_FINISH_CUTTING: Integer;

        /* High feed movement type. */
        declare const MOVEMENT_HIGH_FEED: Integer;

        /* Depositing movement type. */
        declare const MOVEMENT_DEPOSITING: Integer;

        /* Connection between toolpaths. */
        declare const MOVEMENT_CONNECTION: Integer;

        /* The drill has broken through material. */
        declare const MOVEMENT_DRILL_BREAKTHROUGH: Integer;

        /* Positioning the drill in the pilot for gun drilling. */
        declare const MOVEMENT_GUN_DRILL_POSITIONING: Integer;

        /* Do not map rapid travesal to high feed. */
        declare const HIGH_FEED_NO_MAPPING: Integer;

        /* Map rapid travesal along more than one axis to high feed. */
        declare const HIGH_FEED_MAP_MULTI: Integer;

        declare const HIGH_FEED_MAP_XY_Z: Integer;

        /* Map all rapid travesals to high feed. */
        declare const HIGH_FEED_MAP_ANY: Integer;

        declare const tool: Tool;

        declare const spindleAxis: Integer;

        declare const feedrate: Number;

        declare const activeFeedMode: Integer;

        declare const spindleSpeed: Number;

        declare const machineConfiguration: MachineConfiguration;

        declare const cycleType: String;

        declare const cycle: Map<any, any>;

        declare const cycleExpanded: Boolean;

        declare const simulation: Simulation;


    /**
     * A 2D range specified by an minimum and maximum value. 
     */
    declare class Range {
        /**
         * Constructs the range as uninitialized. 
         */
        Range(): Range;

        /**
         * Constructs the range from the specified values.
         * @param a Outer value for the range. 
         * @param b Outer value for the range. 
         * @param actuator The actuator type (ie. either "linear" or "rotational"). The default is "rotational". 
         */
        Range(
            a: Number,
        ): Range;

        /**
         * Returns true if the range is a non-range. 
         */
        isNonRange(): Boolean;

        /**
         * Returns the minimum value. 
         */
        getMinimum(): Number;

        /**
         * Returns the maximum value. 
         */
        getMaximum(): Number;

        /**
         * Returns the span of the range. 
         */
        getSpan(): Number;

        /**
         * Returns the middle of the range. 
         */
        getMiddle(): Number;

        /**
         * Grows the range by the specified offset. 
         */
        grow(
            offset: Number
        ): void;

        /**
         * Reduces the range by the specified offset. Returns the middle of the range if the range collapses. 
         */
        reduce(
            offset: Number
        ): void;

        /**
         * Translates the range. 
         */
        translate(
            offset: Number
        ): void;

        /**
         * Expands the range to include the specified value. 
         */
        expandTo(
            value: Number
        ): void;

        /**
         * Expands the range to include the specified range. 
         */
        expandToRange(
            value: Range
        ): void;

        /**
         * Returns the U coordinate for the specified value. 0 and 1 corresponds to the minimum and maximum, respectively. 
         */
        getU(
            value: Number
        ): Number;

        /**
         * Returns true if the specified value is within the range. 
         */
        isWithin(
            value: Number
        ): Boolean;

        /**
         * Returns the nearest value which is in the range. 
         */
        clamp(
            value: Number
        ): Number;

        /**
         * Converts the range to a string (e.g. [-10.5; 5.75]). 
         */
        toString(): String;

        /* The minimum coordinate of the range. */
        minimum: Number;

        /* The maximum coordinate of the range. */
        maximum: Number;

        /* The span of the range. */
        span: Number;

        /* The middle of the range. */
        middle: Number;

    }

    /**
     * An NC record in the NC database. 
     */
    declare class Record {
        /**
         * Constructs an invalid record. 
         */
        Record(): Record;

        /**
         * Returns true if the record is valid. 
         */
        isValid(): Boolean;

        /**
         * Returns the id of the record. 
         */
        getId(): Integer;

        /**
         * Returns the type of the record. 
         */
        getType(): Integer;

        /**
         * Returns the categories. 
         */
        getCategories(): Integer;

        /**
         * Returns true if the record is a motion record. 
         */
        isMotion(): Boolean;

        /**
         * Returns true if the record is a cycle record. 
         */
        isCycle(): Boolean;

        /**
         * Returns the cycle type. 
         */
        getCycleType(): String;

        /**
         * Returns true if the record is a parameter.
         * @since r41192 
         */
        isParameter(): Boolean;

        /**
         * Returns the parameter name.
         * @since r41192 
         */
        getParameterName(): String;

        /**
         * Returns the parameter value.
         * @since r41192 
         */
        getParameterValue(): Value;

    }

    /**
     * A referenced formatting variable. This object is used to avoid output of values if not required when values are identical to a specific reference value.
     */
    declare class ReferenceVariable {
        /**
         * Constructs a variable.
         * The supported specifiers are:
         * @param specifiers The behavior specifiers. 
         * @param format The instance of type Format used to format the output values.
         */
        ReferenceVariable(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): ReferenceVariable;

        /**
         * Returns the formatted string of the specified value. The empty string is returned if the value is identical to the specified reference value. The reference value is optional. If not specified the value will be output.
         * @param value The value. 
         * @param reference The reference value. 
         */
        format(
            value: Number,
        ): String;

        /**
         * Returns the prefix. 
         */
        getPrefix(): Value;

        /**
         * Sets the prefix.
         * @param prefix The prefix string. 
         */
        setPrefix(
            prefix: Value
        ): void;

        /**
         * Disable variable. When invoked {@link format()} always returns the empty string. 
         */
        disable(): void;

    }

    /**
     * An NC section. A section is a group of NC data which shares the same work plane, tool, and related data. 
     */
    declare class Section {
        /**
         * Returns the zero-based id of the section. 
         */
        getId(): Integer;

        /**
         * Returns the operation property map. 
         */
        getOperationProperties(): PropertyMap;

        /**
         * Returns the strategy of the section. 
         */
        getStrategy(): String | undefined;

        /**
         * Returns the number of records in the section. 
         */
        getNumberOfRecords(): Integer;

        /**
         * Returns the specified record within the section.
         * @since r41192 
         */
        getRecord(
            id: Integer
        ): Record;

        /**
         * Returns the job id of the section. 
         */
        getJobId(): Integer;

        /**
         * Returns the pattern id of the section. You can use this to tell which sections are pattern instances of the same original section. The motion coordinates will be identical for patterned sections but the work plane can be different. Note that, the pattern ids can be different for some types of patterns when the actual motion coordinates are mapped.
         */
        getPatternId(): Integer;

        /**
         * Returns the number of pattern instances for the section. 
         */
        getNumberOfPatternInstances(): Integer;

        /**
         * Returns true if the section is patterned. Ie. at least one other section shares the same pattern id. 
         */
        isPatterned(): Boolean;

        /**
         * Returns the channel ID. Sections with the same channel ID belongs to the same channel. 
         */
        getChannel(): Integer;

        /**
         * Returns true if tool change should be forced. 
         */
        getForceToolChange(): Boolean;

        /**
         * Returns true if the section is optional. 
         */
        isOptional(): Boolean;

        /**
         * Returns the first active compensation for the section. 
         */
        getFirstCompensationOffset(): Integer;

        /**
         * Returns the tool. 
         */
        getTool(): Tool;

        /**
         * Returns the content flags. 
         */
        getContent(): Integer;

        /**
         * Returns true is the section contains multi-axis toolpath. 
         */
        isMultiAxis(): Boolean;

        /**
         * Returns the original unit of the section. This may be different from the output unit.
         * The available values are:
         * - IN - Inches
         * - MM - Millimeters 
         */
        getUnit(): Integer;

        /**
         * Returns the type of the section.
         * The available types are:
         * - TYPE_MILLING
         * - TYPE_TURNING
         * - TYPE_WIRE
         * - TYPE_JET 
         */
        getType(): Integer;

        /**
         * Returns machining type of the section
         * The available types are:
         * - MACHINING_TYPE_UNDEFINED
         * - MACHINING_TYPE_3AXIS
         * - MACHINING_TYPE_POLAR
         * - MACHINING_TYPE_4AXIS
         * - MACHINING_TYPE_5AXIS 
         */
        getMachiningType(): Integer;

        /**
         * Returns user-specified direction for polar machining type 
         */
        getPolarDirection(): Vector;

        /**
         * Returns true, if rapid polar links should be used, false - for linear rapid links, undefined - if not specified (Fusion assumes they are linear) 
         */
        isPolarRapidLinks(): Boolean | undefined;

        /**
         * Returns polar mode type
         * The available types are:
         * - POLAR_MODE_OFF
         * - POLAR_MODE_COMPUTER
         * - POLAR_MODE_CONTROL
         * - POLAR_MODE_AUTO (only if automatic polar handling is not activated) 
         */
        getPolarMode(): Integer;

        /**
         * Returns the associated quality. Used for waterjet, laser, and plasma cutting. 
         */
        getQuality(): Integer;

        /**
         * Returns the type of waterjet, laser, and plasma cutting of the section.
         * The available modes are:
         * - JET_MODE_THROUGH
         * - JET_MODE_ETCHING
         * - JET_MODE_VAPORIZE 
         */
        getJetMode(): Integer;

        /**
         * Returns true if tailstock is active for turning. 
         */
        getTailstock(): Boolean;

        /**
         * Returns true if part catcher should be activated if available for turning. 
         */
        getPartCatcher(): Boolean;

        /**
         * Returns the active spindle.
         * The available values are:
         * - SPINDLE_PRIMARY - The main/primary spindle.
         * - SPINDLE_SECONDARY - The sub-spindle/secondary spindle. 
         */
        getSpindle(): Integer;

        /**
         * Returns the feed mode. For multi-axis operations it will return the feedrate mode as defined in the machine configuration for multi-axis feedrates if TCP is not enabled. If TCP is enabled, then FEED_PER_MINUTE will be returned for multi-axis operations.
         * The available modes are:
         * - FEED_PER_MINUTE
         * - FEED_PER_REVOLUTION
         * - FEED_INVERSE_TIME
         * - FEED_DEGREES_MINUTE 
         */
        getFeedMode(): Integer;

        /**
         * Returns the turning tool orientation (radians). 
         */
        getToolOrientation(): Number;

        /**
         * Returns the work origin in the WCS. 
         */
        getWorkOrigin(): Vector;

        /**
         * Returns the work plane in the WCS. 
         */
        getWorkPlane(): Matrix;

        /**
         * Returns true if the work plane is oriented such that forward is along the +X in the WCS.
         * The same as checking "currentSection.workPlane.forward.x &gt;= (1 - EPSILON)". 
         */
        isXOriented(): Boolean;

        /**
         * Returns true if the work plane is oriented such that forward is along the +Y in the WCS.
         * The same as checking "currentSection.workPlane.forward.y &gt;= (1 - EPSILON)". 
         */
        isYOriented(): Boolean;

        /**
         * Returns true if the work plane is oriented such that forward is along the +Z in the WCS.
         * The same as checking "currentSection.workPlane.forward.z &gt;= (1 - EPSILON)". 
         */
        isZOriented(): Boolean;

        /**
         * Returns true if the work plane is set to the top plane (i.e. looking down the Z-axis) in the WCS.
         * This method has been deprecated. See {@link isZOriented()} instead. 
         */
        isTopWorkPlane(): Boolean;

        /**
         * Returns the work origin in the global coordinate system (top view). 
         */
        getGlobalWorkOrigin(): Vector;

        /**
         * Returns the work plane in the global coordinate system (top view). 
         */
        getGlobalWorkPlane(): Matrix;

        /**
         * Returns the tool axis coordinate index (0:X, 1:Y, and 2:Z). 
         */
        getToolAxis(): Integer;

        /**
         * Returns the origin of the WCS. 
         */
        getWCSOrigin(): Vector;

        /**
         * Returns the WCS plane. 
         */
        getWCSPlane(): Matrix;

        /**
         * Returns the dynamic origin of the WCS. Only use if the dynamic work offset is set. 
         */
        getDynamicWCSOrigin(): Vector;

        /**
         * Returns the dynamic WCS plane. Only use if the dynamic work offset is set. 
         */
        getDynamicWCSPlane(): Matrix;

        /**
         * Returns the origin of the fixture coordinate system (FCS).
         * @since r40294 
         */
        getFCSOrigin(): Vector;

        /**
         * Returns the fixture coordinate system (FCS) plane.
         * @since r40294 
         */
        getFCSPlane(): Matrix;

        /**
         * Returns the origin of the Model coordinate system.
         * @since r42226 
         */
        getModelOrigin(): Vector;

        /**
         * Returns the Model coordinate system plane.
         * @since r42226 
         */
        getModelPlane(): Matrix;

        /**
         * Returns the part attach point in the world coordinate system. The return position is undefined if the machine configuration does not use a machine simulation model.
         * @since r45964 
         */
        getPartAttachPoint(): Vector;

        /**
         * Returns the work offset for the WCS. 
         */
        getWorkOffset(): Integer;

        /**
         * Returns the work offset for the WCS. 
         */
        getProbeWorkOffset(): Integer;

        /**
         * Returns the WCS code string. If there is no WCS definition defined or the work offset is out of range, it will return an empty string. 
         */
        getWCS(): String;

        /**
         * Returns the WCS code index (without format). If there is no WCS definition defined or the work offset is out of range, it will return -1. 
         */
        getWCSIndex(): Integer;

        /**
         * Returns true if the dynamic work offset is defined. 
         */
        hasDynamicWorkOffset(): Boolean;

        /**
         * Returns the dynamic work offset for the WCS. The dynamic work offset is used to control the display coordinates. 
         */
        getDynamicWorkOffset(): Integer;

        /**
         * Returns true if the section uses axis substitution. In axis substitution mode the coordinates are the following meaning:
         * X: The offset along the substitution axis. Y: The rotation angle in radians. Z: Radius (always positive) 
         */
        getAxisSubstitution(): Boolean;

        /**
         * Returns the nominal axis substitution radius. 
         */
        getAxisSubstitutionRadius(): Number;

        /**
         * Returns the global position for the specified local position. 
         */
        getGlobalPosition(
            p: Vector
        ): Vector;

        /**
         * Returns the WCS position for the specified local position. 
         */
        getWCSPosition(
            p: Vector
        ): Vector;

        /**
         * Returns the local position for the specified global position. 
         */
        getSectionPosition(
            p: Vector
        ): Vector;

        /**
         * Returns the maximum spindle speed for the section. 
         */
        getMaximumSpindleSpeed(): Number;

        /**
         * Returns the maximum feedrate for the section. 
         */
        getMaximumFeedrate(): Number;

        /**
         * Returns the cutting distance for the section (excluding rapid traversal). 
         */
        getCuttingDistance(): Number;

        /**
         * Returns the rapid traversal distance for the section. 
         */
        getRapidDistance(): Number;

        /**
         * Returns the movements (as a mask) in use for the section.
         */
        getMovements(): Integer;

        /**
         * Returns the cycle time for the cutting moves the section (rapid traversal is ignored). 
         */
        getCycleTime(): Number;

        /**
         * Returns the number of cycle points for the entire section. 
         */
        getNumberOfCyclePoints(): Integer;

        /**
         * Returns the Z-coordinate range of the toolpath of the section. 
         */
        getZRange(): Range;

        /**
         * Returns the Z-coordinate range of the toolpath of the section in the global coordinate system. 
         */
        getGlobalZRange(): Range;

        /**
         * Returns the range of the toolpath of the section along the global direction.
         * @since r39501 
         */
        getGlobalRange(
            direction: Vector
        ): Range;

        /**
         * Returns the bounding box of the toolpath of the section. 
         */
        getBoundingBox(): BoundingBox;

        /**
         * Returns the bounding box of the toolpath of the section in the global coordinate system. 
         */
        getGlobalBoundingBox(): BoundingBox;

        /**
         * Returns the bounding box of the toolpath of the section in the coordinate system optimized for the rotary axes.
         * @param machine The machine configuration. 
         * @param abc The rotary axis positions.
         * @since r45861 
         */
        getOptimizedBoundingBox(
            machine: MachineConfiguration,
        ): BoundingBox;

        /**
         * Returns true if the milling motion for the section is further away from X0 Y0 than the given distance. Can be used to check if motion gets close to the rotary axis for XZC milling.
         * @param distance The minimum allowed distance from motion to X0 Y0. 
         * @param tolerance Tolerance used to linearize toolpath.
         * @since r41324 
         */
        isCuttingMotionAwayFromRotary(
            distance: Number,
        ): Boolean;

        /**
         * Returns true if the section has a well-defined position. Sections with no position would cause {@link getInitialPosition()} and {@link getFinalPosition()} to fail.
         * @since r41192 
         */
        hasWellDefinedPosition(): Boolean;

        /**
         * Returns the first position for the section. This method has been replaced by {@link getInitialPosition()}.
         * @deprecated 
         */
        getFirstPosition(): Vector;

        /**
         * Returns the initial position for the section. 
         */
        getInitialPosition(): Vector;

        /**
         * Returns the final position for the section. 
         */
        getFinalPosition(): Vector;

        /**
         * Returns the initial tool axis for the section as the direction vector. 
         */
        getInitialToolAxis(): Vector;

        /**
         * Returns the initial tool axis for the section in the WCS frame. Always returns the direction vector. 
         */
        getGlobalInitialToolAxis(): Vector;

        /**
         * Returns the initial tool axis machine angles for the section. The section must have been optimized for the machine using optimizeMachineAngles() or similar before called this method. 
         */
        getInitialToolAxisABC(): Vector;

        /**
         * Returns the final tool axis for the section as the direction vector. 
         */
        getFinalToolAxis(): Vector;

        /**
         * Returns the final tool axis machine angles for the section. The section must have been optimized for the machine using optimizeMachineAngles() or similar before called this method. 
         */
        getFinalToolAxisABC(): Vector;

        /**
         * Returns the final tool axis for the section in the WCS frame. Always returns the direction vector. 
         */
        getGlobalFinalToolAxis(): Vector;

        /**
         * Returns true if the spindle speed is on initially. Might be turned off due to initial canned cycle when it starts with positioning moves.
         * @since r41215 
         */
        getInitialSpindleOn(): Boolean;

        /**
         * Returns the initial spindle speed which can be different from the tool spindle speed. Might be turned off due to initial canned cycle.
         * @since r41215 
         */
        getInitialSpindleSpeed(): Number;

        /**
         * Returns true if the spindle speed is on at the end of the section. Might be turned off due to final canned cycle.
         * @since r42100 
         */
        getFinalSpindleOn(): Boolean;

        /**
         * Returns the final spindle speed which can be different from the tool spindle speed. Might be turned off due to final canned cycle.
         * @since r42100 
         */
        getFinalSpindleSpeed(): Number;

        /**
         * Returns the maximum tilt in the range [-1; 1] for the given multi-axis section. 1 corresponds to 0deg tilt. -1 corresponds to 180deg tilt. 
         */
        getMaximumTilt(): Number;

        /**
         * Returns the upper machine angles for a multi-axis section. The section must have been optimized for the machine using optimizeMachineAngles() or similar before called this method. 
         */
        getLowerToolAxisABC(): Vector;

        /**
         * Returns the upper machine angles for a multi-axis section. The section must have been optimized for the machine using optimizeMachineAngles() or similar before called this method. 
         */
        getUpperToolAxisABC(): Vector;

        /**
         * Returns true if the section has been optimized for a machine configuration. 
         */
        isOptimizedForMachine(): Boolean;

        /**
         * Returns the TCP mode used for the section when it was optimized for a machine configuration. 
         */
        getOptimizedTCPMode(): Integer;

        /**
         * Returns true if the specified parameter has been defined in the section. 
         */
        hasParameter(
            name: String
        ): Boolean;

        /**
         * Returns the value of the first occurrence of the specified parameter.
         * @param name The name of the parameter to return. 
         * @param defaultValue (optional, since r45615) The default value of the parameter to return if it is not defined. 
         */
        getParameter(
            name: String,
        ): Value;

        /**
         * Returns true if the specified cycle occurs anywhere within the section.
         * @since r41184 
         */
        hasCycle(
            uri: String
        ): Boolean;

        /**
         * Returns true if the section has any cycle.
         * @since r41184 
         */
        hasAnyCycle(): Boolean;

        /**
         * Returns the number of times the given cycle occurs within the section.
         * @since r41215 
         */
        getNumberOfCyclesWithId(
            uri: String
        ): Integer;

        /**
         * Returns the number cycles within the section.
         * @since r41215 
         */
        getNumberOfCycles(): Integer;

        /**
         * Returns the ID of the given cycle within the section. Raises exception if index exceeds limit returned by {@link Section.getNumberOfCycles()}.
         * @since r41215 
         */
        getCycleId(
            index: Integer
        ): String;

        /**
         * Returns the first cycle within the section. Returns empty string is no cycle occurs within the section.
         * @since r41184 
         */
        getFirstCycle(): String;

        /**
         * Returns the last cycle within the section. Returns empty string is no cycle occurs within the section.
         * @since r42100 
         */
        getLastCycle(): String;

        /**
         * Returns true if the given cycle is the first effective move within the section. E.g. if onLinear() would be called before onCycle() this method will return false. onParameter() and onComment() are ignored.
         * @since r41184 
         */
        doesStartWithCycle(
            uri: String
        ): Boolean;

        /**
         * Returns true if the given cycle is the last effective move within the section. E.g. if onLinear() would be called after onCycle() this method will return false. onParameter() and onComment() are ignored.
         * @since r42100 
         */
        doesEndWithCycle(
            _uri: String
        ): Boolean;

        /**
         * Returns true if the given cycle is the first effective move within the section ignoring positioning moves (ie. rapid and high feed moves). E.g. if onLinear() would be called before onCycle() this method will return false. onParameter(), onComment() and onDwell() are ignored.
         * @since r41215 
         */
        doesStartWithCycleIgnoringPositioning(
            uri: String
        ): Boolean;

        /**
         * Returns true if the given cycle is the first effective move within the section ignoring positioning moves (ie. rapid and high feed moves). E.g. if onLinear() would be called before onCycle() this method will return false. onParameter(), onComment() and onDwell() are ignored.
         * @since r42100 
         */
        doesEndWithCycleIgnoringPositioning(
            uri: String
        ): Boolean;

        /**
         * Returns true if the given cycle is the only effective move within the section. onParameter() and onComment() are ignored.
         * @since r41184 
         */
        doesStrictCycle(
            uri: String
        ): Boolean;

        /**
         * Returns true if the specified parameter has been defined in the given cycle.
         * @param index The index of the cycle. 
         * @param name The name of the parameter.
         * @since r41215 
         */
        hasCycleParameter(
            index: Integer,
        ): Boolean;

        /**
         * Returns the value of the first occurrence of the specified parameter within the given cycle.
         * @param index The index of the cycle. 
         * @param name The name of the parameter.
         * @since r41215 
         */
        getCycleParameter(
            index: Integer,
        ): Value;

        /**
         * Optimizes the machine angles for 5-axis motion given the specified machine configuration.
         * The directions for onRapid5D() and onLinear5D() are hereafter mapped to machine angles from the initial direction vector. The work plane and origin are mapped into the WCS plane and origin.
         * @param machine The machine configuration. 
         * @param optimizeType Can be one of the following.
OPTIMIZE_NONE: Maintain tool tip position (TCPM).
OPTIMIZE_BOTH: Adjust for tables and heads
OPTIMIZE_TABLES: Adjust for tables only.
OPTIMIZE_HEADS: Adjust for heads only.
OPTIMIZE_AXIS: Adjust per axis definition. 


         */
        optimizeMachineAnglesByMachine(
            machine: MachineConfiguration,
        ): void;

        /**
         * Adjusts the tool end points for the rotary axes positions in 3+2 operations for the active machine configuration.
         * The directions for onRapid(), onLinear(), onCyclePoint(), and onCircular() are hereafter mapped according to the input rotary axis angles.
         * @param machine The machine configuration. 
         * @param abc The rotary axis positions.
         * @param optimizeType Can be one of the following.
OPTIMIZE_NONE: Maintain tool tip position (TCPM).
OPTIMIZE_BOTH: Adjust for tables and heads
OPTIMIZE_TABLES: Adjust for tables only.
OPTIMIZE_HEADS: Adjust for heads only.
OPTIMIZE_AXIS: Adjust per axis definition. 


         */
        optimize3DPositionsByMachine(
            machine: MachineConfiguration,
        ): void;

        /**
         * Returns the preferred ABC angles for the specified orientation such that the forward and optionally right directions are maintained. The preferences are defined in the 'controllingAxis', 'control', and 'options' parameters.
         * @param machine The machine configuration. 
         * @param orientation The desired orientation (work plane). 
         * @param current The current machine angles. 
         * @param controllingAxis The axis used to determine the preferred solution. It can be A, B, C for a single axis or ABC to consider all axes. 
         * @param type The preference type.
PREFER_PREFERENCE - the preference specified in the createAxis definition.
PREFER_CLOSEST- the closest solution.
PREFER_POSITIVE - a positive angle. Not valid with ABC.
PREFER_NEGATIVE - a negative angle. Not valid with ABC.
PREFER_CW - axis moves in a clockwise direction. Not valid with ABC.
PREFER_CCW - axis moves in a counter-clockwise direction. Not valid with ABC. 


         * @param options Options used to control the ABC selection.
ENABLE_NONE - disables all options.
ENABLE_RESET - respects the 'reset' parameter in the createAxis definition.
ENABLE_WCS - solves for a rotary axis perpendicular to the spindle vector as defined by the tool orientation of the operation.
ENABLE_LIMITS - solves for a rotary table perpendicular to the spindle vector in order to keep the linear axes within the limits of the machine.
ENABLE_ALL - enables all options (ENABLE_RESET, ENABLE_WCS, and ENABLE_LIMITS).


         * @since r45860 
         */
        getABCByPreference(
            machine: MachineConfiguration,
        ): Vector;

        /**
         * Returns 'true' if the section's toolpath fits within the linear axis limits of the machine.
         * @param machine The machine configuration. 
         * @param current The current machine angles.
         * @since r45860 
         */
        doesToolpathFitWithinLimits(
            machine: MachineConfiguration,
        ): Boolean;

        /**
         * Returns true if the toolpath belongs to one or several specific strategy groups. 
         * The function can take more than one flag as argurments. e.g.
         * @param groups The group flags to be checked. The available group flags are:
STRATEGY_2D
STRATEGY_3D
STRATEGY_ROUGHING
STRATEGY_FINISHING
STRATEGY_MILLING
STRATEGY_TURNING
STRATEGY_JET
STRATEGY_ADDITIVE
STRATEGY_PROBING
STRATEGY_INSPECTION
STRATEGY_DRILLING
STRATEGY_HOLEMILLING
STRATEGY_THREAD
STRATEGY_SAMPLING
STRATEGY_ROTARY
STRATEGY_SECONDARYSPINDLE
STRATEGY_SURFACE
STRATEGY_CHECKSURFACE
STRATEGY_MULTIAXIS


         * @since r45615 
         */
        checkGroup(
            groups: Integer
        ): Boolean;

        /** Returns true if the section is a connection move section. */
        isConnectionSection(): Boolean;

        /**
         * Returns true if the section requires a rewind. 
         * @param includeStayDownAtApex If true, stay-down-at-apex rewinds are also counted. 
         */
        hasRewind(
            includeStayDownAtApex: Boolean
        ): Boolean;

        /**
         * Retuns the mode for using rotations to fit toolpath within limits. Possible return values:
         * - USE_ROTATIONS_NONE
         * - USE_ROTATIONS_INDEXING_ONLY
         * - USE_ROTATIONS_POLAR 
         */
        getUseRotationsToFitLimits(): Integer;

        /* Specifies the original unit of the section (this may be different from the output unit). */
        unit: Integer;

        /* The work origin in the WCS. */
        workOrigin: Vector;

        /* The work plane in the WCS. */
        workPlane: Matrix;

        /* The work coordinate system (WCS) origin. */
        wcsOrigin: Vector;

        /* The work coordinate system (WCS) plane. */
        wcsPlane: Matrix;

        /* The work offset corresponding to the WCS. */
        workOffset: Integer;

        /* The work offset corresponding to the Probe WCS. */
        probeWorkOffset: Integer;

        /* The index used in the WCS. */
        wcsIndex: Integer;

        /* The WCS. */
        wcs: String;

        dynamicWorkOffset: Integer;

        /* Specifies that the section uses axis substitution. */
        axisSubstitution: Boolean;

        /* Specifies the nominal axis substitution radius. */
        axisSubstitutionRadius: Number;

        /* Specifies the type of the section (TYPE_MILLING, TYPE_TURNING, or TYPE_JET). */
        type: Integer;

        /* Specifies the associated quality. */
        quality: Integer;

        /* Specifies that tailstock is used. */
        tailstock: Boolean;

        /* Specifies that part catcher should be activated if available. */
        partCatcher: Boolean;

        /* Specifies the active spindle. */
        spindle: Integer;

        /* The operation properties. */
        properties: Map<any, any>;

        /* Specifies the strategy type of the section. */
        strategy: String;

        /* Specifies machining type for section (3-Axis, 5-Axis, Polar and so on). */
        machiningType: Integer;

        /* User-specified polar direction for Polar machining type. */
        polarDirection: Vector;

        /* True if polar rapid links requested by user. */
        polarRapidLinks: Boolean;

        /* Returns type of polar mode requested: in computer or in control. */
        polarMode: Integer;

    }

    /**
     * {@link Shaft} of tool. 
     */
    declare class Shaft {
        /**
         * Returns true if the shaft has any sections. 
         */
        hasSections(): Boolean;

        /**
         * Returns the number of sections. 
         */
        getNumberOfSections(): Integer;

        /**
         * Returns the maximum diameter of the shaft. 
         */
        getMaximumDiameter(): Number;

        /**
         * Returns the length of the shaft. 
         */
        getTotalLength(): Number;

        /**
         * Returns the diameter of the specified section. The section is a value in the range [0; {@link getNumberOfSections()}]. 
         */
        getDiameter(
            index: Integer
        ): Number;

        /**
         * Returns the length of the specified section. The section is a value in the range [0; {@link getNumberOfSections()}]. 
         */
        getLength(
            index: Integer
        ): Number;

        /* The maximum diameter. */
        maximumDiameter: Number;

        /* from tip to tool holder.  More... */
        length: Number;

    }

    /**
     * Provides a post API class for sending commands directly to the simulation. 
     */
    declare class Simulation {
        /**
         * Set the target from an axis ID. If the id is ABC, the value will be in radians. 
         */
        setTargetForId(): void;

        /**
         * Set the target from the rotary axis coordinate. coordinate 0=A, 1=B, 2=C. Values are in radians. 
         */
        setTargetForCoordinate(
            coordinate: Integer,
        ): void;

        /**
         * Set the target from an axis. 
         */
        setTarget(
            axis: Axis,
        ): void;

        /**
         * Reset the target coordinates ie. clears the current target(s). 
         */
        resetTarget(): void;

        /**
         * Do a move to the target in machine coordinates.
         * Calling this will also reset the target automatically, so you must build up a new target. 
         */
        moveToTargetInMachineCoords(): void;

        /**
         * Do a move to the target in work coordinates (ie. the post coordinates).
         * Calling this will also reset the target automatically, so you must build up a new target. 
         */
        moveToTargetInWorkCoords(): void;

        /**
         * Set the motion mode to Rapid (ie. G00 style movement).
         * This setting is modal and does not get reset with the moveToTarget calls from above. 
         */
        setMotionToRapid(): void;

        /**
         * Set the motion mode to linear (ie. G01 style movement).
         * This setting is modal and does not get reset with the moveToTarget calls from above. 
         */
        setMotionToLinear(): void;

        /**
         * Set the feedrate to max.
         * This setting is modal and does not get reset with the moveToTarget calls from above. 
         */
        setFeedrateToMax(): void;

        /**
         * Set the feedrate to a specific value.
         * This setting is modal and does not get reset with the moveToTarget calls from above. 
         */
        setFeedrate(
            feedrate: Number
        ): void;

        /**
         * Perform a tool change. 
         */
        performToolChangeCycle(): void;

        /**
         * Call this when activating the work coordinates for a different WCS, e.g. for a new Setup. This will let simulation know what coordinates are used in subsequent calls to moveToTargetInWorkCoords.
         * This will automatically be done when performing a tool change cycle, but can be called separately as well.
         * @since r50334 
         */
        activateWorkCoordsForNextOperation(): void;

        /**
         * Turn the Tilted Work Plane (TWP) mode ON and align the work plane to the current pose of the machine. This emulates a typical "dynamic work offset" command where the user doesn't set the tilted work plane rotations directly. The machine MUST be rotated into position before activating this function. 
         */
        setTWPModeAlignToCurrentPose(): void;

        /**
         * Turn the Tilted Work Plane (TWP) mode ON and set the work plane orientation by rotations using spatial angles. Spatial Angles are always applied in this order:
         * Step 1: rotation about the Z axis by angle C Step 2: rotation about the new Y axis by angle B Step 3: rotation about the new X axis by angle A
         * All angles are measured in radians and the rotations follow the right hand rule. This command can be called before the machine rotation is performed, as the resulting transformed coordinate system is independent of the machine pose. 
         */
        setTWPModeBySpatialAngles(
            angleA: Number,
        ): void;

        /**
         * Turn the Tilted Work Plane (TWP) mode ON and set the work plane by rotations using euler angles following the passed in Euler Convention. All angles are measured in radians and the rotations follow the right hand rule. The Euler Convention should be the same as used in the {@link Matrix::getEuler2} function.
         * The possible Euler conventions are defined in the builtin.cps and take the form of EULER_&lt;order&gt;_&lt;type&gt; where &lt;order&gt; is the order the three rotations are performed in and &lt;type&gt; is either R for body convention and S for world convention.
         * Example: Euler Convention = EULER_ZXZ_R 
         * All angles are measured in radians and the rotations follow the right hand rule. This command can be called before the machine rotation is performed, as the resulting transformed coordinate system is independent of the machine pose. 
         * - 
Step 1: rotation about the Z axis by angle 1 
         * - 
Step 2: rotation about the new X axis by angle 2 
         * - 
Step 3: rotation about the new Z axis by angle 3 
         */
        setTWPModeByEulerAngles(
            eulerConvention: Integer,
        ): void;

        /**
         * Turn the TWP mode OFF. 
         */
        setTWPModeOff(): void;

        /**
         * Get whether any TWP mode is ON. 
         */
        isTWPModeOn(): Boolean;

        /**
         * Do a rapid retract move along the tool axis until the machine reaches a configured limit. 
         */
        retractAlongToolAxisToLimit(): void;

        /**
         * Set Target X 
         */
        setTargetX(
            x: Number
        ): void;

        /**
         * Set Target Y 
         */
        setTargetY(
            y: Number
        ): void;

        /**
         * Set Target Z 
         */
        setTargetZ(
            z: Number
        ): void;

        /**
         * Set Target A 
         */
        setTargetA(
            a: Number
        ): void;

        /**
         * Set Target B 
         */
        setTargetB(
            b: Number
        ): void;

        /**
         * Set Target C 
         */
        setTargetC(
            c: Number
        ): void;

        /**
         * Turn the TCP mode ON. 
         */
        setTCPModeOn(): void;

        /**
         * Turn the TCP mode OFF. 
         */
        setTCPModeOff(): void;

        /**
         * Get whether any TCP mode is on. 
         */
        isTCPModeOn(): Boolean;

        /**
         * The axis will go in direction of the signed difference. 
         */
        setRotaryToGoProgrammedDirection(): void;

        /**
         * Any rotary movement will go the shortest direction. 
         */
        setRotaryToGoShortestDirection(): void;

        /**
         * Returns the current rotary traverse mode setting. Either ROTARY_DIRECTION_AS_PROGRAMMED or ROTARY_DIRECTION_GO_SHORTEST.
         * @since r50334 
         */
        getRotaryDirection(): Integer;

    }

    /**
     * {@link SMTP} interface for sending e-mails. See {@link Mail}. 
     */
    declare class SMTP {
        /**
         * Constructs a new {@link SMTP} instance. 
         */
        SMTP(
            _hostname: String,
        ): SMTP;

        /**
         * Sends the specified mail. 
         */
        send(
            mail: Mail
        ): void;

    }

    /**
     * A string buffer for managing very long text. 
     */
    declare class StringBuffer {
        /**
         * Initializes the buffer as empty. 
         */
        StringBuffer(): StringBuffer;

        /**
         * Clears the buffer. 
         */
        clear(): void;

        /**
         * Returns true if the buffer is empty. 
         */
        isEmpty(): Boolean;

        /**
         * Appends the specified text. 
         */
        append(
            text: String
        ): void;

        /**
         * Assigns the specified text. 
         */
        assign(
            text: String
        ): void;

        /**
         * Returns the text in the buffer. 
         */
        toString(): String;

    }

    /**
     * {@link String} substitution.
     */
    declare class StringSubstitution {
        /**
         * Constructs the string substituion instance. 
         */
        StringSubstitution(): StringSubstitution;

        /**
         * Sets the value for the specified key.
         * @param name The name. 
         * @param value The value. 
         */
        setValue(
            name: String,
        ): void;

        /**
         * Returns the substituted text. 
         */
        substitute(
            text: String
        ): String;

    }

    /**
     * A lookup table.
     */
    declare class Table {
        /**
         * Constructs a new lookup table.
         * The supported specifiers are:
         * @param values The array of possible values. 
         * @param specifiers The format specifiers.
         */
        Table(
            values: Array<any>, // This generic type have been defaulted to any types.
        ): Table;

        /**
         * Returns the value at the specified index. 
         */
        lookup(
            index: Integer
        ): Value;

        /**
         * Invoke to force output of value on the next invocation of {@link lookup()}. {@link reset()} has no effect if force has been set to true. 
         */
        reset(): void;

    }

    /**
     * A string template.
     */
    declare class Template {
        /**
         * Constructs a string template.
         * @param text The templace text. 
         */
        Template(
            text: String
        ): Template;

        /**
         * Returns the substitute text.
         * @param map The variables. 
         */
        substitute(
            map: Map<any, any> // This generic type have been defaulted to any types.
        ): String;

    }

    /**
     * Text file.
     * Security: Requires security level 0.
     */
    declare class TextFile {
        /**
         * Opens a text file for reading/writing.
         * @param path The path of the file to open. 
         * @param write Specifies if the file should be written or read. 
         * @param encoding The encoding of the text file. 
         */
        TextFile(
            path: String,
        ): TextFile;

        /**
         * Returns true if the file is open. 
         */
        isOpen(): Boolean;

        /**
         * Reads a line. 
         */
        readln(): String;

        /**
         * Writes the specified text without end-of-line.
         * @param text The text to be written. 
         */
        write(
            text: String
        ): void;

        /**
         * Writes the specified text and an end-of-line.
         * @param text The text to be written. 
         */
        writeln(
            text: String
        ): void;

        /**
         * Closes the file. 
         */
        close(): void;

    }

    /**
     * A tool specification. 
     */
    declare class Tool {
        /**
         * Constructs a tool. 
         */
        Tool(): Tool;

        /**
         * Returns the tool id.
         * Note: it is an internal (unique) id of the tool in a Fusion/Inventor document. For instance, when adding/creating a tool (not exist) in the document, a new id is created for the tool, which is added into the document's tool library. 
         */
        getToolId(): String;

        /**
         * Returns the tool number. 
         */
        getNumber(): Integer;

        /**
         * Returns the diameter offset. 
         */
        getDiameterOffset(): Integer;

        /**
         * Returns the length offset. 
         */
        getLengthOffset(): Integer;

        /**
         * Returns the secondary length offset. 
         */
        getSecondaryLengthOffset(): Integer;

        /**
         * Returns true if the tool is a turning tool. 
         */
        isTurningTool(): Boolean;

        /**
         * Returns true if the tool is a jet tool. 
         */
        isJetTool(): Boolean;

        /**
         * Returns the holder number. 
         */
        getHolderNumber(): Integer;

        /**
         * Returns the spindle mode.
         * The available modes are:
         * - SPINDLE_CONSTANT_SPINDLE_SPEED
         * - SPINDLE_CONSTANT_SURFACE_SPEED 
         */
        getSpindleMode(): Integer;

        /**
         * Returns the spindle speed in RPM (rotations per minute). 
         */
        getSpindleRPM(): Number;

        /**
         * Returns the ramping spindle speed in RPM (rotations per minute). 
         */
        getRampingSpindleRPM(): Number;

        /**
         * Returns true if the spindle rotates in clockwise direction. 
         */
        isClockwise(): Boolean;

        /**
         * Returns the surface speed (CSS). 
         */
        getSurfaceSpeed(): Number;

        /**
         * Returns the maximum spindle speed (RPM) when using surface speed (CSS). 
         */
        getMaximumSpindleSpeed(): Number;

        /**
         * Returns the number of flutes. 
         */
        getNumberOfFlutes(): Integer;

        /**
         * Returns the number of threads per unit of length. 
         */
        getThreadPitch(): Number;

        /**
         * Returns the tapping feedrate as spindle RPM * thread pitch. 
         */
        getTappingFeedrate(): Number;

        /**
         * Returns true if the tool is a drill. 
         */
        isDrill(): Boolean;

        /**
         * Returns the coolant mode. 
         */
        getCoolant(): Integer;

        /**
         * Returns the material. 
         */
        getMaterial(): Integer;

        /**
         * Returns the tool description. 
         */
        getDescription(): String;

        /**
         * Returns the tool comment. 
         */
        getComment(): String;

        /**
         * Returns the tool vendor. 
         */
        getVendor(): String;

        /**
         * Returns the tool product id. 
         */
        getProductId(): String;

        /**
         * Returns the holder description. 
         */
        getHolderDescription(): String;

        /**
         * Returns the holder comment. 
         */
        getHolderComment(): String;

        /**
         * Returns the holder vendor. 
         */
        getHolderVendor(): String;

        /**
         * Returns the holder product id. 
         */
        getHolderProductId(): String;

        /**
         * Returns the aggregate id. 
         */
        getAggregateId(): String;

        /**
         * Returns the tool unit.
         * The available values are:
         * - IN - Inches
         * - MM - Millimeters 
         */
        getUnit(): Integer;

        /**
         * Returns the tool type.
         * The types are:
         * - TOOL_UNSPECIFIED - Unspecified tool.
         * - TOOL_DRILL - Drill.
         * - TOOL_DRILL_CENTER - Center drill.
         * - TOOL_DRILL_SPOT - Spot drill.
         * - TOOL_DRILL_BLOCK - Block drill.
         * - TOOL_MILLING_END_FLAT - Flat end-mill.
         * - TOOL_MILLING_END_BALL - Ball end-mill.
         * - TOOL_MILLING_END_BULLNOSE - Bullnose mill.
         * - TOOL_MILLING_CHAMFER - Chamfer mill.
         * - TOOL_MILLING_FACE - Face mill.
         * - TOOL_MILLING_SLOT - Slot mill.
         * - TOOL_MILLING_RADIUS - Radius mill.
         * - TOOL_MILLING_DOVETAIL - Dovetail mill.
         * - TOOL_MILLING_TAPERED - Tapered mill.
         * - TOOL_MILLING_LOLLIPOP - Lollipop mill.
         * - TOOL_TAP_RIGHT_HAND - Right tap tool.
         * - TOOL_TAP_LEFT_HAND - Left tap tool.
         * - TOOL_REAMER - Reamer tool.
         * - TOOL_BORING_BAR - Boring bar tool.
         * - TOOL_COUNTER_BORE - Counterbore tool.
         * - TOOL_COUNTER_SINK - Countersink tool.
         * - TOOL_HOLDER_ONLY - Holder.
         * - TOOL_TURNING_GENERAL - General turning tool.
         * - TOOL_TURNING_THREADING - Thread turning tool.
         * - TOOL_TURNING_GROOVING - Groove turning tool.
         * - TOOL_TURNING_BORING - Boring turning tool.
         * - TOOL_TURNING_CUSTOM - Custom turning tool.
         * - TOOL_PROBE - Probe.
         * - TOOL_WIRE - Wire.
         * - TOOL_WATER_JET - Water jet.
         * - TOOL_LASER_CUTTER - Laser cutter.
         * - TOOL_WELDER - Welder.
         * - TOOL_GRINDER - Grinder.
         * - TOOL_MILLING_FORM - Form mill.
         * - TOOL_PLASMA_CUTTER - Plasma cutter.
         * - TOOL_MARKER - Marker tool.
         * - TOOL_MILLING_THREAD - Thread mill. 
         */
        getType(): Integer;

        /**
         * Returns the cutter diameter. 
         */
        getDiameter(): Number;

        /**
         * Returns the tip diameter. 
         */
        getTipDiameter(): Number;

        /**
         * Returns the corner radius. 
         */
        getCornerRadius(): Number;

        /**
         * Returns the taper angle in radians. 
         */
        getTaperAngle(): Number;

        /**
         * Returns the flute length. 
         */
        getFluteLength(): Number;

        /**
         * Returns the shoulder length. 
         */
        getShoulderLength(): Number;

        /**
         * Returns the shaft diameter. 
         */
        getShaftDiameter(): Number;

        /**
         * Returns the body length. 
         */
        getBodyLength(): Number;

        /**
         * Returns the entire length of the tool.
         * @since 45845 
         */
        getOverallLength(): Number;

        /**
         * Returns the assembly gauge length. 
         */
        getAssemblyGaugeLength(): Number;

        /**
         * Returns the shaft. 
         */
        getShaft(): Shaft;

        /**
         * Deprecated (not used). 
         * @deprecated XXXXX 
         */
        getJetDistance(): Number;

        /**
         * @since XXXXX 
         */
        getJetDiameter(): Number;

        /**
         * @since XXXXX 
         */
        getKerfWidth(): Number;

        /**
         * When set to "manual" the programmed feedrates will be used, "automatic" will disable feedrate output and the machine control will handle the feedrates of the toolpaths. The post processor must support this setting for "automatic " to have any effect on the output. 
         * @since XXXXX 
         */
        getMachineQualityControl(): String;

        /**
         * @since XXXXX 
         */
        getCutHeight(): Number;

        /**
         * @since XXXXX 
         */
        getPierceHeight(): Number;

        /**
         * @since XXXXX 
         * @note PSI if tool units are IN, BAR if tool units are MM. 
         */
        getPressure(): Number;

        /**
         * @since XXXXX 
         */
        getPierceTime(): Number;

        /**
         * @since XXXXX 
         * @note Unitless. 
         */
        getAbrasiveFlowRate(): Number;

        /**
         * @since XXXXX 
         */
        getPiercePower(): Number;

        /**
         * @since XXXXX 
         */
        getCutPower(): Number;

        /**
         * @since XXXXX 
         */
        getAssistGas(): String;

        /**
         * Returns the tip diameter of the holder. 
         */
        getHolderTipDiameter(): Number;

        /**
         * Returns the maximum holder diameter. 
         */
        getHolderDiameter(): Number;

        /**
         * Returns the total holder length. 
         */
        getHolderLength(): Number;

        /**
         * Returns the holder. 
         */
        getHolder(): Holder;

        /**
         * Returns the boring bar orientation in radians. 
         */
        getBoringBarOrientation(): Number;

        /**
         * Returns the primary compensation offset for turning. 
         */
        getCompensationOffset(): Integer;

        /**
         * Returns the secondary compensation offset for turning. 
         */
        getSecondaryCompensationOffset(): Integer;

        /**
         * Returns true if the tool must be manually changed. 
         */
        getManualToolChange(): Boolean;

        /**
         * Returns true if break control is enabled. 
         */
        getBreakControl(): Boolean;

        /**
         * Returns true if the tool is live. Otherwise tool is static (ie. mounted without a spindle).
         * @since 41222 
         */
        isLiveTool(): Boolean;

        /**
         * Returns the turret for turning. 
         */
        getTurret(): Integer;

        /**
         * Returns the ISO/ANSI insert code for turning tool.
         * @since 50320 
         */
        getInsertStandardCode(): String;

        /**
         * Returns the turning insert type.
         * The available types are:
         * - TURNING_INSERT_USER_DEFINED
         * - TURNING_INSERT_ISO_A - 85deg parallelogram - 85deg
         * - TURNING_INSERT_ISO_B - 82deg parallelogram - 82deg
         * - TURNING_INSERT_ISO_C - 80deg diamond - 80deg
         * - TURNING_INSERT_ISO_D - 55deg diamond - 55deg
         * - TURNING_INSERT_ISO_E - 75deg diamond - 75deg
         * - TURNING_INSERT_ISO_H - hexagon - 120deg
         * - TURNING_INSERT_ISO_K - 55deg parallelogram - 55deg
         * - TURNING_INSERT_ISO_L - rectangle - 90deg
         * - TURNING_INSERT_ISO_M - 86deg diamond - 86deg
         * - TURNING_INSERT_ISO_O - octagon - 135deg
         * - TURNING_INSERT_ISO_P - pentagon - 108deg
         * - TURNING_INSERT_ISO_R - round - full radius
         * - TURNING_INSERT_ISO_S - square - 90deg
         * - TURNING_INSERT_ISO_T - triangle - 60deg
         * - TURNING_INSERT_ISO_V - 35deg diamond - 35deg
         * - TURNING_INSERT_ISO_W - trigon - 80deg
         * - TURNING_INSERT_GROOVE_ROUND
         * - TURNING_INSERT_GROOVE_RADIUS
         * - TURNING_INSERT_GROOVE_SQUARE
         * - TURNING_INSERT_GROOVE_CHAMFER
         * - TURNING_INSERT_GROOVE_40DEG
         * - TURNING_INSERT_THREAD_ISO_DOUBLE_FULL
         * - TURNING_INSERT_THREAD_ISO_TRIPLE_FULL
         * - TURNING_INSERT_THREAD_UTS_DOUBLE_FULL
         * - TURNING_INSERT_THREAD_UTS_TRIPLE_FULL
         * - TURNING_INSERT_THREAD_ISO_DOUBLE_VPROFILE
         * - TURNING_INSERT_THREAD_ISO_TRIPLE_VPROFILE
         * - TURNING_INSERT_THREAD_UTS_DOUBLE_VPROFILE
         * - TURNING_INSERT_THREAD_UTS_TRIPLE_VPROFILE 
         */
        getInsertType(): Integer;

        /**
         * Returns the turning holder type.
         * The available types are:
         * - HOLDER_NONE
         * - HOLDER_ISO_A
         * - HOLDER_ISO_B
         * - HOLDER_ISO_C
         * - HOLDER_ISO_D
         * - HOLDER_ISO_E
         * - HOLDER_ISO_F
         * - HOLDER_ISO_G
         * - HOLDER_ISO_H
         * - HOLDER_ISO_J
         * - HOLDER_ISO_K
         * - HOLDER_ISO_L
         * - HOLDER_ISO_M
         * - HOLDER_ISO_N
         * - HOLDER_ISO_P
         * - HOLDER_ISO_Q
         * - HOLDER_ISO_R
         * - HOLDER_ISO_S
         * - HOLDER_ISO_T
         * - HOLDER_ISO_U
         * - HOLDER_ISO_V
         * - HOLDER_ISO_W
         * - HOLDER_ISO_Y
         * - HOLDER_OFFSET_PROFILE
         * - HOLDER_STRAIGHT_PROFILE
         * - HOLDER_GROOVE_EXTERNAL
         * - HOLDER_GROOVE_INTERNAL
         * - HOLDER_GROOVE_FACE
         * - HOLDER_THREAD_STRAIGHT
         * - HOLDER_THREAD_OFFSET
         * - HOLDER_THREAD_FACE
         * - HOLDER_BORING_BAR_ISO_F 0 deg end clearance
         * - HOLDER_BORING_BAR_ISO_G 0 deg side clearance
         * - HOLDER_BORING_BAR_ISO_J -3 deg side
         * - HOLDER_BORING_BAR_ISO_K 15 deg end clearance
         * - HOLDER_BORING_BAR_ISO_L -5 deg side clearance
         * - HOLDER_BORING_BAR_ISO_P -27.5 deg end clearance
         * - HOLDER_BORING_BAR_ISO_Q 17.5 deg end clearance
         * - HOLDER_BORING_BAR_ISO_S 45 deg end
         * - HOLDER_BORING_BAR_ISO_U -3 deg end clearance
         * - HOLDER_BORING_BAR_ISO_W -30 deg end
         * - HOLDER_BORING_BAR_ISO_Y 5 deg end
         * - HOLDER_BORING_BAR_ISO_X 5 deg back boring 
         */
        getHolderType(): Integer;

        /**
         * Returns the turning compensation mode.
         * The available modes are:
         * - TOOL_COMPENSATION_INSERT_CENTER
         * - TOOL_COMPENSATION_TIP
         * - TOOL_COMPENSATION_TIP_CENTER
         * - TOOL_COMPENSATION_TIP_TANGENT 
         */
        getCompensationMode(): Integer;

        /**
         * Returns the turning compensation displacement for the toolpath. The Z-coordinate is always 0.
         * @since 41366 
         */
        getCompensationDisplacement(): Vector;

        /**
         * Returns the tool extent.
         * @param includeHolder Set to true to include the extent of the holder.
         * @since 41366 
         */
        getExtent(
            includeHolder: Boolean
        ): BoundingBox;

        /**
         * Returns the profile of the cutter as a {@link Curve}.
         * @since 41366 
         */
        getCutterProfile(): Curve;

        /**
         * Returns the profile of the holder as a {@link Curve}.
         * @since 41366 
         */
        getHolderProfile(): Curve;

        /**
         * Returns the SVG profile of the cutter.
         * @since 41366 
         */
        getCutterProfileAsSVGPath(): String;

        /**
         * Returns the SVG profile of the holder.
         * @since 41366 
         */
        getHolderProfileAsSVGPath(): String;

        /**
         * Returns the cutter as a mesh.
         * @param tolerance The linearization tolerance.
         * @since 42020
         */
        getCutterAsMesh(
            tolerance: Number
        ): Mesh;

        /**
         * Returns the holder as a mesh.
         * @param tolerance The linearization tolerance.
         * @since 42020 
         */
        getHolderAsMesh(
            tolerance: Number
        ): Mesh;

        /* The tool number. */
        number: Integer;

        /* The turret. */
        turret: Integer;

        /* The diameter offset (used for milling). */
        diameterOffset: Integer;

        /* The length offset (used for milling). */
        lengthOffset: Integer;

        /* The compensation offset (used for turning). */
        compensationOffset: Integer;

        /* True if tool must be manually changed. */
        manualToolChange: Boolean;

        /* True if break control is enabled. */
        breakControl: Boolean;

        /* True if the tool is live - otherwise it is static. */
        liveTool: Boolean;

        /* Number identifying the holder. */
        holderNumber: Integer;

        /* The spindle mode. */
        spindleMode: Integer;

        /* The spindle speed in RPM. Positive for clockwise direction. */
        spindleRPM: Number;

        /* The spindle speed in RPM for ramping. Positive for clockwise direction. */
        rampingSpindleRPM: Number;

        /* The surface speed (CSS). */
        surfaceSpeed: Number;

        /* The maximum spindle speed (RPM) when using surface speed (CSS). */
        maximumSpindleSpeed: Number;

        /* The number of flutes. */
        numberOfFlutes: Integer;

        /* The number of thread per unit of length. */
        threadPitch: Number;

        /* The coolant. */
        coolant: Integer;

        /* The material. */
        material: Integer;

        /* Comment. */
        comment: String;

        /* The vendor. */
        vendor: String;

        /* The product id. */
        productId: String;

        /* The unit. */
        unit: Integer;

        /* The tool type. */
        type: Integer;

        /* The diameter. */
        diameter: Number;

        /* The corner radius. */
        cornerRadius: Number;

        /* The taper angle. */
        taperAngle: Number;

        /* The flute length. */
        fluteLength: Number;

        /* The shoulder length. */
        shoulderLength: Number;

        /* The shaft diameter. */
        shaftDiameter: Number;

        /* The body length. */
        bodyLength: Number;

        /* The entire length of the tool. */
        overallLength: Number;

        /* The tool shaft. */
        shaft: Shaft;

        /* The holder tip diameter. */
        holderTipDiameter: Number;

        /* The holder diameter. */
        holderDiameter: Number;

        /* The holder length. */
        holderLength: Number;

        /* The tool holder. */
        holder: Holder;

        /* The boring bar orientation in radians. */
        boringBarOrientation: Number;

        /* The inscribed circle diameter for turning tool. */
        inscribedCircleDiameter: Number;

        /* The edge length for turning tool. */
        edgeLength: Number;

        /* The nose radius for turning tools. */
        noseRadius: Number;

        /* The relief angle in degrees. */
        reliefAngle: Number;

        /* The turning tool thickness;. */
        thickness: Number;

        /* The groove tool width. */
        grooveWidth: Number;

        /* The cross section type for turning tools. */
        crossSection: String;

        /* The tolerance for turning tools. */
        tolerance: String;

        /* The thread pitch for turning tools. */
        pitch: Number;

        /* The holder hand. Left, Right, or Neutral. */
        hand: String;

        /* Clamping for turning tools. */
        clamping: String;

        /* The jet distance.  More... */
        jetDistance: Number;

        /* The jet diameter. */
        jetDiameter: Number;

        /* The kerf width. */
        kerfWidth: Number;

        /* The machine quality control. */
        machineQualityControl: String;

        /* The cut height. */
        cutHeight: Number;

        /* The pierce height. */
        pierceHeight: Number;

        /* The pressure. */
        pressure: Number;

        /* The pierce time. */
        pierceTime: Number;

        /* The abrasive flow rate. */
        abrasiveFlowRate: Number;

        /* The pierce power. */
        piercePower: Number;

        /* The cut power. */
        cutPower: Number;

        /* The assist gas. */
        assistGas: String;

    }

    /**
     * {@link Tool} to image converter. 
     */
    declare class ToolRenderer {
        /**
         * Initializes the tool renderer. 
         */
        ToolRenderer(): ToolRenderer;

        /**
         * Sets the tool segments to render.
         * @param segment The segment which can be TOOL_FLUTE, TOOL_SHOULDER, or TOOL_SHAFT. 
         */
        setSegment(
            segment: Integer
        ): void;

        /**
         * Sets the background color. 
         */
        setBackgroundColor(
            color: Color
        ): void;

        /**
         * Sets the color of the flute. 
         */
        setFluteColor(
            color: Color
        ): void;

        /**
         * Sets the color of the shoulder. 
         */
        setShoulderColor(
            color: Color
        ): void;

        /**
         * Sets the color of the shaft. 
         */
        setShaftColor(
            color: Color
        ): void;

        /**
         * Sets the color of the holder. 
         */
        setHolderColor(
            color: Color
        ): void;

        /**
         * Exports the tool image to the specified path.
         * @param path The destination path. 
         * @param mimetype The MIME type of the image (must be "image/png"). 
         * @param tool The tool to render. 
         * @param width The width of the image. 
         * @param height The height of the image. 
         */
        exportAs(
            path: String,
        ): void;

        /**
         * Exports the tool image to a binary string.
         * @param mimetype The MIME type of the image (must be "image/png"). 
         * @param tool The tool to render. 
         * @param width The width of the image. 
         * @param height The height of the image.
         * @since 41349 
         */
        getAsBinary(
            mimetype: String,
        ): String;

    }

    /**
     * {@link Tool} table. 
     */
    declare class ToolTable {
        /**
         * Returns the number of tools in the tool table. 
         */
        getNumberOfTools(): Integer;

        /**
         * Returns the tool at the specified index. The index must be in the range [0; {@link getNumberOfTools()}]. 
         */
        getTool(
            index: Integer
        ): Tool;

    }

    /**
     * JavaScript value. Can be either {@link Boolean}, {@link Integer}, {@link Number}, or {@link String}. 
     */
    declare class Value {
    }

    /**
     * A formatting variable. Instances of this class are used to format incoming values. State is maintained to avoid output for unchanged values.
     */
    declare class Variable {
        /**
         * Constructs a variable.
         * The supported specifiers are:
         * @param specifiers The specifiers. 
         * @param format The desired format.
         */
        Variable(
            specifiers: Map<any, any>, // This generic type have been defaulted to any types.
        ): Variable;

        /**
         * Returns the formatted string of the specified value. The empty string is returned if the value is identical to the last formatted value. 
         */
        format(
            value: Number
        ): String;

        /**
         * Returns the prefix. 
         */
        getPrefix(): Value;

        /**
         * Sets the prefix. 
         */
        setPrefix(
            prefix: Value
        ): void;

        /**
         * Disable variable. When invoked {@link format()} always returns the empty string. 
         */
        disable(): void;

        /**
         * Invoke to force output of value on the next invocation of {@link format()}. {@link reset()} has no effect if force has been set to true for the given format. 
         */
        reset(): void;

        /**
         * Returns the last formatted value. 
         */
        getCurrent(): Value;

    }

    /**
     * A 3D vector (X, Y, and Z coordinates). 
     */
    declare class Vector {
        /**
         * Constructs a (0, 0, 0) vector. 
         */
        Vector(): Vector;

        /**
         * Constructs a vector with the specified x, y, and z coordinates. 
         */
        Vector(
            x: Number,
        ): Vector;

        /**
         * Returns the X coordinate. 
         */
        getX(): Number;

        /**
         * Sets the X coordinate. 
         */
        setX(
            x: Number
        ): void;

        /**
         * Returns the Y coordinate. 
         */
        getY(): Number;

        /**
         * Sets the Y coordinate. 
         */
        setY(
            y: Number
        ): void;

        /**
         * Returns the Z coordinate. 
         */
        getZ(): Number;

        /**
         * Sets the Z coordinate. 
         */
        setZ(
            z: Number
        ): void;

        /**
         * Returns the specified coordinate (0:X, 1:Y, and 2:Z). 
         */
        getCoordinate(
            coordinate: Integer
        ): Number;

        /**
         * Sets the specified coordinate (0:X, 1:Y, and 2:Z). 
         */
        setCoordinate(
            coordinate: Integer,
        ): void;

        /**
         * Adds the specified vector. 
         */
        add(
            value: Vector
        ): void;

        /**
         * Adds the specified X, Y, and Z coordinates. 
         */
        add(
            x: Number,
        ): void;

        /**
         * Subtracts the specified vector. 
         */
        subtract(
            value: Vector
        ): void;

        /**
         * Subtracts the specified X, Y, and Z coordinates. 
         */
        subtract(
            x: Number,
        ): void;

        /**
         * Multiplies the specified value. 
         */
        multiply(
            value: Number
        ): void;

        /**
         * Divides the vector by the specified value. 
         */
        divide(
            value: Number
        ): void;

        /**
         * Returns true if the vector has non-zero length.
         * 'tolerance' is available: 
         * @param tolerance An optional comparison tolerance/accuracy. If 'tolerance' is greater than or equal to 1 then 'tolerance' specifies the number of digits to the right of the decimal to truncate the vector to prior to comparison.
         * @since r51671 
         */
        isNonZero(
            tolerance: Number
        ): Boolean;

        /**
         * Returns true if the vector is zero length.
         * 'tolerance' is available: 
         * @param tolerance An optional comparison tolerance/accuracy. If 'tolerance' is greater than or equal to 1 then 'tolerance' specifies the number of digits to the right of the decimal to truncate the vector to prior to comparison.
         * @since r51671 
         */
        isZero(
            tolerance: Number
        ): Boolean;

        /**
         * Returns the angle in the X-Y plane (spherical coordinate). 
         */
        getXYAngle(): Number;

        /**
         * Returns the Z angle relative to the X-Y plane (spherical coordinate). 
         */
        getZAngle(): Number;

        /**
         * Returns the length of this vector. 
         */
        getLength(): Number;

        /**
         * Returns the square length of this vector. 
         */
        getLength2(): Number;

        /**
         * Normalizes the vector. 
         */
        normalize(): void;

        /**
         * Returns the normalized vector. 
         */
        getNormalized(): Vector;

        /**
         * Negates the vector. 
         */
        negate(): void;

        /**
         * Returns the negated vector. 
         */
        getNegated(): Vector;

        /**
         * Returns the vector with the absolute values of the coordinates. 
         */
        getAbsolute(): Vector;

        /**
         * Returns the value for the minimum coordinate. 
         */
        getMinimum(): Number;

        /**
         * Returns the value for the maximum coordinate. 
         */
        getMaximum(): Number;

        /**
         * Converts the vector to a string (e.g. (1, 2, 3)). 
         */
        toString(): String;

        /**
         * Returns the value converted from radians to degrees. 
         */
        toDeg(): Vector;

        /**
         * Returns the value converted from degrees to radians. 
         */
        toRad(): Vector;

        /**
         * Returns the sum of the specified vectors. 
         */
        static sum(
            left: Vector,
        ): Vector;

        /**
         * Returns the difference of the specified vectors. 
         */
        static diff(
            left: Vector,
        ): Vector;

        /**
         * Returns the product of the specified vector and number. 
         */
        static product(
            left: Vector,
        ): Vector;

        /**
         * Returns the dot product of this vector and the specified vector. 
         */
        static dot(
            left: Vector,
        ): Number;

        /**
         * Returns the angle between the specified vectors. 
         */
        static getAngle(
            v1: Vector,
        ): Number;

        /**
         * Returns the cross product of this vector and the specified vector. 
         */
        static cross(
            left: Vector,
        ): Vector;

        /**
         * Returns the distance between the specified vectors. 
         */
        static getDistance(
            left: Vector,
        ): Number;

        /**
         * Returns the square of the distance between the specified vectors. 
         */
        static getDistance2(
            left: Vector,
        ): Number;

        /**
         * Linear interpolation between the specified vectors. The U coordinate is clamped to the range [0; 1]. U equal to 0 and 1 corresponds to the left vector and the right vector, respectively. 
         */
        static lerp(
            left: Vector,
        ): Vector;

        /**
         * Returns true if two vectors are parallel.
         * @param a First vector to compare. 
         * @param b Second vector to compare. 
         * @param tolerance An optional comparison tolerance/accuracy. If 'tolerance' is greater than or equal to 1 then 'tolerance' specifies the number of digits to the right of the decimal to truncate the vectors to prior to comparison.
         * @since r51671 
         */
        static areParallel(
            a: Vector,
        ): Boolean;

        /**
         * Returns true if two vectors are perpendicular.
         * @param a First vector to compare. 
         * @param b Second vector to compare. 
         * @param tolerance An optional comparison tolerance/accuracy. If 'tolerance' is greater than or equal to 1 then 'tolerance' specifies the number of digits to the right of the decimal to truncate the vectors to prior to comparison.
         * @since r51671 
         */
        static arePerpendicular(
            a: Vector,
        ): Boolean;

        /**
         * Returns true if two vectors are in the same direction.
         * @param a First vector to compare. 
         * @param b Second vector to compare. 
         * @param tolerance An optional comparison tolerance/accuracy. If 'tolerance' is greater than or equal to 1 then 'tolerance' specifies the number of digits to the right of the decimal to truncate the vectors to prior to comparison.
         * @since r51671 
         */
        static areSameDirection(
            a: Vector,
        ): Boolean;

        /**
         * Returns true if two vectors are different.
         * @param a First vector to compare. 
         * @param b Second vector to compare. 
         * @param tolerance An optional comparison tolerance/accuracy. If 'tolerance' is greater than or equal to 1 then 'tolerance' specifies the number of digits to the right of the decimal to truncate the vectors to prior to comparison.
         * @since r51671 
         */
        static areDifferent(
            a: Vector,
        ): Boolean;

        /**
         * Returns the distance between a point and a line of infinite length.
         * @param point Point used to calculate distance to line. 
         * @param start First point on infinite line. 
         * @param end Second point on infinite line.
         * @since r51671 
         */
        static pointLineDistance(
            point: Vector,
        ): Number;

        /**
         * Returns the distance between a point and a line segment. If the projected point is outside of the line segment, then the distance to the closest end point of the line segment is returned.
         * @param point Point used to calculate the distance to line. 
         * @param start Start point of line segment. 
         * @param end End point of line segment.
         * @since r51671 
         */
        static pointSegmentDistance(
            point: Vector,
        ): Number;

        /**
         * Projects a point to a line of infinite length.
         * @param point Point to project onto line. 
         * @param start First point on infinite line. 
         * @param end Second point on infinite line.
         * @since r51671 
         */
        static projectPointToLine(
            point: Vector,
        ): Vector;

        /**
         * Returns the distance between a point and a plane.
         * @param point Point used to calculate the distance to plane. 
         * @param planePoint Point on plane. 
         * @param planeNormal Vector normal to plane.
         * @since r51671 
         */
        static pointPlaneDistance(
            point: Vector,
        ): Number;

        /**
         * Projects a point to a plane.
         * @param point Point to project onto plane. 
         * @param planePoint Point on plane. 
         * @param planeNormal Vector normal to plane.
         * @since r51671 
         */
        static projectPointToPlane(
            point: Vector,
        ): Vector;

        /**
         * Translates a point along a vector at a given distance.
         * @param point Point to translate. 
         * @param vector Vector to translate point along. 
         * @param distance Distance to translate point.
         * @since r51671 
         */
        static translatePoint(
            point: Vector,
        ): Vector;

        /**
         * Returns the vector for the specified spherical coordinates. 
         */
        static getBySpherical(
            xyAngle: Number,
        ): Vector;

        /* The X coordinate. */
        x: Number;

        /* The Y coordinate. */
        y: Number;

        /* The Z coordinate. */
        z: Number;

        /* The length of the vector. */
        length: Number;

        /* The square of the length of the vector. */
        length2: Number;

        /* The negated vector. */
        negated: Vector;

        /* The vector with the absolute coordinates. */
        abs: Vector;

        /* The vector normalized to length 1. */
        normalized: Vector;

    }

    /**
     * Two vectors. 
     */
    declare class VectorPair {
        first: Vector;

        second: Vector;

    }

    /**
     * {@link XMLHttpRequest} support.
     * Security: Requires security level 0.
     * @since 41366 
     */
    declare class XMLHttpRequest {
        /**
         * Constructs an {@link XMLHttpRequest} object. 
         */
        XMLHttpRequest(): XMLHttpRequest;

        /**
         * Initializes the request.
         * @param method The request method. 
         * @param url The connection url. 
         * @param async True for asynchronous. Not supported. 
         * @param user The user. 
         * @param password The password. 
         */
        open(
            method: String,
        ): void;

        /**
         * Aborts the request. 
         */
        abort(): void;

        /**
         * Sets the value of request header field. Must be called before {@link send()}. If the header field already exists, the value will be appended to the previous value.
         * @param name The name of the header field. 
         * @param value The value of the header field. 
         */
        setRequestHeader(
            name: String,
        ): void;

        /**
         * Sends the request.
         * @param data The request body data.(unused for a GET request) 
         */
        send(
            data: String
        ): void;

        /**
         * Returns the value of response header field. Returns an empty string if the requested header field is not present or empty in the response
         * @param name The name of the header field. Precondition: open() and send() called 
         */
        getResponseHeader(
            name: String
        ): String;

        /* The request response. */
        response: String;

        /* Not supported. */
        responseXML: String;

        /* The status. */
        status: Integer;

        /* The status text. */
        statusText: String;

    }

    /**
     * ZIP file.
     * Security: Requires security level 0. 
     */
    declare class ZipFile {
        /**
         * Zips the specified folder content to the specified file.
         * @param src The path of the source folder. 
         * @param dest The path of the ZIP file. 
         */
        static unzipTo(
            src: String,
        ): void;

        /**
         * Unzips the specified file to the specified folder.
         * @param src The path of the ZIP file. 
         * @param dest The path of the destination folder. 
         */
        static zipTo(
            src: String,
        ): void;

    }

}
